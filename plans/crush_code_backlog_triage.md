# crush_code hardening backlog — triage ledger

**Derived 2026-09-10 at master `3bf356bd6`..`170bd49be` (the two differ by root `composer.lock` only — 11 lines, nothing under `src/` or `tests/`), PHP 8.3.6.** Companion to `docs/plans/crush_code_hardening_backlog.md`, whose `### E<n>` headings carry the one-word stamp. This file carries the evidence, the confidence, and the notes — and the notes are the part that makes a stamp safe to act on. **Round-61 closeout 2026-09-10 at `0549736d4`: 15 rows moved CLOSED, E649 → PARTIAL, E652–E662 added from the lane seams under `/home/sites/crush-r61-artifacts/`. The `### E652+` headings are NOT yet minted in the companion backlog — mint them at the next backlog-touching close.** **Round-62 closeout 2026-09-10 at `1a6ef5f60`: 22 rows CLOSED (21 moves + E663 born-closed), E42/E43/E653 → PARTIAL, E537 re-stamped (stays NO-FIX, STEP retargeted per lane Y), E664–E671 filed (8). Companion `### E652+` minting remains outstanding, and its E650/E651 one-word stamps lag this ledger.** **Round-63 closeout 2026-09-10 at `8c52b26e5`: 29 rows moved CLOSED, E440/E214/E261 → PARTIAL, E41 CLOSED (was PARTIAL), E43/E633 re-annotated (still PARTIAL), Phase-9 layer A CLOSED (Phase 9 → PARTIAL, layer B/C param seam absent-pinned), E672–E687 minted (16). Companion one-word stamps for the moved ids re-synced the same date; the E652+ minting debt persists.** **Round-64 closeout 2026-09-11 at `e028f142c`: 13 rows CLOSED, E678/E686 → PARTIAL, E688 born+closed same round, E689–E692 filed (4). Actionable 131 → 117.** **Round-65 closeout 2026-09-11 at `0c61c0686`: 6 rows CLOSED (E653 — Shape A, Shape B follow-up noted; E682; E683; E689; E690; E692 CLOSED-WITH-DECISION — the in-process exec-free fork is out of exec-containment's remit, the scope call landed as a comment at the fork site), E686 re-annotated (tranche-2 landed, DocFigure arms = 12), E691 re-annotated (stays OPEN — coverage job untouched by design in r65), the `### E652–E688` backlog headings minted at `25924d512` (the standing minting debt is CLOSED). Actionable 117 → 111.** **Round-66 closeout 2026-09-11 at `61cde19c5`: 11 rows CLOSED — `E657`/`E658`/`E659`/`E662` (ca, master `31b6b94df`, with the E658 ledger erratum: true split ~77 fixed/~35 held of 112 raw units, not "31/81"), `E670`/`E687` (cd, master `7f352c691`, census trio in-step), `E691` (cc, master `a59d8a547`, per-shard clover + `scripts/merge-clover.php`), `E678` CLOSED-**superseded** (ch verdict: `mcpServerInventory` `a4be8263f` predates the panel — original chronology inverted; `--json` rows `b76fee3cd`; panel `6220e5b29`/`a6768adde`), plus the three measure-first closers `E261` (teardown log = E687 shipped), `E440` (both mount seams = E677 `b76fee3cd` + E690 `858614688`), `E42` (heading said OPEN while this ledger said PARTIAL — reconciled CLOSED: `_5F` injective `0373308a3` + surfacing chain `fa4f40ed4`→`b76fee3cd`→`6220e5b29`). `E653` re-stamped **FULL CLOSED** (Shape B `/notices` landed `5010c8bb2` — the "follow-up" wording retired; the ce REPORT's '+13th-case = SlashDispatch provider row' attribution was FALSE — the 13th case's home was never attributed; the measured figure re-pin supersedes). `E686` re-annotated (tranche-3 landed `51e59798d`, DocFigure arms 12→18; tranche-4 carry = HOOKS `CRUSH_*` 7×/8× dynamic-roster claims). `E43` re-measured — **stays PARTIAL**: the adoption half (per-block reflow-vs-clip toggle) has zero trace at tip; carrier E682 closed only the palette-paint rule. Two NO-OP lane verdicts recorded: cg (E653-B already at tip — duplicate brief caught pre-work), ch (E678 half already landed). Actionable **111 → 100** (11 moves; E653 was already out of the count).** File-map row census note: the regenerated `crush_code_filemap.md` table stamps 106 OPEN-family rows — a +6 drift over the ledger that pre-dates round 66 (the `0c61c0686` cut carried 117 rows vs ledger 111); per the rounds-64–66 precedent the header arithmetic above is authoritative and the surplus six rows are flagged for evidence-based reconciliation at the round-67 close.

**Round-67 WAVE-1 closeout 2026-09-11 at master `fb5078e6d` (WAVE 2 PENDING, NOT pushed):** **39 rows CLOSED** — da `d3fb349fd` (E572, E577, E578, E612, E615, E617) · db `0e9ce9e05` (E195, E258, E259, E267, E269, E270) · dc `18daf2d55` (E174, E205, E235, E283, E343, E417-closed-no-code) · dd `4d58dadd7` (E111, E125, E127, E143, E144, E148, E149, E357, E386) · de `2da286745`+`2973380d4` (E4, E17, E20, E175 — E154's (c) site rides de; E154 stays PARTIAL, remainder `Bootstrap.php:5722` → wave-2) · df `7fbc7636b`/`436965761`/`aa2497e07`/`5b6bb6b1b`/`1a4c39fc4` (E366, E370, E447-closed-refuted, E448, E466, E486, E566, E629). E3 re-stamped OPEN→PARTIAL (input half landed; caret seam refused on ownership). **REFUSED this close (trust-git rule):** E228 (no message/pickaxe trace in `0e9ce9e05` or any r67 pick — stays PARTIAL) and E214 (`9f0c5db62` is a +4-line E20 follow-through, not the widening decision — stays PARTIAL). The briefs' **"E42b" has no ledger row and no git trace** — the F1 heal `351d5a915` cites **E137** (already CLOSED r63); recorded as naming errata at the E137 row. Tooling: E671 local-K=8 addendum `fb5078e6d`. Actionable **100 → 61**. Filemap regen DEFERRED to the round-67 close (wave 2 after merge).

**Round-67 WAVE-2 close + ROUND-67 CLOSED 2026-09-12 at master `323605773` (NEVER pushed):** **12 rows CLOSED** — dh `b08a96cf1` (E45, E74 — cached-empty-refusal FREEZE, a real fix, reviewer non-vacuity pin — , E78c CLOSED-with-evidence NO-OP, E154 — both remainder sites accounted: :5722 already-prefixed with a three-week erratum comment corrected, :8174 via de's E154c) · dj `00c8bc14b` (E342, E358; **E319 stays PARTIAL** — inversion dead but three stale comments at ForkedChildTest:182/:275 + ChatTest:846, the vocabulary-guard docblock exemplar, and the header causal-claim renote route to round-68 **ee**) · di `a94628c5b` (E272 already-landed, E331 no-op-pin, E356 pinned, E481 consts-polities shipped, E565 already-landed, E610 real hole fixed) · dl `66b92e3d6` (**E686 stays PARTIAL**, tranche-4 stamped — DocFigure arms 18→23; tranche-5 → r68 **ed**). **Errata healed at this close:** dd's FIVE triage row stamps (E127/E143/E144/E148/E149) — those closures were REAL at `4d58dadd7` (backlog headings CLOSED, worklog ledger, tree re-verified) but `6682c9426` flipped the ledger arithmetic without stamping the rows; stamped now with the erratum inline. E417 stale-prescription renote SHIPPED inline at the entry (dc's deferral closed). E655 phantom re-issue note on the row. E671 K=8 addendum (`fb5078e6d`, lane ks `4810321e4`) on the row. **Process:** TWO fabricated builder reports this round (di, dl first incarnations — reported commits that existed nowhere; both caught by tree verification before review, both re-cuts landed clean) → the FABRICATION-VERIFICATION rule is in force (RESUME §0-NOW-69 addendum). Actionable **61 → 49** by the chained header arithmetic (flips: dh 4, dj 2, di 6; dl 0; E319/E686 stay in-set; E154 counted once).

**Coverage: 681 of 681 entries.**

## Tally

| disposition | n | share |
|---|---:|---:|
| `OPEN` | 88 | 13% |
| `PARTIAL` | 41 | 6% |
| `STALE-CITATION` | 1 | 0% |
| `UNCERTAIN` | 1 | 0% |
| `SUPERSEDED` | 7 | 1% |
| `CLOSED` | 402 | 59% |
| `NO-FIX` | 143 | 21% |
| **total** | **683** | |

**Actionable: 131** (`OPEN` + `PARTIAL` + `STALE-CITATION` + `UNCERTAIN`). **Needs nothing: 550** (`CLOSED` + `NO-FIX` + `SUPERSEDED`).

**Live arithmetic (overrides the derivation-time tally above; header prose authoritative per rounds-64–67 precedent):** actionable **49** at `323605773` — 61 at `fb5078e6d` minus wave-2's 12 closures (E3/E319/E228/E214/E686 remain in-set; wave-1 refusals stand). **ROW-CENSUS RECONCILIATION (the +6 the r66 close owed):** open-family rows measure **55** at this cut (72 before the close − 5 dd row-stamp errata healed now − 12 wave-2 flips), i.e. ledger 49 + the disclosed 6. The reconciliation verdict, by evidence: the six are NOT stale rows — every open-family row cross-checked against both its backlog heading stamp and every worklog CLOSED claim came back genuinely live; the drift is **arithmetic provenance** — the round-64 close (`c611441b5`) declared "131 → 117" while its own enumerated flips (12 moves, E688 born-and-closed, E689–E692 filed +4) give **123 rows / 119 ledger-consistent**, and every later round chained the undercounted 117 (r65 111, r66 100, r67 61→49). **Next re-mint should derive actionable from the row census, not chained deltas.** Section tables below are NOT re-bucketed — closed rows carry their stamp IN PLACE (rounds 64–67 precedent).

**ROUND-68 CLOSE 2026-09-12 (atop code tip `318cd7be5`; this closeout commit): ACTIONABLE = 49 BY ROW CENSUS — OPEN 30 / PARTIAL 18 / STALE-CITATION 1 / UNCERTAIN 0.** The r67 recommendation is ADOPTED: from here actionable derives from the ROW CENSUS, and the +6 chained-drift reconciliation is **CLOSED**. Derivation: the r67 wave-2 cut measured 55 open-family rows; r68 moves six of them CLOSED-in-place — OPEN-section rows **E107/E165/E583**, PARTIAL-section rows **E319/E547**, UNCERTAIN-section row **E36** — 55 − 6 = **49**. (**E17**'s r68 stamp corrects an ALREADY-CLOSED-in-place row — a false-attribution erratum, no census move.) Census rule: within the OPEN/PARTIAL/STALE-CITATION/UNCERTAIN tables, a row counts in its SECTION's bucket unless its evidence cell leads with `**CLOSED` (E3 and E686 carry PARTIAL stamp prose but sit physically in the OPEN table and count there). The historical chained number (49 minus r68's six = 43) is **RETIRED as authoritative** — census wins; the fact that r67's chained 49 and r68's census 49 coincide is arithmetic history, not healing. r68 row stamps also land: E3/E25/E43/E686 PARTIAL renotes (caret half CLOSED eb; E25 p1 verified; E43 declined w/ measurement → fj; E686 tranche-5 → t6 carry), E12/E206 re-confirmed. Ledger 55 → **49**.

**ROUND-69 CLOSE 2026-09-12 (13 lanes across two waves — w1 fa/fb/fc/fd/fi/fj, w2 fe/ff/fg/fh/fk/fl/fn; every builder AND reviewer APPROVE 0C/0M; code tip `b600c9760`, floor **11,656 / 167,184 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial 9m27s, K=8 CONSERVATION PASS +0/+0, five-guard 74T/5150A, Config 805T/17129A/1S, durations 503 rows, runtime config.json md5 05480c74\u20262210 stable; NEVER pushed): ACTIONABLE = 14 BY ROW CENSUS — OPEN 10 / PARTIAL 4 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r68's 49 − **35 rows stamped CLOSED-in-place this close** — OPEN-table 20 (E3, E5, E16, E93 NO-FIX-with-measurement, E140, E194, E208, E242 DECLINED, E281, E369, E375 B-QUALIFIER, E376, E378, E391, E419, E424, E445, E469, E564, E609), PARTIAL-table 14 (E43 CLOSED-DECLINED-CONFIRMED, E54, E158, E176 CLOSED-MEASURED-STAMP, E214 w/ 9f0c5db62 ERRATUM, E228, E246 refuted-plus-replaced, E257, E291, E322, E347, E505, E541, E633), STALE-CITATION-table 1 (E9 — README Tools bullet rewritten with the twelfth-file clause; guard-required 'Eleven classes' literal KEPT). Census-note: a fresh strict-prefix recount at the pre-stamp tree measured **50**, not 49 — the surplus is E653, whose r66 stamp read '**FULL CLOSED\u2026' and did not literally LEAD with '**CLOSED'; healed to '**CLOSED (FULL)' in this close, restoring exact agreement (49 before r69's flips). **Ten rows renoted, still actionable:** E10 PARTIAL (record landed / rename blocked on the tool-schema owner), E172 PARTIAL (feeder verified / Bootstrap drain seam), E199 PARTIAL (PER-TURN ruling shipped by fn; Chat.php:8266 beginTurn() wiring left OPT-IN by design), E309 OPEN (tree-wide factory refused with evidence), E325 PARTIAL (both directions shipped; 12 inline-slice readers carry), E390 DEFERRED (dual-guard evidence; post-merge both blocker files unowned -> gb), E493 PARTIAL (record extended w/ E524 measurements; providers heartbeat seam), E611 design-carry renote (supervisor-harness scope), E616 PARTIAL (3 fixtures landed; '-' lookbehind reports all-three-nowhere, measured), E686 PARTIAL (tranche-6 arms Z/AA/AB, DocFigure total 32; tranche-7 ~50-file remainder). E583's CLOSED-in-place row gained the roster-cleared addendum (fi: DEFERRED_BARE_CITATIONS emptied same-commit). Orchestrator rulings recorded: E375 -> B-QUALIFIER (CLOSED), E199 -> PER-TURN (shipped; PARTIAL for the seam), E611 -> design-carry, fj's Chat.php ownership breach ACCEPTED (erratum lands in the worklog; filemap is another agent's regen). Surviving actionable rows (14): E10, E199, E204, E309, E325, E353, E493, E611, E616, E686, E25, E134, E172, E390. Rows E134/E204/E353 were fh-brief-listed but NOT in fh's landed scope (report covers E9/E564/E611/E633; E291 rode fg) — they remain OPEN, unowned, round-70 candidates. Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-69 precedent).

**ROUND-70 CLOSE 2026-09-13 (lanes ga/gb/gd/ge wave-1 + gc wave-2; merge chain `379fc8554`(launch docs) → `f0d0d2ea1`(RESUME trim) → picks `109293485`(ga) `a852eb2ba`(gb) `80806b39f`(gd) → w1 re-pin `a9e7b8f5f` → pick `d7be5733f`(gc) → w2 re-pin `4da36922a` = tip; all five builders APPROVE (ge verdict-lane reviewed; gc/ga/gb/gd builders+reviewers 0C/0M); floor **11,674 / 167,892 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial 9m27s, K=8 CONSERVATION PASS +0/+0, five-guard 45T/4532A, durations 504 rows (+ProviderHeartbeatProgressTest), runtime config.json md5 05480c74…2210 stable; NEVER pushed): ACTIONABLE = 8 BY ROW CENSUS — OPEN 5 / PARTIAL 3 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r69's 14 − **6 rows stamped CLOSED-in-place this close** — OPEN-table 5 (**E10** CLOSED-DECIDED keep-lowercase zero-code (gc), **E199** CLOSED (ga wiring seam landed), **E309** CLOSED-VERDICT open-by-design w/ named trigger (ge, zero code), **E353** CLOSED-BY-FOLD (gd arms AC–AH), **E616** CLOSED-DECIDED DENIAL_SHAPE bytes unchanged (gb)) + PARTIAL-table 1 (**E172** CLOSED (gc drain half)). **Three rows renoted, still actionable:** E390 DEFERRED→PARTIAL (gb shipped the law-pin + tripwire arm; licensé drop blocked on the byte-identical BootstrapSkillSkipsTest/RequirementDirectiveProvenanceTest pair → r71 gf), E493 providers-half SHIPPED (gc; EngineBackend/Runtime consumer-threading seam → r71 gh), E686 tranche-7 stamped (gd, DocFigure 38 arms; ~9.5 HELD carry → r71 gg). **E375** gained the gb DECIDED-stamp confirmation addendum (row already CLOSED r69 — no census move). Unmoved carry rows: E204, E325, E611 (OPEN table) · E25, E134 (PARTIAL table). Surviving actionable rows (8): **OPEN-table — E204, E325, E493, E611, E686 · PARTIAL-table — E25, E134, E390.** Process: the RESUME trim (`f0d0d2ea1`, 518,315→86,915 B, −83.2%, 46/46 PROPOSALS items + 5 amendments, independent audit 7/7 APPROVE) landed pre-lanes; the **gb phantom-replay saga** minted **ADDENDUM -72** — the replaying builder had in fact LANDED `fdeddc1f3`; the fresh re-cut's salvage-first step-0 verified diff-vs-brief + re-ran all three gates green, zero new work (rule-4 re-cut law + salvage-first). Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-70 precedent).

**SUPERVISOR DISPOSITIONS 2026-09-13 (docs-only, mid-cycle after the r70 close): E204 CLOSED-BY-PRACTICE + E134 CLOSED-BY-PRACTICE.** E204's enforcement half verified live in the trimmed RESUME (APPENDIX I rule 10 evidence-dir mandate + rule 9 absolute paths, both carried through `f0d0d2ea1`). E134's law was **partially trim-dropped** — the digest item "never git-mutate in a non-commit step" sat truncated mid-sentence and the dirty-tree prohibition was gone; restored VERBATIM from the trim anchor (`/home/sites/crush-r61-artifacts/resume-trim/BEFORE.md:3820`) into APPENDIX V, r64 lane-ab incident recorded as the law's third instance. Recount under the in-place-stamp rule (rows whose evidence cell LEADS with `**CLOSED` do not count): OPEN-table 5→4 · PARTIAL-table 3→2 · **actionable 8 → 6** — OPEN-table E325, E493, E611, E686 · PARTIAL-table E25, E390. Tables NOT physically re-bucketed (closed rows stay IN PLACE).

**ROUND-71 CLOSE 2026-09-13 (lanes gf/gi/gg/gh merged UNREVIEWED by a prior session that died mid-closeout; successor ran review-after-merge — all APPROVE (r71-rv-*), one MAJOR healed by gg2; chain `6d87cedda` → `913d6f5b5` → picks `0c6820f39`(gf) `571dcf85d`(gi) `7924075c7`(gg) `9d05c5e9f`(gh) → hold-fix `52121fcac` (RATIFIED, rule 33) → review-fix `00bab5d21` → re-pin `328b14d91` = code tip; floor **11,687 / 168,165 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial 9m27s, K=8 CONSERVATION PASS +0/+0, five-guard 45T/4544A, Config 820T/17,890A/1S green incl. drift pair, durations 504→505 rows (+EngineBackendHeartbeatThreadingTest), runtime config.json md5 05480c74…2210 stable; NEVER pushed): ACTIONABLE = 4 BY ROW CENSUS — OPEN 3 / PARTIAL 1 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: the supervisor-dispositions cut measured 6 (OPEN-table E325/E493/E611/E686 · PARTIAL-table E25/E390); r71 stamps THREE rows CLOSED-in-place — OPEN-table **E325** (gi `571dcf85d`: 12 seam-4 readers + HelpTest folded onto the canonical trait, residue minted E693) + **E493** (gh `9d05c5e9f` atop gc `d7be5733f`: heartbeat threaded child-writer→EngineBackend→Runtime→CompleteRequest, kinds-exact both polarities, E646 clean) + PARTIAL-table **E390** (gf `0c6820f39`: RunsWallClockBoundedChildTrait owns both const pairs + sole KILL wrapper, licensés 23→21 deviation documented, hold-fix welds pass-through) — and MINTS **E693 [LOW]** OPEN (SuiteSkipRosterTest double getMethod → r72 hb), so 6 − 3 + 1 = **4**: OPEN-table E611 (design-carry, pick-or-drop) · E686 (tranche-8 landed `7924075c7` + review-fix `00bab5d21`, DocFigure 47 arms; PARTIAL prose in the OPEN table counts there per the r68 rule) · E693 · PARTIAL-table E25 (piece-2 design). E686 note renoted (next = tranche-9 per-arm tables). Process: **ADDENDUM -73 minted** — MERGE-AFTER-THE-FACT: on taking over a dead session, audit master for unreviewed picks BEFORE any new work; tree state, not session reports, is authority. Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-71 precedent).

**ROUND-72 CLOSE 2026-09-13 (lanes ha–hb, BOTH reviewed BEFORE merge — r72-rv-{ha,hb} APPROVE; chain `0683b5f8a` → picks `868221189`(ha `4ff314590`) `d75ce3af7`(ha vacuity fix `f4aaf5eb2`) `179f0b37e`(hb `2dcc802f1`) → re-pin `3deab0b6f` = code tip; floor **11,696 / 168,413 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, first serial EXIT=1 exactly the staleness pair → hand-bump → green second serial identical totals, K=8 CONSERVATION PASS +0/+0 (616.7s), five-guard 98T/6174A (DocFigure 56T = 47+9 exact), Config 829T/18134A/1S, durations 505 rows HELD (no new test files), runtime config.json md5 05480c74…2210 stable; NEVER pushed): ACTIONABLE = 3 BY ROW CENSUS — OPEN 2 / PARTIAL 1 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r71's 4 − **1 row stamped CLOSED-in-place this close** — OPEN-table **E693** (hb `179f0b37e`: getMethod double-index folded onto SlicesDeclaredMethodsTrait, fold bug absent by kept `$gate->getFileName()`, finisher salvage of a mid-commit builder death) — and **E686 renoted, stays actionable** (ha tranche-9 landed arms AS–BA +9T/+244A, DocFigure 47→56; phantom-page brief healed by measurement — PROVIDERS/ANTHROPICS/TOOLS.md do not exist; next = tranche-10 remaining pages per ha/measures.md). Surviving actionable rows (3): **OPEN-table — E611 (design-carry, pick-or-drop) · E686 (campaign, hc) · PARTIAL-table — E25 (piece-2 design).** E309's open-by-design trigger watch (ge r70 verdict) stays a watch, not a row. Round-73 seeds: hc E686 tranche-10 (AU-duplication precedent: check for an existing pin before adding an arm) · hd ledger-reconcile verdict lane (re-verify r70–r72 stamps at tip, docs-only). Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-72 precedent).

**ROUND-73 CLOSE 2026-09-13 (single code lane hc + zero-commit verdict lane hd; hc reviewed BEFORE merge (r73-rv-hc APPROVE 0C/0M/2MINOR); chain `739c547a3` (r72 filemap) → picks `41834f960`(hc `994155c3f`) `c616d5738`(hc review-fix `79fba8c10`) → re-pin `f46c203d6` = code tip; floor **11,703 / 168,611 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, first serial EXIT=1 exactly the staleness pair → hand-bump → green second serial identical totals, K=8 CONSERVATION PASS +0/+0, five-guard 105T/6372A (DocFigure 63T = 56+7 exact), Config 836T/18332A/1S, durations 505 rows HELD (no new test files), runtime config.json md5 05480c74…2210 stable; NEVER pushed): ACTIONABLE = 3 BY ROW CENSUS — OPEN 2 / PARTIAL 1 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r72's 3 − **0 rows stamped CLOSED-in-place** ± **0 mints** = **3** — **E686 renoted, stays actionable** (hc tranche-10 landed arms BB–BH on docs/MEMORY.md — the LAST un-armed docs page — +7T/+198A, DocFigure 56→63, two FALSE sentences healed in-step; the campaign's docs seed list is now DECLARED EMPTY but closure waits for an independent completeness verdict at r74 (lane ie) — no blind close), E611 unmoved (pick-or-drop AT r74 close), E25 unmoved (piece-2 → lane if). hd's verdict pass CONFIRMED all nine r70–r72 closure stamps at tip with ZERO FALSE/STALE — no other row moved. Surviving actionable rows (3): **OPEN-table — E611 (design-carry, pick-or-drop) · E686 (campaign, ie verdict) · PARTIAL-table — E25 (piece-2 design, if).** E309's open-by-design trigger watch (ge r70 verdict) stays a watch, not a row. Round-74 seeds: ie E686 completeness verdict (measure-and-verdict, docs only) · if E25 piece-2 project-scope memory-block writer (DESIGN-FIRST, owns src/Context/ + Runtime seams + tests). Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-73 precedent).

**ROUND-74 CLOSE 2026-09-13 (one code lane if + zero-commit verdict lane ie; if reviewed BEFORE merge — r74-rv-if APPROVE 0C/0M/3MINOR; chain `f46c203d6` → pick `72079870d` (if `da38ed361`) → companion `96c01bbab` (containment inventories — the census the builder+reviewer missed; rosters 38→39 routed sites in 16→17 files, dot-paths 38→39 / repository-chosen 18→19) → re-pin `ab7f3726a` = code tip; floor **11,714 / 168,729 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial3 green, K=8 shardcheck EXIT=0, five-guard 116T/6492A, Config 836T/18348A/1S, Glob window 254T/1890A, durations 505→**506** rows (+ProjectMemoryWriterTest), runtime config.json md5 05480c74…2210 stable; NEVER pushed; the merge agent DIED after landing steps 1–4 — finisher's -72 SALVAGE-FIRST audit found all four clean, redid NOTHING, docs-only closeout): ACTIONABLE = 2 BY ROW CENSUS — OPEN 2 / PARTIAL 0 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r73's 3 − **1 row stamped CLOSED-in-place this close** — PARTIAL-table **E25** (piece-2 PROJECT-scope writer `ProjectMemoryWriter` shipped; seams held for jb's E694-mint-or-fold decision, no blind mint) — and **0 mints**; survivors (2), both physically in the OPEN table: **E611** (design-carry — r74's ie verdict lane never touched it; pick-or-drop SLIPS to the r75 close, erratum noted at the backlog row) · **E686** (TRANCHE-11 minted by ie's completeness verdict, CONFIRMED 8/9 by r74-rv-ie with erratum E1 — `prompt_expand.md` IS tracked at the monorepo root, §9.12 cite valid-but-unguarded: 3 pages fully un-armed — AGENTS_AUTHORING/COMMANDS/PROMPT_ENGINEERING — ARCHITECTURE 5 drifted bare anchors + unguarded Providers, 3 FALSE claim-sites at tip, gd row-22 blame-erratum; execution = r75 ja, whose success carries the CLOSE verdict). Census executed by awk survivor recount over the four section tables (evidence-cell `**CLOSED`-leads rule), 3→2, never chained. Surviving actionable rows (2): **OPEN-table — E611 · E686.** Round-75 seeds: ja E686 tranche-11 execution · jb memory-ops design lane. Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-74 precedent).

**ROUND-75 CLOSE 2026-09-13 (three code picks ja/ja2/jb + merge-side companion; ja reviewed BEFORE merge — r75-rv-ja REQUEST-CHANGES 1 MAJOR → fix-lane ja2, reviewer's written close condition verified SATISFIED against the tree (first end-to-end conditional-close); jb reviewed BEFORE merge — r75-rv-jb APPROVE 0C/0M; chain `fff97295d` (r74 filemap) → picks `99cc68e02`(ja `51e5b9b39`) `332e6fdd7`(ja2 `ddf761169`) `90b7d1ae7`(jb `c79d69cfd`) → companion `02c65e6eb` (Bootstrap unfilteredTools docblock ELEVEN→TWELVE, census trio green) → re-pin `f8eba0f34` = code tip; floor **11,727 / 169,276 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial1 (tmux-PTY harness) EXIT=1 with the staleness pair + 4 FULLY-EXPLAINED tty artifacts (stdin-pin + size-keystone — harness, not code; cure = detached serial with `< /dev/null` redirect INSIDE tmux), serial2 plain-pipe EXIT=1 exactly the staleness pair → hand-bump → green third serial, K=8 CONSERVATION PASS, five-guard green, Config window green, Glob pair 14T/360A, DocFigure alone 71T/2451A, durations 506 rows HELD (zero new test files), runtime config.json md5 05480c74…2210 stable; NEVER pushed): ACTIONABLE = 2 BY ROW CENSUS — OPEN 2 / PARTIAL 0 / STALE-CITATION 0 / UNCERTAIN 0.** Derivation: r74's 2 − **1 row stamped CLOSED-in-place this close** — OPEN-table **E686** (CAMPAIGN CLOSED: tranche-11 executed by ja, arms AI–AO +8T, DocFigure 63→71, ARCHITECTURE anchor heals + all-13-pages bare-anchor absence guard + AGENTS_AUTHORING twelve-truth; rv-ja MAJOR (builder-missed sibling-page eleven/twelve contradiction) healed by ja2 with the AK1 cross-page AGREEMENT leg + AI planted-mismatch leg; ja2's erratum: ARCHITECTURE:31 digit row EXISTS, pinned by arm H) — and **1 MINT** — **E694 [OPEN]** from jb's design (project-scope list/search grouped display + bulk-clear posture; orchestrator ruling recorded: bulk-clear NOT supported for project scope, per-id ops only, refuse loudly, `--confirm --force` DECLINED; agent memory tool GATED on E25 re-severity review) — 2 − 1 + 1 = **2**: OPEN-table **E611** (verdict SLIPS again — kb is the r76 pick-or-drop lane) · **E694** (step-1 lands in ka). E25 gained a r75 renote (ops half landed via jb; remainder IS E694). Round-76 seeds: ka E694 slice-A design→ship · kb E611 verdict lane. Census rule stands: recount the four section tables, never chain; closed rows stay IN PLACE (rounds 64-75 precedent).

**ROUND-81 CLOSE 2026-09-14 (PHASE-3 wave, seven lanes pa–pg in three waves; ALL reviewed BEFORE merge — pa APPROVE 0C/0M/1MINOR, pb APPROVE-WITH-FIX 0C/1MAJOR+1MINOR healed `1a7d67823`, pc APPROVE 0C/0M/0MINOR, pd APPROVE 0C/0M/2MINOR, pe APPROVE-WITH-FIX minor-only (fix `f42ef1794`), pf APPROVE 0C/0M/3MINOR, pg APPROVE 0C/0M/2MINOR. Chain `8cd2c5f4a` (MINTED) → W1 picks `2452947ba`+`4b767f85c`+`56a71d134`+`0bf64d08e` → re-pin `a39b34ee2` (11,899/171,108) → W2 picks `fea4b3b26`+`772ef1b03`+`2a33d175b` → re-pin `0908d39aa` (11,911/171,706) → W3 picks `ab21cfd48`(pf `244c6b2a8`)+`0c347e995`(pg `f73112e7b`) — ALL NINE picks ZERO-conflict → companion `8dd48df9d` (rv MINORs) → drift-fix `086035f50` (serial1 caught THREE whole-tree guards past the lane gates: pg stacked-docblock burial + pg unaccounted tempDir walk both arms + pf one-sided HOME — each reproduced at its own lane tip, all healed by the guards' own prescriptions; FOURTH co-shard-class demonstration: serial is the only complete gate) → weld `a5d0033d9` floor **11,991 / 172,259 / 0F / 0E / 1 skipped (McpClientTest canary) / EXIT 0** linked @cwd=sugar-crush: serial2 green in one pass 10m1s, K=8 CONSERVATION +0/+0 EXACT sums, five-guard 125T/7333A (DocFigure alone 83T), Config 879T/19588A/1S, citation 13T/6057A, SwallowingCatch 9T/4173A, targeted family 77T green, Glob corpus 131,765=365×361 intact, durations **522→529** (set-diff exactly the 7 new files — pf five, pg two), config.json md5 05480c743aff302fd6c06c5a4a4c2210 start==end; NEVER pushed): **ACTIONABLE = 0 BY ROW CENSUS — PHASE-3 COMPLETE.** Derivation: r80 close minted 7 (E704–E710) − **7 rows stamped CLOSED-in-place this close** − 0 relabels + 0 mints. Re-verified live after the flips: the §2(a) awk returns EMPTY. Rows stay put (rounds 64–80 doctrine). Harness law corrected LOUD this round: serials run through the plain bash-tool pipe, NEVER tmux/PTY (bootstrap's fd-0 repair is stdin-isatty-guarded; PTY shape reddens exactly 3 stdin-pin tests — w2 first attempt proved it). SwallowingCatch gate-law HELD CLEAN r78–r81. Carry-forward trigger-watches (WATCHES, not rows): **NEW — pa paste-while-modal behavioral pin** (rv 1MINOR declined in-lane); **NEW — ReadPathCensus `\`-prefix blind spot** (pg-disclosed: the census' quote-prefix walk does not see backslash-prefixed calls, `\scandir(...)` style — measured today: no live offender); **NEW — pf maxOutputTokens threading seam** (TaskTool/Workflow keep provider defaults; ClaudeCode transport reads no request ceiling); existing unchanged: E696-deny-residual, `updateRegistration()` redirect-churn, E309, E611 tripwire, E694 step-3/E25 re-severity, E655 VOID, LspClientDispatchPumpTest ambient, Chat.php:8266 beginTurn seam.

**ROUND-80 CLOSE 2026-09-14 (two lanes ob+oc, PHASE-2 FINAL wave; BOTH reviewed BEFORE merge — r80-rv-ob APPROVE 0C/0M, r80-rv-oc APPROVE-WITH-FIX 0C/1MAJOR+4MINOR all healed in fix-round `8b7989af1`. Chain `492314c17` → pick `896c18205` (ob) → picks `ad8dfda12`+`e438c9dca`+`f0ef430fc`+`b57ed8fec`+`4b9ab1258` (oc, ALL CLEAN) → drift-fix `b11a34ca3` (stacked docblock the fix-round buried caught by RuntimeNoticeSinkDeliveryTest in serial1 — third tree-scan co-shard class catch; guard's prescription applied, zero behavior change) → weld `710544154` floor **11,870 / 170,786 / 0F / 0E / 1 skipped (McpClientTest canary) / EXIT 0** linked @cwd=sugar-crush: serial2 green in ONE pass (+33T exact vs r79 floor; hand-bump tests-only cadence), K=8 CONSERVATION +0/+0, five-guard 123T/7176A, DocFigure alone 81T/2604A, Config window 860T/19271A/1S, citation+roster+swallow+census4 179T/14152A ZERO census flips, check-child-lifetimes rc0, Glob pair green (corpus 131,765), durations **515→518** (+3 oc test files, set-diff exact), config.json md5 stable start==end; NEVER pushed): **ACTIONABLE = 0 BY ROW CENSUS — PHASE-2 COMPLETE.** Derivation: r79's 1 − **1 row stamped CLOSED-in-place this close** (OPEN **E699** → operator ruled "wire it", oc shipped the double-opt-in gate; backlog heading flipped with its CLOSED paragraph) − 0 relabels + 0 mints. ob closed the r79 trigger-watch tempnam leak as test hygiene (+0T). Rows stay put (rounds 64-79 doctrine). SwallowingCatch gate-law held CLEAN (r78–r80). Phase-3 mint pending: three operator-reported UX findings under investigation — lanes to be minted round-81.

**ROUND-79 CLOSE 2026-09-14 (single code lane na; reviewed BEFORE merge — r79-rv-na APPROVE-WITH-FIX 0C/0M/3MINOR, all healed at lane tip `44adf5fe0`. Design-first held: nd produced the PKCE design; its §4 self-found DEFECT (buffer-window arm must branch on `refreshToken !== ''` or a near-expiry token POSTs `refresh_token=''`) was fixed in tranche-1. Chain `92a7e0136` (r78 closeout) → picks `8ed8ada1d`+`7b83a6454`+`1b8cc7ce8` (na, linear, ALL CLEAN — zero conflicts) → weld `ca9aac147` floor **11,837 / 170,424 / 0F / 0E / 1 skipped (McpClientTest canary) / EXIT 0** linked @cwd=sugar-crush: serial green in ONE pass (+53T exact vs r78 floor; hand-bump tests-only cadence per r78 fact), K=8 CONSERVATION PASS +0/+0, five-guard 122T/7130A, DocFigure alone **80T** (77+3 = BI/BJ/BK exact), Config window 859T/19201A/1S, citation+roster+swallow combined 152T, Glob pair green (corpus 131,765), durations **508→515** (+7 na test files, set-diff exact), runtime config.json md5 05480c743aff302fd6c06c5a4a4c2210 stable start==end; NEVER pushed): **ACTIONABLE = 0 lanes + 1 OPERATOR DECISION BY ROW CENSUS** (sole survivor OPEN **E699** — decision gate, NOT a lane: keep-as-is recommended ON RECORD, delete is STOP-class). Derivation: r78's 2 − **1 row stamped CLOSED-in-place this close** — OPEN-table **E701** (na: interactive OAuth authorization-code + PKCE; §10 acceptance met in full, backlog CLOSED paragraph cites it verbatim) − **0 relabels** + **0 mints** (two NEW trigger-watch entries recorded as WATCHES, not rows: `updateRegistration()` redirect-churn seam per nd §2.2; /tmp/crush-mcp-auth-* tempnam hygiene leak — 1,878 files from CommandTableRenderingTest, found closeout-side). Rows stay put (rounds 64-78 doctrine). SwallowingCatch gate-law held CLEAN this wave. Builder-death salvage re-proved -72: tranche-2 had been committed from the uncommitted working tree; SALVAGE-FIRST audit before any redo. Phase-2 queue is AT THE DECISION GATE: E699 ruling, fresh audit sweep, or bank.

**ROUND-78 CLOSE 2026-09-14 (two waves; design-first — mc re-verified the r77 stamps, mz designed the E698 surface before mb built it. ma ALONE wave-1, mb wave-2; BOTH lanes reviewed: r78-rv-ma APPROVE 0C/0M/4MINOR-info, r78-rv-mb APPROVE-WITH-FIX 0C/0M/2MINOR both healed in fix `4d1672b75`. Chain `fcbf19d43` (r77 closeout) → pick `f64399c82`(ma `c4d08c169` E696-α) → wave-1 weld `41ad0bc93` floor **11,756/169,439** (+7T exact, K=8 +0/+0, durations 506 HELD) → picks `4854c86a3` `1f9e6fec8` `4ed2924a8` `15d57cc84` `4eca19378`(mb 5 commits, ALL CLEAN — sole wave, zero conflicts) → wave-2 weld `a594f073b` floor **11,784 / 169,799 / 0F / 0E / 1 skipped (McpClientTest canary) / EXIT 0** linked @cwd=sugar-crush: serial1 green in ONE pass (+28T exact vs wave-1 floor; assertions -31 vs builder serial = tree-scan wobble, green-serial weld truth), K=8 CONSERVATION PASS tests=+0 assertions=+0 (fresh /tmp/r78-shardcheck, wall 83s), five-guard 119T, DocFigure alone **77T**, Config window 856T/19122A/1S, citation+roster+swallow combined 216T, Glob pair green (corpus 131,765), durations **506→508** (+McpClientStartedSnapshotTest +BootstrapMcpLivenessTest), runtime config.json md5 05480c743aff302fd6c06c5a4a4c2210 stable start==end; NEVER pushed): **ACTIONABLE = 2 BY ROW CENSUS** (OPEN **E699** — operator disposition pending, keep-as-is ON RECORD — + **E701** — PKCE next OAuth slice). Derivation: r77's 6 − **4 flipped CLOSED-in-place this close** (E696 [PARTIAL]→[CLOSED] α half, deny-residual under CLOSED-notation per house rule; E698, E702, E703 →[CLOSED]) − **0 relabels** + **0 mints**. Rows stay put (rounds 64-77 doctrine). SwallowingCatch gate-law held CLEAN this wave (zero violations). 

**ROUND-76 CLOSE 2026-09-13 (two lanes ka/kb, BOTH reviewed BEFORE merge — r76-rv-ka APPROVE 0C/0M/2MINOR (entry-present clear-polarity KEEP ruled in item-2); r76-rv-kb APPROVE-WITH-FIX 0C/1MAJOR — the :6546/:6552 heal was ITSELF false (Task never reaches the project-glob filter), comment-only fix-lane `a56d9014c` landed the post-filter/manager-gated truth; chain `9b83cf8f4` (r75 filemap) → pick `35489b1d1`(ka `6b113f67e`, E694a store-aware list/search + loud project-clear refusal + memoryLocate tuple doc + MEMORY.md sentence & DocFigure arm AQ in-step, +7T) → picks `fefd95468`(kb `0b60af42e` ELEVEN-family docblock heals) `252dea07b`(kb-fix `a56d9014c`) `c27178e0c`(kb docs `53bf60534`, E611 DROP; triage E611-row conflict resolved keeping BOTH master's r75 SLIPS sentence and kb's flip) → companion `a001bd9ce` (README :224/:233 filterable-eleven/twelve-wired truth + `ReadmeSettingsTierClaimTest` quoted needle + census-domain docblocks + TrustKey locator comment moved IN-STEP — rv-kb MINOR-8 apply-safe pairing, one commit) → re-pin `ea61174b5` = code tip; floor **11,734 / 169,328 / 0F / 0E / 1 skipped (McpClientTest canary) / exit0** linked @cwd=sugar-crush, serial1 EXIT=1 exactly the staleness pair (11,734/169,328) → hand-bump json tests only (a README assertions bump BEFORE the weld trips the headline↔artifact arm — re-pin law) → serial2b EXIT=0, K=8 CONSERVATION PASS +0/+0, five-guard green (DocFigure 72T), Config window green, Glob pair green (corpus 131,765), durations **506 rows HELD** (zero new *Test.php), runtime config.json md5 05480c74…2210 stable; NEVER pushed): **ACTIONABLE = 0 BY ROW CENSUS — OPEN 0 / PARTIAL 0 / STALE-CITATION 0 / UNCERTAIN 0. THE QUEUE IS EMPTY.** Derivation: r75's 2 − **2 rows stamped CLOSED-in-place this close** — OPEN-table **E611** (kb DROP verdict: no in-repo mechanically-checkable half remains; the harness-tool + lane-registry residue is supervisor tooling per the r69 ruling; reopen tripwire = a round shipping out-of-ownership CODE edits un-reported) and **E694** (ka slice-A shipped discovery; bulk-clear shipped-by-absence as the loud refusal; agent memory tool declined into the E25-re-severity trigger watch) — and **0 mints**. Salvage audit clean (zero pre-landed work). Seam dispositions: rv-kb MINOR-9's E686-heading-normalize and "E694 no ledger row" both verified OBSOLETE/REFUTED at master (E686 heading CLOSED since `92b1fe9cc`; E694 had rows in both tables). Carried honestly to §0-NOW-78 §2: SETTINGS.md :359/:403 eleven-family NOT widened (brief scoped README), Bootstrap tool-count BODY docblocks unpinned by any guard (rv-kb item-10), ka sentinel docblock line unpoliced (rv-ka M5). Census evidence: awk survivor recount over the four section tables returned EMPTY (exact command + output pasted in §0-NOW-78 §2(a)). **PLAN-COMPLETENESS DECLARATION lands at §0-NOW-78 §2 — the next move is an orchestrator decision point, not an automatic round-77 roster.** Census rule stands: recount, never chain; closed rows stay IN PLACE (rounds 64-76 precedent).

🔴 **A `CLOSED` stamp does not mean the entry's Step is safe to implement.** Several entries were closed by a different mechanism than they prescribe, and at least one (E219) would be a regression if its Step were followed literally today. The note column says so where it applies.

---

## `OPEN` — 88

| id | conf | evidence | note |
|---|---|---|---|
| **E3** | HIGH | **CLOSED ROUND-69 (lane fj `80dff1595`→pick `c3605f9e6`)** — caret half shipped, re-verified at tip: `handlePaletteKey` gains Char-insert-at-caret / Backspace-erase-before / Delete-erase-after / Left-Right cluster motion / Home / End; `PaletteState::queryCursor` = 4th ctor param default 0 (verified :30); `withQuery` moves the caret with the text, `withQueryCursor` keeps the row, root resets clear it; end-caret byte-identity keeps eb's pin; append-only `dropLast` DELETED (verified — only `dropLastWord` + the historical `Chat.php:12420` 'is gone' comment remain); `PaletteQueryCaretTest` 14T; lane-wide +18T. | reverse-video caret alternative recorded NOT-taken (product decision); Chat.php ownership breach ACCEPTED at this close — handlePaletteKey lives ONLY in Chat.php, no live collision (erratum in the worklog; filemap is another agent's regen) |
| **E4** | HIGH | **CLOSED ROUND-67 wave-1 (lane de → master `2da286745`, lane `bc3e11e1a`)** — checkpoint saves `'inputCursor' => inputCursorOffset()`; `/rewind` applies `withInputCursor` when is_int; legacy keyless = caret-end preserved. Prior evidence: checkpoint save writes only 'inputBuf' (src/Chat.php:6943); restore reads only 'inputBuf' (src/Chat.php:10936) and reseeds cursor at end | inputCursorOffset() exists (:11615) and IS now persisted |
| **E5** | HIGH | **CLOSED ROUND-69 (fj, same SHAs)** — snap half shipped: `Renderer::snapToClusterStart` (verified :4173) floors mid-cluster carets to the cluster start, wired into BOTH renderInput and renderPalette (verified call sites :3634/:4244); cluster boundaries short-circuit byte-identically (eb's PaletteCaretHonoursHandoffTest pins survive); `InputCaretGraphemeTest` 3T. | forms-domain residual noted: the TextArea draft widget still walks codepoints (widget seam); reverse-video alternative NOT taken (product) |
| **E10** | HIGH | **CLOSED ROUND-70 (lane gc `9de0e3fd6`→pick `d7be5733f`) — DECIDED keep-lowercase, ZERO code** — the row's own evidence (model trained on `doctor`; tracker #78 defers to the tool-schema owner) favors status quo; fc's both-site rationale pins (`src/Tools/BuiltIn/Doctor.php:45` + `tests/Tools/BuiltInToolTest.php:194`, verified at tip) stand AS the resolution; gc — holding the round's providers/tool-file ownership — confirmed NO reversal, matching the row recommendation. | inconsistency stays recorded, deliberately |
| **E16** | MED | **CLOSED ROUND-69 (lane fa `8db951a0b`→pick `a9e7a333c`)** — decision + pin, both re-verified at tip: pre-gate `ToolStarted` carrying the model's RAW arguments is documented design at BOTH emit sites (`src/Runtime.php` :1674 sequential + :1784 concurrent) and BEHAVIOUR-PINNED by `testToolStartedCarriesTheModelArgumentsWhenAHookRewritesThem` (frame=original, executed=rewritten vacuity guard; ToolFinished pairs by id and carries no arguments property); the model's rewrite still reaches PostToolUse via `gate()`'s withRewrittenArgs; DTO half documented since `38e70d99c`. +1T/+8A; 2 mutation proofs. | parallel-path EVENT pin (ParallelToolCallsTest) was fa's seam — the hook half is already pinned there |
| **E17** | HIGH | **CLOSED 2026-09-12 r68/ec `4a353a77c`→pick `40107b8d5`** — 11 provider sites (backlog's 7 undercounted: Bedrock×2, OpenAI, Custom, Sglang×2, Vertex×5 incl. per-side Anthropic start/delta `Usage` carriers — never whole-doc on stream arms) + `Runtime::foldUsage()` byte-identical projection fallback; ClaudeCode/Echo keep-null with honest comments; +17T (ProviderUsageCarriersTest, RuntimeUsageFoldTest); 7 mutation proofs. Chat-side half landed ROUND-67 wave-1 (lane de → masters `2da286745`+`2973380d4`, lanes `bc3e11e1a`+`e4d448ead`): `CompleteResponse` gains `?Usage`+`promptTokens()`; Chat calibrates estimate vs billed tokens, factor f←clamp(real/raw,1.0,3.0), one-shot, fail-open — but that row's tail claim "7 provider sites + Runtime:1362/:1495 fold rode the same lane" was FALSE at base (`e4d448ead` = Chat only, zero usage: in providers); erratum lands with ec. Prior evidence: src/Providers/CompleteResponse.php:13 still had one flat $tokensUsed; countTokens() ceil(len/4)+10 fed the tier | prompt/completion split now flows; providers carry the parsed Usage document and the fold prefers it over the projection |
| **E20** | HIGH | **CLOSED ROUND-67 wave-1 (lane de → master `2da286745`)** — mid-turn abort via `Events\\SpendCapBreached` frame event; `EngineBackend` `withSpendCap`+`sessionSpendAtStartUsd` (13 clone sites), check past the `toolResults !== []` continue point, Chat folds at both pumps under generation guards; NO wall-clock kill (E646 honored). Follow-through `9f0c5db62`: `MemoryPromptWiringTest::$steps` exercises `withSpendCap`. Prior evidence: spendCapRefusal() said the crossing turn "ran to completion"; no cap check in EngineBackend | the two amendments' items were genuinely closed; the mid-turn abort is now closed too |
| **E78c** | MED | **CLOSED-WITH-EVIDENCE ROUND-67 wave-2 (dh → `b08a96cf1`) — NO-OP verdict measured before touching**: both named NonInteractive sites carry their sugarcrush: literal + the round-62 DECISION STAMP (0373308a3); :248's bare getMessage() is exempt per E78's own run() carve; ce's NoticesCommand added a launchNotices() reader but that is the interactive path — the one-shot seam-cannot-reach decision stands. Prior row: src/Cli/NonInteractive.php:809 (piped-stdin truncation) and :397 (offline default) are still raw fwrite(STDERR); launchNotices() readers are still only Bootstrap.php:1214/2140/2182 | Deferred one-shot-path decision; no later backlog entry resolves it |
| **E93** | MED | **CLOSED (NO-FIX, measured) ROUND-69 (lane fc `183474831`→pick `71cc12f38`)** — the entry's Step is conditional on a SkillRegistry logging seam that does not exist at tip, and triage itself offered the NO-FIX stamp. Fresh measurement at tip (PHP 8.3.6): the recorded pathological trio (`a/**/**/**z`, `**/**/**/nomatch-z`, `x**y**z**w` vs a 60-segment non-matching path) runs 2000 iterations in 0.0017/0.0004/0.0004 s with `preg_last_error()==PREG_NO_ERROR` — anchored, never backtracks; the forced-fallback comparison case is bounded too. A misroute answers CORRECTLY, just via the slower predicate. `grep preg_last_error` in SkillRegistry.php + PathGlob.php still nil (verified). | freshness-stamped at fc's measurement; revisit only if the compiled matcher gains a logging seam |
| **E107** | LOW | **CLOSED 2026-09-12 r68 closeout re-verification** — 8/8 claim families RED under perturbation at `318cd7be5` (MD@ReadmeRosterDriftTest:303, ME@:328, MF@:399 needs BOTH enumerations removed, MH+MI@:619, MJ@:110, MK/ML src-side; baseline 9T/61A green; evidence /home/sites/crush-r61-artifacts/closeout-r68/e107.md). ec's OPEN was trigger-missed, not blind guard. Prior: grep for 'E107' and the mutation ids across src/tests/backlog returns only the entry itself; no record of a re-run anywhere | process item, unverifiable from the tree; its own trigger has fired since ReadmeRosterDriftTest changed under E104 — now re-run with per-mutation plant/revert evidence |
| **E127** | HIGH | **CLOSED ROUND-67 wave-1 (dd → `4d58dadd7`, lane `1e4ff44c5`)** — methodSignature innermost-enclosing-class/trait/enum resolution + whole-file sweep as fall-through; shadow pin shipped. [ROW-STAMP ERRATUM: the closure was real at the lane (backlog heading CLOSED, worklog ledger, tree confirms) but 6682c9426 missed this row's in-place stamp — stamped at the round-67 close.] Prior row: tests/Config/Support/EnvReadScanner.php:731-739 methodSignature() scans whole token stream for `function <name>`, first match wins |  |
| **E140** | HIGH | **CLOSED ROUND-69 (fa, same SHAs)** — `src/Support/ToolIpcFiles.php` class doc-block rewritten: the collect side no longer claims 'the ONLY unlink either of them has' (Runtime discards on the round-45 abandon/degrade paths and at collect); the `sweep()`-as-only-ORPHAN-reaper conclusion STANDS with corrected wording; residual-claim grep in src/tests returns zero at tip (verified 'ONLY unlink' count 0). |  |
| **E143** | HIGH | **CLOSED ROUND-67 wave-1 (dd → `4d58dadd7`)** — stalePattern/retracts memoized (Config window 31,014A→16,830A — honest per-paragraph noise cut, re-pinned). [ROW-STAMP ERRATUM — same as E127.] Prior row: tests/Config/GlobFigureDriftTest.php:109 assertArrayHasKey inside word(), reached from stalePattern():434 via carriesTheStaleFigure():412 — still one assertion per paragraph, no cache |  |
| **E144** | HIGH | **CLOSED ROUND-67 wave-1 (dd → `4d58dadd7`)** — Chat splitter -> DocumentParagraphs::of, delegation proven by breaking the canonical (oracle reds). [ROW-STAMP ERRATUM — same as E127.] Prior row: tests/Chat/ChatConfigChangeDoorsDocumentationDriftTest.php:386 still declares `private static function paragraphs()` instead of calling DocumentParagraphs::of() | three other private copies also remain (GlobFigureDriftTest:171, ConfigWriteProducerDocumentationDriftTest:261, ThemePersistenceFramingTest:71) |
| **E148** | HIGH | **CLOSED ROUND-67 wave-1 (dd → `4d58dadd7`)** — recursive docs/ walk + flat-tier floor; SKILL.md DECLINED with measurement; temp-tree depth pin folded. [ROW-STAMP ERRATUM — same as E127.] Prior row: tests/Config/EnvRosterDriftTest.php:794 still `glob($root . /docs/*.md)`, non-recursive, README.md only |  |
| **E149** | HIGH | **CLOSED ROUND-67 wave-1 (dd → `4d58dadd7`)** — prefixedNamesIn via window + one-gap rejoin + head veto (NAME_PATTERN validator); censusFixture wrap pos+neg. [ROW-STAMP ERRATUM — same as E127.] Prior row: tests/Config/EnvRosterDriftTest.php:843 still preg_match_all over raw page text, no DocumentParagraphs normalisation |  |
| **E165** | MED | **CLOSED-KEEP 2026-09-12 r68/ec** — the "84" figure already carries dated erratum + `06126017` re-measurement at :494-510; verdict KEEP-AS-IS, tree named in stamp (supervisor disposition per §0-NOW-69 §2). Prior: tests/Config/ReadmeRosterDriftTest.php:495 still carries "OK (14 tests, 84 assertions)" and :497-503 still says the tree cannot be identified | Tree argues for KEEPING the figure with its tree named; the entry preferred dropping — disposition settles on KEEP (NO-FIX-style outcome, row closed in place per rounds-64–67 precedent) |
| **E175** | HIGH | **CLOSED ROUND-67 wave-1 (lane de → master `2da286745`)** — detach error_log gated behind `Chat::DEBUG_STREAM_ENV` (SUGARCRUSH_DEBUG_STREAM), report-only, DETACH ungated; StreamingWiringTest both polarities + ENVIRONMENT.md row + Help.php roster IN-STEP. This also closes E154's (c) site. Prior evidence: src/Chat.php:8100 error_log('Chat: onToken observer threw, detaching it for this turn: ' ...) still ungated | Chat.php was rewritten by prompt_plan but this exact site survived verbatim until r67-w1 |
| **E194** | HIGH | **CLOSED ROUND-69 (lane fd `5a9819fc7`→pick `3555a3940`)** — judged small config-hygiene and done fully: NEW `tests/Support/RuntimeNoticeSinkResetExtension.php` (PreparationStarted → `RuntimeNoticeSink::reset()`) + phpunit.xml `<extensions>` block (verified :75-77) + 3T pin file. GOTCHA shipped in the pin: PHPUnit 10.5's loader reads `extensions/bootstrap` (vendor Loader.php:198) — an `<extension>` child PARSES AND DROPS SILENTLY (throw-probe proved the mis-spelling loads nothing while the run stayed green); the pin asserts the loader-read spelling. New file rowed into durations at the re-pin. | seam: `src/Diagnostics/RuntimeNoticeSink.php` :778 reset() 'live callers' doc-block is now stale (the extension is a third caller; src was forbidden to fd) — ga/gb micro |
| **E195** | HIGH | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`, lane `687f68cce`)** — alias-aware channel 6: class-alias resolution added in scan(). Prior evidence: StderrEmitterCensusTest.php isRuntimeNoticeSinkCall() keys on the last name segment only; no class-alias resolver in scan() | the use-alias detection that exists is for `use const`/`use function` on channel "other", not for channel 6 |
| **E199** | HIGH | **CLOSED ROUND-70 (lane ga `77b4883b3`→pick `109293485`)** — the wiring seam landed: `Chat::scheduleBackendCompletion()` (:8266) calls `RuntimeNoticeSink::beginTurn()` at top, GATED on `$this->drainsRuntimeNotices` (class contract names the DRAIN OWNER as the only armer — an unappointed/host Chat arming would re-open the owner's budget mid-turn); 2 pins in RuntimeNoticeSinkDeliveryTest (vacuity-guarded budget pre-spend via reflection + unappointed negative arm; transport burst 25→pump→pump = saturated 20-head + ONE overflow row, hasPending false); class/beginTurn/OVERFLOW_FORMAT doc-blocks truth-flipped; +2T. Per-turn ruling + `TURN_NOTICE_LIMIT` shipped by fn r69. | mutations: `beginTurn();`→commented = RED exactly the 2 new tests; gate removed = RED exactly the negative arm (md5-restored) |
| **E204** | MED | **CLOSED-BY-PRACTICE 2026-09-13 (supervisor disposition, post-r70 close)** — the per-lane mandate is now a tree artifact: RESUME APPENDIX I **rule 10** "Evidence to `/home/sites/crush-r61-artifacts/<lane>/`" (verified verbatim, carried through the trim `f0d0d2ea1`; restated in §0-NOW-72) + **rule 9** "briefs demand absolute paths"; live per-lane dirs at stamp time (`gf/`, `gd/` under the artifacts root). Prior row: grep "scratchpad/lane-" hits only the backlog; docs/plans/crush_code_RESUME.md carries no per-lane scratchpad instruction | dupe E218 already SUPERSEDED — both sides now resolved (E204 closed-by-practice; the evidence-dir rule supersedes the `scratchpad/lane-<x>/` naming) |
| **E205** | HIGH | **CLOSED ROUND-67 wave-1 (lane dc → master `18daf2d55`, lane `bb83512bc`)** — text-regex predicate replaced by commandFragments() (T_CONSTANT decoded, T_ENCAPSED appended, \\x00 gaps block operators but stay word-fillers) + quote/order-aware shellFdTwo() over ARG0 only; the two pinned FPs flipped to CAPTURED with polarity twins; tree moved ZERO (FP shapes were fixture-only). Prior evidence: tests/Support/ChildStderrCaptureScanner.php:229 sendsFdTwoToTheNullDevice() unchanged - bare 2>/dev/null still matches anywhere, no nesting model | the measured-and-rejected "obvious fix" stayed rejected — the shipped shape is the token model |
| **E208** | HIGH | **CLOSED (fold) ROUND-69 (fd `3555a3940` + fk `c8b8f5437`)** — every in-tree foldable copy consolidated: ForkedChildExitScanner's private `matching()` deleted (verified 0 at tip), 3 sites → `TokenFunctionRanges::matching` now PUBLIC (verified :152) with brace dispatch folded to `opensBraceDepth()`/`closesBraceDepth()`; fk folded the two scanner copies (ChildStderrCaptureScanner + ChildLifetimeScanner — verified call sites; whole scanner suite green = behavioural proof; `matchingBacktick` stays local; opener-token census re-derived — ChildStderrCaptureScanner EXITS selection, measured). | carries NAMED, outside the fold: a 3rd private `matching()` lives in `tests/Support/ProcessUniqueTempNameTest` (verified at tip) → round-70 ga candidate; the 'confirm on 8.4 in CI' half is not judgeable in-tree |
| **E242** | HIGH | **CLOSED-DECLINED ROUND-69 (lane fg `0dd5a61fa`→pick `bce05c166`, no code)** — re-key measured and DECLINED at round-49 (the `SuiteTempSandboxContractTest` pins hold — file verified at tip); the uid-keyed sandbox root-cause was settled by E290/E298. No new fact since round-49's judgement. |  |
| **E258** | HIGH | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`)** — constructionSites() counts named static factories (ERROR_LOG_SITES counts CALLS vs MESSAGE_SHAPES literals; reviewer mutation-proved). Prior evidence: tests/Cli/StderrEmitterCensusTest.php:2678 constructionSites() still matches only T_NEW and ::new(); its doc-block lists new-class/new-$var holes but not the named factory; src/Providers/ProviderFactory.php:708 still calls SglangProvider::openAiCompatible() | option taken per the commit's census cluster; do NOT leave as-is satisfied |
| **E259** | HIGH | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`)** — swallow routed; census trio NINETEEN/19/WorktreeManager.php => 5 flipped IN-STEP (reconstructed after the builder's first mutation was lost). Prior evidence: src/Agents/WorktreeManager.php:1000-1001 catch (\\Throwable) { // Skip worktrees that fail to remove } unchanged; tests/Cli/StderrEmitterCensusTest.php:379 still says WorktreeManager.php => 4 | both coupled numbers moved with the fix |
| **E269** | HIGH | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`)** — funnel cardinality kept honest per the prescribed remedy inside the census cluster. Prior evidence: tests/Cli/StderrEmitterCensusTest.php:641 testTheTwoEmitterFunnelsDoNotCountTheSameWrite still named for the cardinality; no roster asserting the application has exactly two emitter-side funnels | the two remedies (rename, funnel roster) — see the commit |
| **E281** | HIGH | **CLOSED ROUND-69 (fe, same SHAs)** — TeamTest attribution shipped (verified :47/:58/:134 at tip): `processToken = uniqid((string) getmypid(), true)` before the baseline snapshot; an IDEMPOTENT `teamId()` routes every call site (createTeam/createTeammate + 8 direct constructions; raw and already-tokenised inputs both land once — the naive prefix double-transform reds the addTeammate ownership check); footprint = configDir entries minus structural 'teams', teams-level token-basename filter extracted as `isOwnTeamsEntry` with a both-polarities fixture table (own / throwing-* / bare / foreign-pid; the `basename('../..')` traversal trap pinned). +1T; M1/M1b mutations. | origin of the residue: `MultiAgentRefactorTest:423` 'throwing-'.uniqid ids are NOT pid-prefixed — round-70 ga |
| **E309** | HIGH | **CLOSED-VERDICT ROUND-70 (ge — measure-and-verdict, ZERO code; disposition OPEN-BY-DESIGN with a named trigger)** — the literal factory prescription stays un-shipped and expensive (~95 construction sites / 18+ files; engine-class split 72 sites / 15 files after ge's [r70 audit-fix] re-attribution of Chat.php's 3 bare `new ToolResult(` to the ROOT class), and its protective outcome is already largely enforced by the whole-`src/` exact-map guard fb's lineage completed — residual = zero-runtime-benefit typing ceremony today. TRIGGER: fold refusal-factory typing into the crush_feat §1D two-class `ToolResult` unification, or mint when a 4th `DenialKind` case lands via the E375 product-decision route. The r69 premise read was FALSE-TO-TREE: TWO ToolResult classes exist (engine `src/Tools/ToolResult.php` ctor-only; chat-side `src/ToolResult.php` already carries `ok()`/`error()`/`okWithImage()`/`fromEngineResult()`), the refusal producers are ALREADY enum-routed, and duplicate frame-spelling is machine-impossible since E246/E541/fb. DENIAL_TERMS stays hand-written exactly as the entry says. | verdict, not a fix — ge REPORT carries the full construction census + 3 audit-fix errata; backlog heading carries the CLOSED-VERDICT stamp paragraph |
| **E325** | HIGH |**CLOSED ROUND-71 (lane gi `a27671180`→pick `571dcf85d`)** — step (b) completion: the remaining 12 seam-4 inline slice readers + HelpTest folded onto the canonical `tests/Support/SlicesDeclaredMethodsTrait.php` (+0T/+63A; the census hop rosters stayed `[]` IN-STEP — zero-move consolidation, tree bytes unmoved outside the fold sites); reviewer F-1 INFO: the fold is convention, not instrument (the census hop resolves by unique name). Residue minted **E693** (SuiteSkipRosterTest double `$reflection->getMethod()` → r72 hb). PRIOR:  PARTIAL ROUND-69 (fd `5a9819fc7`→pick `3555a3940`)** — BOTH prescribed directions shipped: (a) `HelpTest::backendSelectionVariables` now asserts the declaring FILE per row (`ReflectionMethod::getFileName`), the census `DECLARING_FILE_UNCHECKED` row DELETED → const `[]` (verified :97 — the census redded at the overtaken arm exactly as designed pre-deletion); (b) `VhsTapeContractTest::declaredSlice` (row's :3734 cite exact) folded into NEW `tests/Support/SlicesDeclaredMethodsTrait.php` (file verified), the slice-reader census hop widened SAME-COMMIT: callee resolves same-file-first, cross-file iff exactly one walked fn carries the name, ambiguous → SLICE_UNREACHABLE fail-closed; 4 cross-file known-answer fixtures (census 10T, 569→577A). Remainder: 12 inline-slice readers keep their own array_slice (unchecked-by-design today; full list in fd REPORT seam 4, mirrored in worklog ROUND 69). |  |
| **E342** | HIGH | **CLOSED ROUND-67 wave-2 (dj → `00c8bc14b`, lane `b43fb131c`)** — all EIGHT inverted failure messages rewritten (ForkedChildTest x4, ChatTest x2, EngineBackendTest x2 — the enumerated three-per-file was itself half-right) + :379 stale flag-repair comment in E342 three-part form; filter gate byte-equal; mutation re-inverts -> guard red exactly 1. Residual stale COMMENTS travel under E319 to r68 ee. Prior row: grep -n O_NONBLOCK: ForkedChildTest.php 203,230,292,315; ChatTest.php 873,894; EngineBackendTest.php 402,419; stale comment EngineBackendTest.php:379 | Same 8 sentences are E358's residual half; fix once, close both |
| **E343** | MED | **CLOSED ROUND-67 wave-1 (lane dc → master `18daf2d55`)** — both unclassified-failure messages name the same-named-function spec bound + the asymmetry vs ChildLifetimeScanner::specFds (follows same-file T_CONST — verified at the scanner). Prior evidence: scanner has no class-constant/property resolution and the failure text at tests/Support/ChildStderrCaptureTest.php:900-907 never names the enclosing-function bound | resolution itself stays same-function by design; the call sites now SAY so |
| **E353** | MED | **CLOSED ROUND-70 (lane gd `8b639e092`→pick `80806b39f`) — CLOSED-BY-FOLD** — the guard/no-guard decision resolved as FOLD: HOOKS.md's built-in-hook and Events tables, entry/exit key/delimiter/exit-row sections, and drain/sweep/marker claims are now read LIVE by `DocFigureProseDriftTest` arms AC–AH (dynamic derivation, zero rosters; no golden file, no new test file). All adjudicated claims TRUE → `docs/HOOKS.md` byte-untouched. | rode gd's E686 tranche-7 brief per the supervisor fold-in (2026-09-12) |
| **E357** | HIGH | **CLOSED ROUND-67 wave-1 (lane dd → master `4d58dadd7`, lane `1e4ff44c5`)** — three copies folded into RefusesAnUnreadableSourceTrait (+3 fixture T); ACCEPTED_DIVERGENCE 'readOrFail' row deleted IN-STEP; ONE readOrFail impl tree-wide. Prior evidence: readOrFail() still declared at tests/Config/DocumentParagraphsTest.php:544, ConfigWriteProducerDocumentationDriftTest.php:594, GlobFigureDriftTest.php:618; DuplicatedTestHelperDriftTest.php:170 row still present |  |
| **E369** | HIGH | **CLOSED ROUND-69 (fd, same SHAs)** — the three stale census rows rewritten rule-7 against tip facts: EnvDetect (`candy-core/src/Util/Tty/EnvDetect.php` :187-194 now guards defined+is_resource+stream_isatty; zero prod callers; the new test caller `closed_descriptor_zero_probe.php:133` — row text verified :201+/:367), Program (`Program::childDescriptor()` ladder verified in the row at :244-region); the RawMode/Tty row was ALREADY TRUE at tip → its closure claim cites `e163e7bfe` (SHA verified live). |  |
| **E370** | MED | **CLOSED ROUND-67 wave-1 (lane df → master `7fbc7636b`, lane `25033773e`)** — candy-core PosixBackend::isTty pinned into the closed-descriptor-zero family (probe rows; WindowsBackend judged-not-a-row with reason). Prior evidence: candy-core/tests/Util/ClosedDescriptorZeroFamilyTest.php:97-107 - no PosixBackend::isTty() row; grep PosixBackend in that file returns nothing | nearest existing coverage was PosixBackendTest.php:75 with a php://memory stream, not a closed one |
| **E375** | MED | **CLOSED ROUND-69 — RULING: B-QUALIFIER (fb `5ea48fe21`→`ff2a96306` recorded the question + fc `71cc12f38` recorded the ruling in-ownership + fn `5d050411d`→`f2c2c2327` shipped the carrier)** — `DenialKind` keeps EXACTLY three cases with byte-identical backing values (verified at tip :91 'Permission denied:' / :102 'Permission required:' / :109 'Hook denied:'; the enum BODY untouched — the round's whole diff to that file is fb's +19-line decision-home doc-block paragraph, so every 3-count pin — README roster, backing values, DenialPrefixRosterTest — stays green BY DESIGN). Carrier: `NonInteractive::$askWasUnattended` (verified :712) — cleared at run() (:246), SOLE writer HeadlessPermissionPrompt no-tty arm (:383 region), read-and-clear in `refusalFrom()` gated `kind === Refused`; the refusal row gains the OPTIONAL 4th key `unattended: true` ONLY on that arm (row shape verified :248) — human/hook/unanswered rows stay 3-key BYTE-IDENTICAL (fb's exact-array pins survive; `ToolRefusal.php` ZERO edits — verified absent the token). Pins: `testAnAskRefusedWithNoTerminalCarriesTheUnattendedQualifier` + consume-proof vs `testAHumanAnsweredRefusalCarriesNoQualifier` (real EngineBackend + PermissionGate(Default) Edit-ASK arms); README refusals conditional-key paragraph rode IN-STEP (:417-428, hunk-local vs fh's ~:1046 Tools bullet). Carrier-strip mutation → exactly the unattended pin red. **ROUND-70 CONFIRMATION (gb `fdeddc1f3`→pick `a852eb2ba`):** the DECIDED stamp landed in the `DenialKind` class doc-block (`src/Permissions/DenialKind.php` — the decision's home): three cases FROZEN, qualifier = optional 4th envelope key, carrier lifecycle named, fn SHAs cited; the doc-block no longer reads undecided. No census move. | machine-only qualifier: noticeRefusal stderr stays UNCHANGED (it already names the arm) |
| **E376** | HIGH | **CLOSED ROUND-69 (lane fc `183474831`→pick `71cc12f38`)** — one-line doc-block fixed, re-verified at tip (`HeadlessPermissionPrompt.php:277`): `$in` is now `@var resource|null` naming the cause — `NonInteractive::stdinDefault()` answers null when no pin is installed AND \STDIN is closed (the old bare `resource` was a lie since E338); `$err` KEPT `resource` exactly per the entry (resolves `?? \STDERR`, never null — widening would invite a guard the write() path does not need); ctor `@param resource|null` was already correct, untouched. Behaviour unchanged (is_resource-gated readers). |  |
| **E378** | HIGH | **CLOSED ROUND-69 (lane fg `0dd5a61fa`→pick `bce05c166`)** — one-word SCOPE addition, verified at tip :60: reaper SCOPE now includes 'Hooks/'; M3 mutation (comment the reap in the Hooks test) reddens naming `Hooks/AuditHookConcurrentAppendTest.php` — the row is alive, not decorative. |  |
| **E386** | HIGH | **CLOSED ROUND-67 wave-1 (lane dd → master `4d58dadd7`)** — same fold as E357; the trait's fixture reaches the arm. Prior evidence: sugar-crush/tests/Config/{DocumentParagraphsTest.php:544,GlobFigureDriftTest.php:618,ConfigWriteProducerDocumentationDriftTest.php:594} each still declare private static readOrFail(); none uses RefusesAnUnreadableSourceTrait | the DuplicatedTestHelperDriftTest ACCEPTED_DIVERGENCE readOrFail row was deleted in-step |
| **E391** | HIGH | **CLOSED ROUND-69 (fk, same SHAs)** — the declaring-filter replaced: `requirementsOf` walks IN-TREE parents (the vendor TestCase chain is skipped deliberately — machinery, never a population, ~200 parser calls/file); inherited hits keyed `<declaring FQN>::<method>`, own declarations keep the bare key; class-level directives NOT inherited (mirrors forClass); fixture pair `Fixtures/AnnotationSkipProvenance/DirectiveBearingParentFixture` + `InheritedDirectiveChildFixture` (verified referenced :63/:119); `skippedTestsIn` UPGRADED file::name → class::name with the child-run measurement recorded (an inherited skip prints the PARENT's file attribute + the CHILD's class attribute — file-keyed rows would collapse under a concrete parent's own skip); exact-match set 2→3 rows in instrument sort order; population paragraph + @see roster + attribution arms in-step (M2 restore-filter mutation reddens exactly the child-attribution arm). | runner prints 'DirectiveBearingParentFixture is abstract' as a harmless warning — recorded in fk REPORT so no one chases it |
| **E417** | HIGH | **CLOSED-WITH-EVIDENCE ROUND-67 wave-1 (lane dc → master `18daf2d55`), zero code** — re-proved: child inherits ALL non-cloexec parent fds regardless of spec; naming fds ADDS never removes; stoppers = O_CLOEXEC/'e', close-before-spawn, reaping — the guard file already carries that truth. Prior evidence: sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:127-193 still holds all seven ACCOUNTED_FOR rows, every reason a deferral | The entry's prescribed fix is SUPERSEDED: the same file :96-105 records that appending a high fd to the spec does NOT stop inheritance (testNamingAHighFdDoesNotStopTheInheritance), so "name the fds in the descriptor spec" is measured wrong; a row closes only by reaping in the spawning function |
| **E419** | MED | **CLOSED ROUND-69 (fk, same SHAs)** — `enclosingArrayOpener` backward walk (verified :530/:1197) follows array membership ONE LEVEL: from `=>`, stoppers `; { } ( )` and T_ATTRIBUTE; property-store-through-member returns LONG; a plain-local assignment registers a CARRIER whose return/store/escape fates are reported AS the handle's ('stored in $x through $bundle'); rostered closers invoked on a carrier buy NO short claim (they land in the escape sentence). Real-site flip measured by direct scan probe: `ProcessExecutor::spawnWorker` unclassified → LONG 'the handle in $process is returned in $processDescriptor'; `DescriptorInheritanceGuardTest` ACCOUNTED_FOR row rewritten in-step (still count 1 — LONG stays exposed). 6 new fixture arms; the old cannot-follow fixture RENAMED to the followed contract (external citations grepped: none). | M3-M5 each exactly red (carrier never-registered → 4 arms; closer-claim and paren-stopper mutations discriminate per arm) |
| **E424** | HIGH | **CLOSED ROUND-69 (fk, same SHAs)** — roster keys now RESOLVE against src/: `testEveryRosterKeyNamesAMethodThatExistsInSrc` requires every CLOSING_HELPERS/BEST_EFFORT_REAPERS key to name a class + method declared TOGETHER in one src/ file, derived through `SourceFileWalkTrait::everySourceFileIn(package, ['src'])` (fi's trait — second consumer, immediate vindication); ghost-class and ghost-method controls run the SAME resolver (rule-15 pair); population guard makes an empty walk vacuously red; class header gained the measured EXCEPTION paragraph (a name-walk, not a spawn-site walk) and the spending arm's 'residual recorded in the backlog' sentence flipped in-step (existence now closed; the behaviour claim stays human-measured). |  |
| **E445** | HIGH | **CLOSED ROUND-69 (lane fg `0dd5a61fa`→pick `bce05c166`)** — both rosters re-keyed `<file>::<function>` (verified: `ACCEPTED_BARE_EXIT` keys carry `::testPlainExit…` from :129; UNTRACKED_FORKS_ALLOWED likewise); scanner's `functionKey()` is the single shared implementation; `missingHalves` 3rd param int → fn=>count map with PER-FUNCTION allowance spend + staleness/count arms; message gains ' in fn()' (never for the `<top>` scope — fixture messages stable). M1/M2 mutations name the exact old hole (`Support/ForkedChildTest.php::testMutationM1UnarguedFork` / an unlicensed fn): a licence can no longer be spent by FILE. |  |
| **E447** | HIGH | **CLOSED-REFUTED ROUND-67 wave-1 (lane df → master `7fbc7636b`), NO-OP** — the entry's action is moot: E485's guard arm covers the shape (df re-measured at tip). Prior evidence: candy-core/src/Program.php:847 $descriptors still assembled from $childIn/$childOut/$childErr helper results; guard scope unchanged at sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:2597 sourceFiles() = own src only | neither half of the STEP done |
| **E448** | HIGH | **CLOSED ROUND-67 wave-1 (lane df → masters `aa2497e07` dash / `5b6bb6b1b` reel / `1a4c39fc4` repo)** — bounded-reap mitigations landed in the libs themselves (E366) + the three spawns sit ACCOUNTED on the new repo-wide gate. Prior evidence: ChildLifetimeScanner still at sugar-crush/tests/Support/ChildLifetimeScanner.php; grep -rln ChildLifetimeScanner over sugar-dash/tests sugar-reel/tests candy-testing returns nothing; the three spawns persist (sugar-dash/src/Plugin/ExternalModule.php:152, sugar-reel/src/Decode/FfmpegDecoder.php:160, sugar-reel/src/AudioPlayer.php:79) |  |
| **E466** | HIGH | **CLOSED ROUND-67 wave-1 (lane df → master `436965761`, lane `98e21fda0`)** — the owed caller census shipped (comment-only): 18 scopes, zero post-close fd reads; the dup is released inside close() — no window ever. Prior evidence: candy-pty/src/Posix/PosixMasterPty.php:295-297 "no search for a caller pattern that WOULD need a stable reference has been run" | entry is explicitly "Not done"; the owed caller census across candy-pty/flip/mosaic/wish/sugar-crush still does not exist |
| **E469** | HIGH | **CLOSED ROUND-69 (lane fe `b6c2c8283`→pick `41b19f5e5`)** — all three arms exercised (+3T): (A) FORK E2E — an off-roster skip armed BEFORE `pcntl_fork()`, child `exit(7)`; asserts parent rc!=0, banner `substr_count == 1`, `CHILD-EXIT: 7` in the child (verified :775-:806 region); the no-pcntl branch keeps the banner-count arm; the PARENT never markTestSkips (suite self-guarded) — guard mutation → CHILD-EXIT:1 + banner 2× exactly as predicted (M4). (B) real-EXPECTED `countsOff`: the full real roster passes clean; a planted duplicate reds 'SKIP EVENT COUNT IS N+1, ROSTER SIZE IS N' with N derived, never pinned (M3). (C) preparability premise reflected over rostered files — Requires attributes above Test\Prepared disarm the counts check; body-level markTestSkipped present; setUpBeforeClass not skipping (M5 with an injected RequiresFunction → exactly that arm red). |  |
| **E486** | HIGH | **CLOSED ROUND-67 wave-1 (lane df → master `1a4c39fc4`, lane `1afee66ab`)** — alternative shipped: tools/check-child-lifetimes.php requires the canonical ChildLifetimeScanner BY PATH (no third copy), 57-lib autoload-root walk, 24 sites, ACCOUNTED roster 6 rows, fail-closed, CLI-wired as the LAST step of path-repo-check (SET equality couples them). Prior evidence: ls tools/ -> only check-path-repos.php, gen-ansi-readme.php, gen-docs.php; no child-lifetime step in .github/workflows/ci.yml path-repo-check | entry is explicitly NOT DONE; recommended tools/ script + ci.yml step never landed. ROUND-66 follow-up stamped here (ci.yml is outside closeout touch): E691's "needs a clover-merge story" YAML comment was checked at `61cde19c5` — NO surviving occurrence (a59d8a547 rewrote the coverage-job comments in-file), so no comment edit is owed; the remaining clover mentions in docs/plans are the §0-NOW-66/-67 history blocks (kept) and the now-CLOSED E691 rows. df owns E486/E629 proper. |
| **E493** | HIGH |**CLOSED ROUND-71 consumer half (lane gh `4cb065af3`→pick `9d05c5e9f`, atop providers-half gc `d7be5733f`)** — `$onHeartbeat` threaded `EngineBackend::complete()` tail → `Runtime::run` 6th arg → `CompleteRequest`; the forked child beat-writer emits EMPTY-TEXT reasoning frames; parent runs `$resetTimeout` FIRST per frame (`EngineBackend.php:1236-1238`) + drop-empty before `$onReasoning` (`:1281-1286`) — reviewer-verified literally, no phantom paint; NEW `EngineBackendHeartbeatThreadingTest` 4T/13A (fork via `forkTracked`, kinds-exact pins both polarities); `AwaitPromise` `matching()` 4th copy folded onto `TokenFunctionRanges::matching()`; zero wall-clock kills (E646 honored); +4T; review r71-rv-gh APPROVE 0C/0M/2 MINOR. PRIOR:  **PARTIAL — providers-half SHIPPED ROUND-70 (lane gc `9de0e3fd6`→pick `d7be5733f`)** — `CompleteRequest::$onHeartbeat` (tail `?\Closure`, default null) + `HttpClientDefaults::heartbeatOptions()`: Guzzle `progress` (E524 — the only carrier that fires inside the blocking curl), 1/s throttle with immediate first beat, fail-soft Throwable swallow, wrapper always returns false (libcurl keep-transferring); wired on the Sglang + Custom batch `complete()` paths; +8T in NEW `tests/Providers/ProviderHeartbeatProgressTest.php` (durations-rowed at the w2 re-pin; M2/M3 mutation arms). Prior (fa r69): record re-verified + extended, E524 measurements stamped at `EngineBackend.php:87`. | **r72 ha residue:** `src/Backend.php` `$onEvent` docblock roster UNPINNED — live-arm pin (gh reviewer MINOR; moved from hb to ha at the closeout filemap regen — the arm is a DocFigureProseDriftTest lane and ha owns that file). OpenAI/Bedrock/Vertex own their transports — gaps documented in the DTO doc-block |
| **E564** | HIGH | **CLOSED ROUND-69 (lane fh `19eb66001`→pick `d7272bd0a`)** — resolution (2) taken and GUARDED: MAX_SECTION_BYTES 'THE MEASUREMENT' + MAX_ENTRY_BYTES 'MEASURED' paragraphs recast as DATED RECORDS (the 2026-08-21 revision; provenance via git log -S on `bc7b17f60`/`6c33705c5`), all live digits elided (6,878 / 1,314 / 1,915 / 57-and-32 / 159-165 B medians / 49-of-58 / +947 / +1,836 / 8,714 — verified 'DATED RECORD' headings at RepoMapBlock.php :238/:274), durable conclusions kept (sized-to-largest-measured-input off the growth ladder; the clip-vs-drop cliff at 160; the '160 B median was the upper middle pair, not the midpoint' error lesson). New pin `testTheByteCapDocblocksCarryTheirMeasurementsAsDatedRecords` (verified :938): reflection-reads BOTH const doc-blocks, asserts date-anchor count==2 + conclusion substrings + ABSENCE of every retired figure; M1-M5 each exactly red. | reader-mechanism lesson: a path literal in this test re-grades its own file under TreeWideGuardRoster's walker census (2F) — use ReflectionClassConstant::getDocComment, no dirname walk |
| **E566** | MED | **CLOSED ROUND-67 wave-1 (lane df → master `1a4c39fc4`), re-derived NO ACTION** — all remaining T_FUNCTION-no-T_FN mentions are NAME/forward/comment/brace-walk idioms, not walk bounds. Prior evidence: re-ran the entry generator: 22 of 26 files under sugar-crush/tests/ name T_FUNCTION and never T_FN (entry says 19 of 21) | Figures have drifted since filing; entry is CENSUS ONLY and forbids a blanket edit, so re-derive before acting |
| **E572** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`, lane `2a943d13b`)** — both blind spots closed (indirect-assertion + set_error_handler arms) + permissive-fallback keystone; 10 AgentManagerTest true-positives on the shrink-only roster (3 byte-identical dup rows = cosmetic closeout chore). Gotcha: an EXECUTED swallowing handler poisons the PHPUnit process itself. Prior evidence: sugar-crush/tests/SwallowingCatchCensusTest.php:60-67 still declares both blind spots (indirect assertion, set_error_handler) as not closed |  |
| **E577** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`)** — `resolvedTypes` FQN key implemented (display-label rename option declined). Prior evidence: sugar-crush/tests/Support/AssertionSwallowingCatchTest.php:198,223,649 — @return still 'types: list<string>', no resolved-FQN key, no display-only label at definition | prescription (second key OR rename) landed as the key |
| **E578** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`)** — pinned in BOTH measured polarities; E578-as-written FALSE per E615 (real divergence is the other direction) — closure evidence is the two-polarity pin, not the original fixture. Prior evidence: sugar-crush/tests/SwallowingCatchCensusTest.php:90-245 fixtures cover group-use/same-namespace/unresolvable; none pins 'permissive fallback can never yield safe' | resolve() doc-block now states both directions |
| **E583** | HIGH | **CLOSED 2026-09-12 r68/ea `c2a9b4089`→pick `e4b6cb931`** — 11 owned bare {@see} citations → declaring FQNs (7 files); SymbolCitationDriftTest widened w/ comments-only tree census + KNOWN-POSITIVE planted offender + DEFERRED_BARE_CITATIONS roster (6 rows/7 occurrences in 5 files — SessionStartHookWireTest under tests/Chat/ :275+:373, GlobDialectDifferentialTest:961, RulePathScopingWiringTest:398, RuntimeInitialDispatchOrderTest:22, DuplicatedDocBlockLineTest:30/:113) w/ staleness arm :1262-1268; guard GREEN on merged tree; roster carried to r69 lane fi. Prior: 12 bare {@see X()} citations still present under sugar-crush/tests (pump/flattened/significantTokens/forPaths/readResponse/__destruct/selectedProviderLabel/warnPermissionConfigInTranscript) — 11 owned fixed by ea, the 12th (pump) is E547, closed same pick **ROSTER CLEARED ROUND-69 (fi `e30fb540a`→pick `58d030980`)**: all 6 DEFERRED_BARE_CITATIONS rows / 7 sites qualified IN PLACE (SessionStartHookWireTest :275/:373→Chat FQNs; GlobDialectDifferentialTest:961→SkillRegistry::legacyPathMatch(); RulePathScopingWiringTest→Bootstrap::tools(); RuntimeInitialDispatchOrderTest:38→Runtime::executeToolCalls(); DuplicatedDocBlockLineTest :30/:113→TestFileWalkTrait::everyTestFile()); roster retires EMPTY in the same commit, staleness arm KEPT, loops no-op by design; no numeric roster-size assertion existed (verified); mutation re-bare→guard RED naming :38.| SymbolCitationDriftTest now keys on the whole comments tree, not test-method spelling (looksLikeATestSymbol superseded); roster line cites are the guard's comment-open anchors — the bare {@see} tokens themselves sit at RulePathScopingWiringTest:400 / RuntimeInitialDispatchOrderTest:38 |
| **E609** | HIGH | **CLOSED ROUND-69 (lane fi `e30fb540a`→pick `58d030980`)** — SHIPPED (not already-landed): NEW `tests/Support/SourceFileWalkTrait.php` (file verified) — `everySourceFileIn(string $package, list<string> $roots)` package-relative keys + shebang-aware `isPhp` (DuplicatedDocBlockLineTest's stronger superset); both drifted private walkers deleted (DuplicatedDocBlockLineTest −64 lines; BackendSignatureNullabilityTest keeps a 1-line delegating wrapper — both consumers verified at tip); `contractFamily()` untouched (different mechanism). Immediate second consumer: fk's E424 resolver derives through the trait. | honest note shipped: a shebang-PHP file under src/ now joins the Backend walk — its own census's correct meaning |
| **E610** | HIGH | **CLOSED ROUND-67 wave-2 (di → `a94628c5b`)** — REAL hole fixed: the name walk steps over T_COMMENT/T_DOC_COMMENT (the exposure was the IMPLICIT-public leg silently dropping; explicit half was red-safe); proved green-at-base/red-post-fix naming testProbeDi; permanent 3-polarity synthetic; guard 11T/104A -> 14T/160A. Prior row: DuplicatedTestHelperDriftTest.php:2126 name walk skips only T_WHITESPACE, no T_COMMENT arm | Entry itself says census is zero sites; latent |
| **E611** | MED | **CLOSED-BY-PRACTICE / SUPERSEDED (round-76, lane kb — verdict DROP)**; was design-carry. | **ROUND-76 CLOSE (lane kb, evidence /home/sites/crush-r61-artifacts/kb/verdicts.md):** re-derived at tip f8eba0f34 — (Q1) still no machine check: grep -rni "ownership" over tools/ scripts/ prompt_kit/tools/ .github/workflows/ returns only a durations-file NAME, and check-path-repos has zero brief/lane/scope logic (E334 existence-only confirmed); (Q2) the OWNERSHIP-block practice demonstrably catches out-of-ownership commissions — worklog :3069 (SkillLoader half filed out-of-lane: REPORTED not implemented), :5437 (left alone, outside ownership), :11010 (reviewer bound minted), triage r69-close (fj Chat.php breach caught + dispositioned); (Q3) the residual harness-tool + schema needs a stable machine-readable lane registry that does not exist (lanes are per-round ephemera in merge-owned RESUME prose) — building it IS the excluded harness design; no in-scope code/test step remains. **ROUND-69 RENOTE (fh seam, backlog :18927):** the fix is a SUPERVISOR-HARNESS tool + a machine-readable ownership schema (prompt_kit/briefs are prose .md with no owner map) — architectural, OUT OF CODE-PLAN SCOPE; mitigation in force since round-62 = explicit OWNERSHIP blocks in every brief. Carried as a design item, not a code finding.  **ROUND-75:** verdict SLIPS again — kb (r76) is the pick-or-drop verdict lane; r75 lanes (ja/jb) never touched it. |

| **E612** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`)** — census now models catch-clause order; the post-EFCatch repair shape is no longer reported. Prior evidence: sugar-crush/tests/SwallowingCatchCensusTest.php:404-416 classifies every catch clause independently; no skip for clauses after an ExpectationFailedException catch | Prescribed ordering rule implemented |
| **E615** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`)** — the two Demo578 rows pinned with the corrected direction; resolve() doc-block names both. Prior evidence: no fixture pinning the two Demo578 rows exists in SwallowingCatchCensusTest.php; resolve() doc-block at :562-570 still names only the harmless direction | E578 closes on this correction |
| **E616** | HIGH | **CLOSED ROUND-70 (lane gb `fdeddc1f3`→pick `a852eb2ba`) — DECIDED: DENIAL_SHAPE bytes UNCHANGED, hyphen-tail visibility ACCEPTED** — fb's measurement is the ruling's basis (adding `'-'` to the lookbehind reports all three tail fixtures NOWHERE while the roster prefixes frame identically); the trade is pinned BOTH ways: the three tail frames stay pinned REPORTED and a dated DECIDED block sits in `DenialPrefixRosterTest` (~:748, comment-only hunk — DENIAL_SHAPE byte-untouched, diff-verified by gb + reviewer). | decision recorded; revisit only if a future roster term needs hyphen-aware framing |
| **E617** | HIGH | **CLOSED ROUND-67 wave-1 (lane da → master `d3fb349fd`)** — reachability claim superseded: da re-measured and landed E577's key in-lane. Prior evidence: tests/Support/AssertionSwallowingCatchTest.php:258 types key set with no display-only sentence; @return at :223 carries no resolved-FQN key | tracking row retired with its subject |
| **E629** | HIGH | **CLOSED ROUND-67 wave-1 (lane df → master `1a4c39fc4`)** — ciJobNeeds block-list spelling + ciJobName literal name-pin test (mutation-discrimination proven by df). Prior evidence: tools/tests/CheckPathReposTest.php:80 CI_JOBS keys on job id only; :453 ciJobNeeds matches only /^    needs:\s*\[([^\]]*)\]/ | Both deferred sub-items unchanged |
| **E657** | MED | **CLOSED ROUND-66 lane ca (master `31b6b94df`, lane `5b7348249`)** — src/ prose `File.php:NNN` citations re-anchored to symbols (rule 4), prose only, floor-exact | Was lane-D inventory: SglangProvider/VertexProvider/Runtime "+ more". Landed. |
| **E658** | LOW | **CLOSED ROUND-66 lane ca (master `31b6b94df`)** — the ~20-test-dir sweep completed: ~77 of 112 raw citation units judged fixed (symbol re-anchors), ~35 held labeled-historical/measured-external | **ERRATA:** the ca REPORT's header split "31 fixed / 81 held" double-counted wrapped multi-line cites; the ~77/~35 re-derivation (closeout review) supersedes it — honest record, history unrewritten. |
| **E659** | MED | **CLOSED ROUND-66 lane ca (master `31b6b94df`)** — guard-test rename + `RepoMapBlockTest` `{@see}` + `SymbolCitationDriftTest` enforcement in ONE commit as prescribed | Landed. |
| **E662** | LOW | **CLOSED ROUND-66 lane ca (master `31b6b94df`)** — record-only re-derivation done: autoload-then-miss stays unreachable, `classNames()` comment flags it, corpus gate never reds | Record retired; re-open only if the corpus gate reddens. |
| **E670** | LOW | **CLOSED ROUND-66 lane cd (master `7f352c691`, lane `5b61bdb31`)** — property-order nit fixed inside the existing pin | Same pick carried the E687 census pairing + r65 seam bundle. |
| **E671** | MED | **CLOSED ROUND-64 lane aa (master `f67328f94`)** — the ci.yml sugar-crush cell runs `scripts/parallel-tests.sh` at K=min(nproc,4); `scripts/parallel-tests-durations.tsv` committed; `--against-json` conservation vs suite-figure.json is FAIL-CLOSED (a new test file w/o a manifest row → shard-sum < figure → red). K=8 REFUTED by measurement: runners are 2–4 vCPU, ≥2× oversubscription trips the lease/idle-ceiling races | Was ROUND-64 STEP 1 (user directive) — landed. **R67 addendum:** local (devbox) default flipped to K=min(nproc,8) at `fb5078e6d` (lane ks `4810321e4`) — 565s serial → 159s K4 → ~65–80s K8; hard floor the 62.6s single-file ProcessExecutorTest bucket; K-sweep 8/16/24/32 no gain past 8. CI UNAFFECTED — both ci.yml legs keep explicit K=min(nproc,4); the K=8 refutation left is runner-scoped (2–4 vCPU) | Coverage-job sharding seam minted as **E691**; harness gained `--durations`/`--against-json` (additive). |
| **E672** | HIGH | **CLOSED ROUND-64 lane ac (masters `8aaa7211b`→…→`ff1e9530f`)** — `src/Support/ProcessContainment.php` choke point; 10/10 measured spawn sites routed (roster as listed) | Review caught the bogus-binary fail-fast lost through the wrapper — LspConnection/StdioMcpServer (`7d680fe99`), then ClaudeCodeInvocation/ClaudeCodeProvider as sites #3–4 FOUND ONLY BY THE FULL-SUITE GATE; routed-site sweeps must grep start-failure contracts tree-wide. |
| **E673** | MED | **CLOSED ROUND-64 lane ac** — terminate-with-container wired: `?int $groupPid` reaper rungs (literals 15/9), pgid==pid discriminator | Designed WITH the cancellation wiring, as the note required. |
| **E674** | LOW | **CLOSED ROUND-64 lane ac** — `ProcessContainment::env()` strips SUDO_ASKPASS/GPG_TTY and forces NONINTERACTIVE | Effective at all real spawns (choke-point coverage measured 10/10). |
| **E675** | HIGH | **CLOSED ROUND-64 lane ab (master `b636591b6`, review-fix `3b76a0e8d`)** — Bootstrap::chat() hoist threaded through backend()/backendFor()/tools() + private taskWorkerPool(); TaskTool moved → src/Tools/BuiltIn/ with the Runtime.php:549 FQN sweep; census trio flipped in-step (CorpusTest 11→12) | Live-feed + pool-constitution pins mutation-proven — P8.13 is REAL. |
| **E676** | MED | **CLOSED ROUND-64 lane ad (master `b76fee3cd`)** — grace ladders consolidated into `ProcessReaper::escalate()` | Real trap-ignoring-child e2e pins: TERM spent, KILL lands. |
| **E677** | MED | **CLOSED ROUND-64 lane ad (master `b76fee3cd`)** — MCP dispatch-entry pumpStderr mounts (entry + timer; the idle-drain removed — the timer was the drain) | LSP-side dispatch-entry mount was out-of-ownership in ad — minted as **E690** (round-65 ba). |
| **E678** | MED | **CLOSED-SUPERSEDED (ROUND-66 lane ch verdict, measured at `61cde19c5`)** — CLI/Bootstrap half ALREADY-LANDED: `Bootstrap::mcpServerInventory()` (`:5384`) since `a4be8263f`, pre-dating this entry — the panel was a consumer of the same source of truth, never this id's missing half (the filing chronology was inverted); `crush mcp --json` wire rows `b76fee3cd` (r64 ad); /mcp panel `6220e5b29`/`a6768adde` (r65 ba) pinned vs live inventory | Residual zero; subsumed by E665/E689. Closeout flips backlog heading + filemap row the same day. |
| **E679** | LOW | **CLOSED ROUND-64 lane ad (master `b76fee3cd`)** — `Support/TimedFileLock` replaces the two drifted hand-rolled twins | TaskList byte-identical; WorktreeManager gains the @-suppress — drift settled deliberately. |
| **E680** | MED | **CLOSED ROUND-64 lane ad (master `b76fee3cd`, census-fix `85e8ad2f7`)** — Session.php verified flock-free: the blocking-flock entry claim was FALSE; a pin added instead | A foreign LOCK_EX on dest never delays save (deterministic pin). |
| **E681** | HIGH | **CLOSED ROUND-64 lane af (master `e4f46f7ed`)** — supervisor decision taken per the recommendation: FIX the save ORDER — Chat.php checkpoint snapshots `$this->inputBuf` pre-clear, rewind restores it (no more hard-coded '') | Polarity-1 discriminating pin (real Enter-submit → store round-trip → /rewind verbatim; reverting either half reddens it); RewindCommandTest:189 now non-vacuous; legacy keyless checkpoints → ''. |
| **E682** | MED | **CLOSED ROUND-65 lane bb (masters `cf70cd0ae`/`8222f9576`; lane `219bd908d`+`d91a3c461`)** — paint-only adoption: `Renderer::setPaletteAbandoned()` computed per frame by `App::view()` from E666's predicate; suppression ≠ close; ghost mutation-pinned (8.8 KB); sidebar-palette negative pinned | Full composite-live route remains open (E12). See E666. |
| **E683** | LOW | **CLOSED ROUND-65 lane bb (master `c477505fc`; lane `87508724f`)** — prose half TRUE → adoption route written; mouse pane-jump half measured FALSE (chrome zones menu-only; `SelectPaneMsg` consumer-without-producer) → pinned DORMANT, not removed (`MousePaneJumpDoesNotHandOverTest`); `mouse.pane` description de-overclaimed to the drift-observed truth | See E666. |
| **E684** | HIGH | **CLOSED ROUND-64 lane ab (master `b636591b6`)** — Bootstrap totals prose re-measured: 29/128/3 at :408, 29/135/2 at :450 | McpToolWiringTest pins live; suite filters for the file must use `-c phpunit.xml`. |
| **E685** | HIGH | **CLOSED ROUND-64 lane ae (master `c0e4681d2`)** — E633-SLICE-2 done: 12 claims judged — 2 fixed-by-reword, 1 true-claim pinned via a REAL mutation-able `DocFigureProseDriftTest` generator, 9 held-with-domains; stale WorkflowEngine.php:895/:1063 cites → symbol-based | src/Tools quantitative-claim roster now guarded; repo-wide residual → E686. |
| **E686** | MED |**CLOSED ROUND-75 — CAMPAIGN CLOSED by orchestrator ruling (tranche-11 FINAL: ja 51e5b9b39→pick 99cc68e02 arms AI–AO +8T, DocFigure 63→71 71T/2451A — bare-anchor guard, Providers/AA/COMMANDS/TEMPLATE/tags live rosters, cross-page arms AN/AO; ARCHITECTURE five-anchor + AGENTS_AUTHORING five-patch heals. rv-ja 1 MAJOR (sibling-page eleven-vs-twelve) healed by ja2 ddf761169→pick 332e6fdd7: AK1 CROSS-PAGE AGREEMENT leg + AI planted-mismatch leg; ja2 erratum: ARCHITECTURE:31 digit row EXISTS pinned by arm H. Companion 02c65e6eb = Bootstrap tools() docblock twelve-truth; r76 seam: Bootstrap BODY live-false eleven claims + README:224/:233 + ForeignAgentPresetWiringTest:273 "six". Residuals HELD-with-reason (§9.12 external dated 6==6, COMMANDS JIT dated, PROMPT_ENGINEERING `models`). PRIOR TRANCHE-8:  (lane gg `9cdc344b5`→pick `7924075c7`; review-fix gg2 `b123b1355`→pick `00bab5d21`)** — arms AI–AR (AQ SKIPPED — its spacing claim is arm AH domain), DocFigure 38→47 methods (+9T/+141A); 2 FALSE ARCHITECTURE.md claims fixed (maxSteps anchors, ext-sqlite3 TaskList caller) with re-introduction mutations; gd 10 HELD carry rows ALL dispositioned; reviewer MAJOR fixed in gg2: arm AP hand-typed layer roster survived the M10 RuleLoader→RuleReader rename → roster now DERIVED from the doc segment (cite↔roster bidirectional, `interface_exists || class_exists`, `(?:\(\))?` regex gotcha; M10 pre-green/post-RED + M-DEL RED). PRIOR:  **PARTIAL — TRANCHE-7 LANDED ROUND-70 (lane gd `8b639e092`→pick `80806b39f`)** — 22 claim rows (HOOKS.md/SKILLS.md/ENVIRONMENT.md + the E353 fold surface) judged ALL TRUE under live re-derivation, ZERO FALSE (docs byte-untouched); arms AC–AH, DocFigure roster **32→38** methods: AC built-in-hook tables (grid RECOMPUTED from live name\| campaign continues — tranche-9 = ANTHROPICS/PROVIDERS/TOOLS/SKILLS/MCP per-arm tables (carry gd/gg measures.md HELDs; GlobDialect glued-literal law binds) | campaign continues — tranche-8 = the ~9.5 HELD carry per gd/measures.md carry-dispositions (9 carry HELDs + 3 fresh labeled-external holds, each row named); E353 CLOSED by this tranche's fold |
| **E687** | MED | **CLOSED ROUND-66 lane cd (master `7f352c691`, lane `5b61bdb31`)** — `__destruct()` teardown total `error_log` (forkFailureCount>0, after owner guard, before resultDir early-return, message substring-disjoint from the two existing lines) + census trio IN-STEP (AgentWorkerPool row 2→3; TWENTY-THREE→TWENTY-FOUR aggregate prose; NUMBER_WORDS untouched — "twenty-four" pre-existed) + `AgentWorkerPoolTeardownForkTotalTest` 4T/15A | The brief's "28→29" roster claim judged FALSE-TO-TREE (that figure is Bootstrap's channel-5 PREFIXED_WRITER_SITES census — untouched here). E261 closes on this half. |
| **E688** | MED | **CLOSED SAME ROUND, lane ac (master `ff1e9530f`)** — `getExitCode():?int` with a 2.0s REAP-WAIT bound, explicitly NOT a request timeout (the E646 standing prohibition honored) + `WorkerExitCodeAttributionTest` keystone | Minted by the ac review (exit-code attribution raced the reap); born AND closed in-lane. |
| **E689** | MED | **CLOSED ROUND-65 lane ba (masters `6220e5b29`/`a6768adde`; lane `90747e04a`+`1c85b8498`)** — `src/Tui/McpPanel`, every row derived from `Bootstrap::mcpServerInventory()` (zero rosters, randomised fixtures), mounted in the `McpAuthCommand` list arm (slash + palette both covered); review added the both-arm emission pin (mutation-verified) + `Width::truncateMiddle` clipping | Trust toggle declined — would invent a second persistence seam. `crush mcp --json` wire-name rows already landed (`b76fee3cd`). |
| **E690** | MED | **CLOSED ROUND-65 lane ba (masters `858614688`/`ce87e4e1d`; lane `35b38ea5c`)** — `LspClient::pumpStderr()` fan-out (instanceof-gated) mounted at all five `*For` dispatch entries, `diagnostics()` skipped (pure cache read); `LspClientDispatchPumpTest`: post-settled noise drained by the NEXT op, <2s, zero timers, mount-order row | Drift fix `ce87e4e1d` (lane `6c3168007`) aligned the copied `waitForFile` fail-message to canonical (DuplicatedTestHelperDriftTest). Mirrors the MCP entry+timer mount (`b76fee3cd`). |
| **E691** | LOW | **CLOSED ROUND-66 lane cc (master `a59d8a547`, lane `a2353660f`)** — `parallel-tests.sh --clover` (OFF-path byte-compatible) + `scripts/merge-clover.php` (per-line union, crap recompute, fail-closed exit 2, byte-identical re-merge) + `scripts/test-merge-clover.php` 66-check CI self-check + ci.yml coverage job split (serial non-sugar-crush / sharded sugar-crush K=min(nproc,4), conservation gate FAIL-CLOSED before merge, same `sugar-crush/coverage.xml` artifact path — downstream consumers untouched) | Validation: pcov K=4 conservation PASS 11,494/176,293 delta +0/+0; serial-vs-merged drift gross 3 lines (process topology, zero merger loss). The YAML's stale "needs a clover-merge story" comment: grep at `61cde19c5` finds NO surviving occurrence — a59d8a547 rewrote the coverage-job comments in-file; no follow-up owed. Devbox record: only xdebug 3.2 present, SEGFAULTS nondeterministically under full-suite coverage — pcov built from source for the validation. |
| **E692** | LOW | **CLOSED-WITH-DECISION ROUND-65 lane bc (master `e18184e8a`, a `b7bb99ff5` hunk)** — the in-process, exec-free `pcntl_fork` carries no argv/PATH/interactive surface, therefore OUT of `ProcessContainment`'s exec-spawn remit; no pgid story owed | The scope call landed as a comment at the fork site itself — reasoning stands with the code. CLOSED-with-decision stamp applied at this close. |
| **E693** | LOW | **CLOSED ROUND-72 (lane hb `2dcc802f1`→pick `179f0b37e`, +0T/+4A)** — double `$reflection->getMethod()` folded onto `SlicesDeclaredMethodsTrait`; gate slice keeps `$gate->getFileName()` (inherited gate — classic fold bug ABSENT, comment names why), rostered slice keeps `$reflection->getFileName()`; census prose truth-flip, rosters stay `[]`. Builder died mid-commit, finisher salvaged (-72 re-proved); reviewer re-proved via own M1 RED at :533. PRIOR: **OPEN — MINTED ROUND-71 close (gi residue)** — `SuiteSkipRosterTest` rostered-skip body indexes `$reflection->getMethod($method)` twice; the slice-reader census docblock names it the SOLE remaining un-folded reader — folds onto `SlicesDeclaredMethodsTrait` (convention, not instrument — F-1). | Rides r72 hb smalls bundle — LANDED. The bundle's `MultiAgentRefactorTest` tokenless-ids half measured CLOSED-BY-PRACTICE at base (ga `109293485` shape; file at `tests/Integration/` not `tests/Agents/`). Evidence: r71-rv-gi REVIEW, worklog ROUND 72. |
| **E694** | MED | **CLOSED ROUND-76 (lane ka `6b113f67e` → pick `35489b1d1`; r76-rv-ka APPROVE 0C/0M/2MINOR — step-1 grouped list/search LANDED, step-2 bulk-clear shipped-by-absence as the loud project-scope REFUSAL with the entry-present polarity KEPT per the rv-ka item-2 ruling, step-3 agent memory tool DECLINED into the E25-re-severity trigger watch) — MINTED ROUND-75 CLOSE (jb design /home/sites/crush-r61-artifacts/jb/design.md; per-id half LANDED via pick 90b7d1ae7)** — repo-local notes are invisible to /memory list/search and immune to /memory clear (delete/edit DO resolve repo-first via Chat::memoryLocate — that precedence is the law). Step 1 (ka, design-first): grouped list/search display naming the owning store per row; home-section wording byte-stable; MEMORY.md sentence + DocFigure arm in the SAME commit. Step 2 RULING 2026-09-13: bulk-clear NOT supported for project scope — per-id ops only, /memory clear stays home-store, any repo-store bulk routing must REFUSE LOUDLY; the --confirm --force variant is DECLINED (unconditional refusal). Step 3: agent memory tool GATED on E25 re-severity review + built-in-tool corpus census — do NOT build. Carries rv-jb MINOR-1 (memoryLocate array-shape tuple doc). | Lands r76 ka slice-A (owns src/Chat.php memory arms + Context + tests; hermeticity + ContainedPath inventories bind; see backlog §E694 + worklog ROUND 75). |
| **E695** | HIGH | **CLOSED ROUND-77 (lane la; masters `e2e427eca..f895f03c5`, picks `eb8318b91`+`4a76fdd40`+drift-fix `9ea12b968`)** — OAuth tokens registered by `/mcp add` never attach to any HTTP request: `getValidAuth()` zero production callers, `HttpMcpServer` static-config headers only, `McpAuthStore::hasCredentials` hard-coded true — an inert write-only store. | Acceptance: token attaches via `getValidAuth()` (refresh honored), `hasCredentials` real, docs/MCP.md auth truth-flip + DocFigure arms IN-STEP, `/mcp add` reporting truthful. Lane la r77. See backlog §E695. rv r77-rv-la APPROVE 0C/0M/2MINOR (MINOR-1/2 pinned `e7e25ca17`→pick `4a76fdd40`). |
| **E696** | HIGH | **CLOSED ROUND-78 (α half shipped — lane ma `c4d08c169`→pick `f64399c82`, wave-1 weld `41ad0bc93` floor 11,756; rv-r78 APPROVE 0C/0M)** — per-preset MCP narrowing enforced at grant-resolution in AgentManager (allowlist enforced; EMPTY allowlist = ALLOW-ALL, zero-blast for declaring-nothing presets); README:1072 + docs/MCP.md enforcement wording re-armed IN-STEP with DocFigure arm; β stays REJECTED (race on memoized singleton). | **RESIDUAL under CLOSED-notation (house rule): the DENY half stays minted-unwired — denyPatterns config producer still absent (McpRouter::setDenyPatterns zero settings callers); trigger-watch: re-arm if a producer is ever minted.** See backlog §E696. |
| **E697** | MED | **CLOSED ROUND-77 (lane lc `3eb4d60e5`→pick `dc5528bba`)** — palette action labeled "Toggle MCPs" only lists (`handleMcpAuthCommand('mcp auth list')` at Chat.php:12839); E689's trust-write DECLINE stands — the label does not. | Acceptance: truthful rename + EVERY derived pin in-step (grep `Toggle MCP` tree-wide: docs/COMMANDS.md, README rosters, DocFigure/ReadmeRoster/KeyBinding guard families). Lane lc r77. See backlog §E697. Label-only rename, every derived pin in-step (DocFigure arms the only lexical pins); E689 trust-write DECLINE byte-intact. |
| **E698** | MED | **CLOSED ROUND-78 (lane mb `b5c0eaee9`+`01a275887`+fix `4d1672b75` → picks `4854c86a3`+`15d57cc84`+`4eca19378`; rv APPROVE-WITH-FIX 0C/0M/2MINOR both fixed)** — non-launching liveness readout SHIPPED: `McpClient::startedSnapshot()` + Bootstrap memo-read panel suffixes; reports running clients with tool counts, NEVER launches, BYTE-STABLE when cold (cold-render negative pins make the byte-identity arm discriminating; MINOR-1 orphan-suppression polarity pinned). | docs/MCP.md liveness prose in-step (MINOR-2 α-line doc honesty). BootstrapMcpLivenessTest + McpClientStartedSnapshotTest new (durations +2 rows). See backlog §E698. |
| **E699** | MED | **CLOSED ROUND-80 (operator ruling "wire it" honored; lane oc `db93dbe16`+`a489db1da`+`54cc5afac`+`2f87b4f8b`+fix `8b7989af1` → picks `ad8dfda12`+`e438c9dca`+`f0ef430fc`+`b57ed8fec`+`4b9ab1258`; rv APPROVE-WITH-FIX 0C/1MAJOR+4MINOR all healed in fix-round)** — gated production door shipped: double opt-in (repo `type: claude-mcp` entry CANNOT name binary/args/env + operator-tier `claudeMcpBinary` absolute path); new `src/MCP/ClaudeCodeMcpServer.php` adapter routes the previously dormant spawner through the same bridge/dispatch/naming/narrowing law as its siblings; group-reap lifecycle; dormancy guard converted to a bidirectional allowlist pin with documented glued-split fidelity ceiling. | Acceptance met (backlog §E699 CLOSED paragraph). +33T exact, 3 new test files (durations +3). Digest-memo restore rode `b57ed8fec` (freeze-pin co-shard pollution caught in-lane). MCP.md four-types doc in-step (AX/E702 arms widened — no lane may re-pin those blind). |\n| **E700** | MED | **CLOSED ROUND-77 (lane lc `4fd247972`→pick `b7e6c2888`)** — 36 backlog headings still stamped [OPEN]/[PARTIAL] while their triage rows are CLOSED (E642/E643 bodies actively contradict their round-61 closure rows; E107/E165/E319/E547 are CLOSED-in-place per rows-stay-put). | Acceptance: flip all 36 headings; rewrite the four contradicting body paragraphs as closed-notation pointing at successor ids. Lane lc r77. See backlog §E700. rv enumerated all deletions (disclosed mid-lane wipe-restore, zero collateral). |
| **E701** | MED | **CLOSED ROUND-79 (lane na `a14f613ec`+`9d19bd2b5`+`44adf5fe0` → picks `8ed8ada1d`+`7b83a6454`+`1b8cc7ce8`)** — interactive OAuth auth-code + PKCE shipped: `sugarcrush mcp auth login <server>` = RFC 8414 discovery + RFC 7591 registration when needed + RFC 8252 loopback + RFC 7636 S256 exchange, state-checked / deadline-bounded (default 300 s) / Ctrl-C-cancellable, stores NOTHING on failure arms; persists a complete `AuthEntry` E695 attachment consumes + refreshes; in-chat `/mcp auth login` guidance-only (zero Chat.php edits); MCP.md prose armed IN-STEP (DocFigure BI/BJ/BK); no new composer dep. | Acceptance fully met (see backlog §E701 CLOSED paragraph for the §10 sentence). +53T exact, 7 new test files (design predicted ~25T/4 — honest deviation). rv r79-rv-na APPROVE-WITH-FIX 0C/0M/3MINOR all healed @`44adf5fe0`. Carried trigger-watch: `updateRegistration()` redirect-churn seam (nd §2.2). |
| **E702** | LOW | **CLOSED ROUND-78 (lane mb `9ce806a56` → pick `4ed2924a8`)** — docs/MCP.md carries the supported-transports table: stdio/http/git real, sse named-unimplemented with the honest throw line. | DocFigure arm in-step. See backlog §E702. |
| **E703** | LOW | **CLOSED ROUND-78 (lane mb `9246add03` → pick `1f9e6fec8`; α shipped, β REJECTED — decision recorded)** — config-DIGEST line + changed-since-launch line in the panel make a mid-session config edit LOUD instead of silently frozen. | β (reload/relaunch seam) REJECTED: relaunch under a still-valid root grant re-arms prompt-injection→proc_open; second-seam prohibition (E689 precedent). `mcp restart` stays out. See backlog §E703. |
| **E704** | HIGH | **CLOSED ROUND-81 (lane pa `291755f57` → pick `2452947ba`; rv APPROVE 0C/0M/1MINOR)** — bracketedPaste enabled + `Chat::update()` ingests `PasteMsg`: one draft per paste, single submit, sanitize default-ON, the pinned drop test flipped IN-STEP. +12T (PasteIngestTest). Carried watch: paste-while-modal behavioral pin (rv 1MINOR) — bracketed paste never enabled (`programOptions()` omits `bracketedPaste`, src/Chat.php:5414-5417; `CSI ?2004h` gated at candy-core Program.php:997-998) so pasted `\n` = per-line submits, AND `Chat::update()` drops `PasteMsg` at the non-KeyMsg early return (:1557-1559, deliberately pinned KeyHelpTest.php:459-475). Decoder + multi-row widget already ready. | Acceptance MET. | Notes: was Acceptance: multi-line paste → ONE draft containing all lines; Enter submits once; sanitize default-ON; pinned drop test flipped in-step; paste-without-final-Enter = draft unsent (ruling recorded). Lane pa r81 wave-1. See backlog §E704. |
| **E705** | MED | **CLOSED ROUND-81 (lane pe `c60b01005`+fix `f42ef1794` → picks `772ef1b03`+`2a33d175b`; rv APPROVE-WITH-FIX minor-only)** — Kitty negotiation pushed in `App::init()` (pop from bin after the loop — no model turn rides every exit door); Ctrl/Shift+Enter newline on capable terminals, fail-soft kept, modifyOtherKeys NEVER enabled. +9T. KEY-BINDINGS/roster guards in-step — modified Enter never distinguishable: the newline arm exists (src/Chat.php:1794-1795) and CSI-u decode exists (candy-core InputReader.php:322-329,479-539; pinned KeyHelpTest.php:689-720) but the Kitty keyboard protocol is never pushed anywhere in sugar-crush src/bin. | Acceptance MET. | Original acceptance: `Cmd::pushKittyKeyboard` negotiation + `KeyboardEnhancementsMsg` consume; Ctrl/Shift+Enter insert `\n` on capable terminals, fail-soft unchanged elsewhere; NEVER enable modifyOtherKeys alongside (`CSI 27;2;13u` misparse); KEY-BINDINGS doc + KeyBindingDrift roster in-step; Alt+Enter = portable newline (works today, InputReader.php:187-203). Lane pe r81 wave-2 (after pa). See backlog §E705. |
| **E706** | HIGH | **CLOSED ROUND-81 (lane pc `73638bcda` → pick `0bf64d08e`; rv APPROVE 0C/0M/0MINOR)** — settled 💭 drops the head-120 clip and rides the fitToPane WRAP (operator rule honored); live ticker keeps tail-120 + NEW honest `REASONING_LIVE_TRAILER`. Render-only. +1T — reasoning display hard-truncates at 120 chars (`REASONING_MAX_CHARS`, src/Renderer.php:482): settled head-120+`…` (:2808-2818, site :2817), live tail-120+`…` — indistinguishable ellipses; FULL text retained in state+persistence (Chat.php:3388/:6067; EngineBackend.php:889/:1464) — hidden, not lost. OPERATOR RULE ON RECORD: WRAP, never truncate. | Acceptance MET. | Original acceptance: settled renders full reasoning as dimmed WRAPPED rows (fitToPane path :2296-2330); live keeps bounded ticker WITH honest count-suffixed trailer (house pattern TruncatesOutput.php:276); ReasoningPaintTest :546-589/:618-647 + RendererTest.php:226-248 re-pinned honestly; geometry captures regenerated from FINAL tree pre-pinning (lane-fj law). Lane pc r81 wave-1. See backlog §E706. |
| **E707** | MED | **CLOSED ROUND-81 (lane pf `244c6b2a8` → pick `ab21cfd48`; rv APPROVE 0C/0M/3MINOR)** — length-stops parsed to `$truncated` across ALL chat providers, consumed end-to-end into a pinned transcript notice via root `Message::lengthStopped`; user-tier `max-output-tokens` key shipped. Honest scope: 5/6 wires honor the ceiling (ClaudeCode reads no request ceiling); TaskTool/Workflow keep defaults; flag not persisted across resume. +57T, DocFigure arm #83 — provider length-stops silent: main chat never sets `maxTokens` (src/Runtime.php:1185-1197 → hard 4096 defaults across OpenAI/Custom/Vertex/Bedrock/Sglang); `CompleteResponse::$truncated` set ONLY by Sglang (:1693,:891) and consumed by NOTHING (CompleteResponse.php:72-77 admits it). Second, independent half of the truncation symptom. | Acceptance MET. | Original acceptance: `finish_reason`/`stop_reason`=length/`MAX_TOKENS` parsed across OpenAI/Custom/Vertex/Bedrock/ClaudeCode → `$truncated` → transcript notice mirroring contextTruncatedMessage (Chat.php:14521-14530); new `max-output-tokens` settings key threaded into CompleteRequest (SETTINGS.md + tier roster in-step). Lane pf r81 wave-3 (after pe). See backlog §E707. |
| **E708** | HIGH | **CLOSED ROUND-81 (lane pb `18eacb4e1`+fix `1a7d67823` → picks `4b767f85c`+`56a71d134`; rv APPROVE-WITH-FIX 0C/1MAJOR+1MINOR, MAJOR healed)** — `startServer()` normalizes local/remote/command[]/environment/enabled with a loud skip-report; verbatim opencode block == translated block (parity pin). AX/E702 arms re-cut IN-STEP. +16T — MCP config shape rejects opencode spellings SILENTLY: engine is claude-shaped (`.mcp.json`-only, Bootstrap.php:5472); `command[]` → loud per-entry TypeError (McpClient.php:273-277,:310-318) but `environment`/`enabled` read NOWHERE — opencode env maps dropped + disabled servers start anyway (CRITICAL-silent class); `type: local/remote` loud-rejected (:357). | Acceptance MET. | Original acceptance: `startServer()` normalization local→stdio, remote→http, command[]→command+args, environment→env, enabled:false→skip WITH report; verbatim opencode block yields identical live servers; DocFigure AX (:3442 literal pin) + E702 transport arms re-cut in-step; StderrEmitterCensus measured. Lane pb r81 wave-1. See backlog §E708. |
| **E709** | MED | **CLOSED ROUND-81 (lane pd `02b59b1bb` → pick `fea4b3b26`; rv APPROVE 0C/0M/2MINOR)** — MCP.md "Adding servers" recipe + translation table + working snippet; empty-state names `<projectRoot>/.mcp.json`; panel guidance. +3T — /mcp discovery UX: the only visible verb is auth (McpAuthCommand.php:179-186 empty-state names only `mcp auth add`; the panel never names the declaration door; docs lack a "how to add a server" recipe) — manufactured the operator's "auth-only" impression. | Acceptance MET. | Original acceptance: docs/MCP.md "Adding servers" section with the opencode/claude translation table + the working 4-server snippet; `/mcp` empty-state sentence naming `<projectRoot>/.mcp.json`; panel footer or guidance line; DocFigure/E702/AX arms + ReadmeRoster in-step; README MCP bullet rides the drift guards. Lane pd r81 wave-2 (after pb). See backlog §E709. |
| **E710** | LOW | **CLOSED ROUND-81 (lane pg `f73112e7b` → pick `0c347e995`; rv APPROVE 0C/0M/2MINOR)** — `mcp import claude|opencode <path>` prints the translated block, NEVER writes; stream separation pinned through the real binary; Subcommands/Help/completion + both censuses in-step. +23T — no foreign MCP importer: `sugarcrush mcp import claude|opencode <path>` missing; nothing reads opencode's `mcp` block (the `/memory import` shape, Chat.php:11406-11445, is the precedent). | Acceptance MET. | Original acceptance: import PRINTS the translated `.mcp.json` (never writes un-confirmed); Subcommands roster rows (:876,:889) + Help.php:63 in-step. Lane pg r81 wave-3 (after pd). See backlog §E710. |
| **E711** | HIGH | **CLOSED ROUND-82 WAVE-1 (lane q1 `42a46a3e6` → pick `3336bea5f`; rv APPROVE 0C/0M/1MINOR; README fold `5c3bb3d1c`)** — sugar-prompt Spinner.php:135 is the ONLY real `pcntl_fork` outside crush; the parent loop :198 polls waitpid WNOHANG with signal handlers but NO wall-clock ceiling — wedged action spins forever; parent SIGKILL orphans the child; unrostered by check-child-lifetimes (fork, not proc_open). | r82 wave plan: W1 q1=E711∥q2=E712∥q3=E713∥q4=E715∥q5=E720∥q6=E719; W2 q7=E716∥q8=E718∥q9=E723; W3 q10=E717∥q11=E721∥q12=E722 (file-disjoint per wave; E716 FOUNDATION ordering before dependents spot-run). Lane q1 r82 wave-1 (sugar-prompt). Acceptance: bounded grace ladder TERM→KILL (BoundedReaper/escalate precedent), ceiling pin test, check-child-lifetimes rc=0. See backlog §E711. |
| **E712** | MED | **CLOSED ROUND-82 WAVE-1 (lane q2 `35430bfb2` → pick `05f155145`; rv APPROVE 0C/0M/0MINOR)** — candy-vcr `src/Cli/RecordCommand.php:430` `exit(128+$signo)` — genuine exit-in-src (borderline CLI-command-class); 46 @-suppressions (repo #2 density). | Lane q2 r82 wave-1 (candy-vcr). Acceptance: throw + CLI mapping, suppressions judged in-place (keep-with-why or remove), suite green. See backlog §E712. |
| **E713** | MED | **CLOSED ROUND-82 WAVE-1 (lane q3 `15cb07ec6` → pick `2fc01a77e`; rv APPROVE 0C/0M/1MINOR; latch docblock fold `5c3bb3d1c`)** — sugar-readline `Readline.php:172` unbounded 20ms busy-poll until input (TextPrompt.php:882 same family); interactive-by-design but no idle backoff/cancel ceiling. | Lane q3 r82 wave-1 (sugar-readline). Acceptance: bounded with documented ceiling + backoff, loop-residue hygiene test, suite green. See backlog §E713. |
| **E714** | LOW | **CLOSED ROUND-82 WAVE-3B (lane q13 `2960463a6` → pick `c66df93b9`; rv APPROVE 0C/0M)** — sugar-glow `FileWatcher.php:62` pure-poll `while(true){usleep}` — no inotify/evpoll offload; suppression density 1.4/file. | Lane q13 r82 wave-3b (sugar-glow).  Acceptance: inotify path where ext available + documented fallback + stopability; suite green. SHIPPED: capped idle backoff (1ms floor, ×2/sweep, cap = caller interval, ladder shift clamped) + snap-back on any change + documented lazy-generator poll contract via additive `?callable $idleSleeper` seam; inotify fast-path deferred per the brief's NO-new-deps ruling. sugar-glow 89T/142A exit0 (+18T). See backlog §E714. |
| **E715** | MED | **CLOSED ROUND-82 WAVE-1 (lane q4 `044dac54a` → pick `8df8c26d8`; rv APPROVE 0C/0M/1MINOR; docblock fold `5c3bb3d1c`)** — candy-input `src/Driver/SignalResizeDriver.php:107` `@shell_exec('tput '.$capability)` UNESCAPED — callers literal-only today (:91-92); the sole soft spot of the unescaped-CLI class (every other site escapeshellarg'd/argv-array — sweep-verified zero confirmed user-input interpolations). | Lane q4 r82 wave-1 (candy-input). Acceptance: capability validated against static allowlist before shell-out, pins both polarities (literal passes; injected-shaped refused). See backlog §E715. |
| **E716** | MED | **CLOSED ROUND-82 WAVE-2 (lane q7 `e49040de3` → pick `364c43b16`; rv APPROVE 0C/1M/3MINOR; MAJOR comment-truth fold `269101e30`)** — candy-core `WorkerPool.php:286` worker-script lifetime unaccounted (nowdoc exits :341/:392 child-side legit); 51 @-sites (repo max) to triage; ships AFTER its gate prove-outs, dependents spot-run after. NOTE: candy-core hosts DescriptorSinkArgumentCensusTest — any libc-fd sink added/removed REQUIRES roster rows there IN-STEP (repo law). | Lane q7 r82 wave-2 (candy-core). Acceptance: lifetime accounted, suppression verdicts inline, full candy-core suite + spot dependents (candy-sprinkles, sugar-bits). See backlog §E716. |
| **E717** | HIGH | **CLOSED ROUND-82 WAVE-3 (lane q10 `291b822e0` → pick `38a284716`; rv APPROVE 0C/0M/2MINOR)** — candy-pty pump family class-density #1: `PosixPump.php:114`, `PosixMasterPty.php:75/:387`, `ChildPollTrait.php:158`, `PosixProcess.php:161` — caller-bounded BY CONTRACT but the contract is unpinned; tests-side HangWatchdog exists, SRC-side nothing prevents an infinite pump if a caller forgets (AGENTS.md gotcha names it). | Lane q10 r82 wave-3 (candy-pty). Acceptance: pump-family audit + bounded wrappers or explicit deadline params + doc'd contract pinned by test; FULL candy-pty suite (HangWatchdog+SharedLoopResidue+LoopPin) + arm64 stty-fallback learnings honored. SHIPPED: `pumpDeadlineUs` + per-iteration exit-only deadlines + MultiPump resume (exercised) + documented+pinned caller-bounds contract; suite 663T/1862A/1W(base-carry)/17S exit0. Re-note: hand-typed 10-carrier list awaits a future reflection pin. See backlog §E717. |
| **E718** | MED | **CLOSED ROUND-82 WAVE-2 (lane q8 `700cbb2fb` → pick `57a270cb1`; rv APPROVE 0C/0M/3MINOR; MINOR-2/3 wording fold `269101e30`; MINOR-1 seam recorded in backlog §E718)** — admin pane runs SHOW GLOBAL STATUS / pg_stat_database SYNCHRONOUSLY in the TUI render path; down-arrow resets adminPage re-triggering; sync DB freezes the ReactPHP loop. Shell-family 21 hits cleared as PDO::exec SQL (false positive). | Lane q8 r82 wave-2 (candy-query; has vendor — verify link state first). Acceptance: 3s TTL cache on ServerContext::statusVariables() + subscriptions background-poll seam DOCUMENTED; full ReactMySQL rewrite explicitly OUT of scope; suite green. See backlog §E718. |
| **E719** | MED | **CLOSED ROUND-82 WAVE-1 (lane q6 `3ca9273ab` → pick `174a12447`; rv APPROVE 0C/0M/2MINOR; severity alignment + gate-figure folds `5c3bb3d1c`; index rows landed at weld)** — sugar-skate is the ONLY lib with no findings/sugar-skate.md + no plan (zero mentions in findings/ — grep-verified at mint); never received the 2026-06-30 P0/P1/P2 pass. Raw sweep hits benign (PDO SQL). | Lane q6 r82 wave-1 (sugar-skate). Acceptance: findings/sugar-skate.md + plan_sugar-skate.md in the 2026-06-30 house format (P0/P1/P2), index rows in-step, NO fixes. See backlog §E719. |
| **E720** | HIGH | **CLOSED-MARKS ROUND-82 (lane q5 `d57b34b5e` → pick `3d053be7b`; rv APPROVE 0C/0M/4MINOR; dash.md corruption weld-repair `d51bb2a6f`; 20 RESIDUAL candidates flow to closeout mint decision)** — candy-vt, candy-vcr, candy-mosaic, sugar-dash, sugar-wish, candy-shell were REWRITTEN by leftover-rollout PR#490–723 AFTER the 2026-06-30 audit — their findings/plan files are the stalest in the tree. | Lane q5 r82 wave-1 (docs + findings only). Acceptance: per finding CONFIRMED/FIXED-by-rewrite/OBSOLETE marked ✅/⏭️ IN PLACE (audit-walkthrough convention, never relocate), verdicts.md artifact; code edits only trivially-fixable confirmed-P0/P1, else mint residuals into triage. See backlog §E720. |
| **E721** | MED | **CLOSED ROUND-82 WAVE-3 (lane q11 `17c7bb29b` → pick `4436e7ebc`; rv APPROVE 0C/0M/3MINOR)** — candy-serve 6 string-form git pipelines (`src/Git/GitDaemon.php:741/:945`, `src/HttpSmartProtocol/Server.php:287/:347/:641` + UploadPack.php:157) — escaped ✓ but SERVER-LIFETIME children outlive handlers; no daemon-shutdown reap; TODO Server.php:652; stale findings/plan carry 28 open-F. | Lane q11 r82 wave-3 (candy-serve). Acceptance: shutdown-ladder proof/tests, roster any new child accounting (fail-closed tool), stale findings/plan marked per E720 conventions; suite green. SHIPPED: try/finally terminate-if-undrained at all six git spawns + ppid-keyed survivor census (zombies=failures); suite 354T/875A exit0. Commit-message precedence-bug claim ERRATUM-restated at backlog §E721 (real defect: overflow path lacked terminate; the claimed spelling never existed). Brief "4 roster sites" false (serve never had rows). See backlog §E721. |
| **E722** | MED | **CLOSED ROUND-82 WAVE-3 (lane q12 `d894850d3`+`e59435108` → picks `4f6637d1c`+`bd07426d2`; rv APPROVE 0C/0M/1MINOR; weld reword `61d7d7deb`)** — sugar-reel `FfmpegDecoder.php:322` pipe reads block while ffmpeg lives (E366 BoundedReaper present — VERIFY sufficiency); candy-mosaic `AsyncRenderer.php:13` fork-style design — anon-inode fd-census caveat (Linux ≥6.6: use fdinfo ids, NOT dev/ino) + r69 ShellContrastTest size-cache-leak family. | Lane q12 r82 wave-3 (sugar-reel + candy-mosaic). Acceptance: blocking-read paths bounded or documented with owner-reap pins; suites green. SHIPPED: reel pump-contract docblocks (judgment: docblock-over-API-break, alternatives refuted) + boundedness pins 361T/2473A/1S; mosaic mint premises re-derived FALSE (no fork-style renderer, no 64MiB referent) — interface carries the FOUR obligations incl. fdinfo-id census law; 465T/7783A/6S. See backlog §E722. |
| **E723** | LOW | **CLOSED ROUND-82 WAVE-2 — SUGAR-DASH-HALF (lane q9 `c1a914e64`+`09c58d6c5` → picks `2dfeaf3b4`/`87c3f5ab4`; mold half via wave-1 q6 `174a12447`)** — sugar-dash `src/Plugin/PluginSdk.php` SDK entry `run(): never`: loop :51, `exit(0)` :74 inside the SDK surface (ExternalModule E366 ladder landed ✓ — verify it covers this door); candy-mold is the ONLY lib of 52 lacking CALIBER_LEARNINGS.md (verified absent at mint). | Lane q9 r82 wave-2 (sugar-dash + candy-mold). Acceptance: SDK exit path throws+mapped (or never-returns contract pinned), mold learnings file per skeleton convention, dash suite green. See backlog §E723. |
| **E724** | HIGH | **CLOSED ROUND-82 WAVE-3B (lane q14 `955c04f85` → pick `37db286ba`; rv APPROVE 0C/0M/2MINOR; weld companion `395bfe60a`)** — candy-vcr `src/Hook/SanitizingHook.php:81-82` `preg_replace(...) ?? $value`: measured at mint (PHP 8.3.6) malformed patterns return NULL on string subjects (the in-code comment is truthful; the closeout brief's false-return premise REFUTED at mint) — so the `??` fail-soft SILENTLY KEEPS RAW VALUES: redaction disabled by a bad user pattern, the q2 filteredHostEnv class. | Lane q14 r82 wave-3b (candy-vcr; file-disjoint — q15's rebinds in 4 CellGrid-consuming test files rode disjoint hunks).  Acceptance: probe-then-preg or checked-`=== null` throw mirroring the q2 fix, truthful comment, pins both polarities, candy-vcr suite green (768T baseline + delta). SHIPPED: ctor door-probe throws InvalidArgumentException on invalid PCRE (NULL-not-false truth stands; int-key refusal PROVEN complete); apply-site `??` kept as documented runtime-limit path; companion truth-flipped the `@`-is-the-handler comment (limit exhaustion SILENT on PHP 8.3.6). candy-vcr 775T/3531A/14S exit0 (+7T). Future-option (NOT minted): lib phpunit.xml lacks failOnWarning — @-hygiene unpinnable suite-wide. See backlog §E724. |
| **E725** | HIGH | **CLOSED ROUND-82 WAVE-3B (lane q15 `2b12ead28` → pick `b797b6d39`; rv APPROVE 0C/0M/2MINOR — lane session died after five blank passes, work landed pre-report, reviewer reconstructed the REPORT)** — candy-vt four sites: `src/CellGrid.php:66-79` `set(): self` returns `$this` after in-place mutation; `src/Terminal/Terminal.php:120-125` resize() ignores the saved alt buffer (`ScreenHandler.php:63 ?Buffer $savedBuffer`); `src/Handler/ModeHandler.php:71-75` cursor-visible dual write; `src/Terminal/Terminal.php:128-132` __clone parse-state undocumented. | ORCHESTRATOR RULING 2026-09-15 on CellGrid::set: change the contract to **void** — mutate-in-place honestly; the grid is a hot workspace like Buffer; chaining was never intended; census any in-tree chain before flipping (mint census: all ~15 CsiHandlerImpl sites statement-form, zero chained; sibling `resize(): self` :86 same shape — judge in-step). Lane q15 r82 wave-3b (candy-vt).  Acceptance: four fixes + pins; vt suite green (baseline ~514T — re-measure at lane base); NO public signatures broken except the ruled CellGrid::set return. SHIPPED: `CellGrid::set(): void` mutate-in-place per the quoted orchestrator ruling (zero production chains; 8 candy-vcr test rebinds + README collapsed in-step; reflection shape pin; sibling `resize()` judged HONEST — it constructs and returns a new instance); `resizeBuffers()` resizes the saved alt buffer (DEC 1049/1047); cursor-visible single write-path — the invariant test caught a REAL pre-lane leaveAltScreen Mode-mirror divergence and fixed it; `__clone` parse-state documented. candy-vt 607T/2388A exit0; sugar-crush zero files touched. See backlog §E725. |
| **E726** | MED | **CLOSED ROUND-82 WAVE-3B (lane q16 `8ce0df6ce` → pick `fb9f813ac`; rv APPROVE 0C/0M)** — sugar-dash small-defect pair + two mint-time errata: `src/Module/LegacyModuleAdapter.php:62-65` limitation IS documented since `50678958a` (2026-07-01) — what remains is the UNPINNED always-advance contract + self-mutating `[$this, null]`; `src/Plugin/PluginSdk.php:70` fgets loop (line moved :52→:70 by the E723 fold) blank lines ALREADY skipped (:73-77) — live half is the absent signal posture (caller-side per E713 precedent — document or filter); D-5 WeatherModule mkdir claim FALSE at tip (both saves ride candy-core AtomicJsonFile :111-114 race-safe mkdir — ✅ re-mark, no code); `src/Plot/Chart/Gauge.php:46/:82/:235` triple double-clamp. | Lane q16 r82 wave-3b (sugar-dash; file-disjoint).  Acceptance: honest docblock-or-fix per item + pins; dash suite green (5949T baseline; determinism law: never single-shot proc_get_status after pipe EOF — poll 10ms ticks, r82-rv-q9 MAJOR). SHIPPED with a FIX-vs-CONTRACT split — item (4) the only real FIX (Gauge triple re-clamp folded into ONE constructor-boundary clamp + structural census pin); items (1)/(2)/(3) contract PINS (adapter always-advance; blank-line filter child pin per the q9 determinism law; mkdir guard re-mark — lives in shared candy-core AtomicJsonFile, no call-site copy). Mint-time errata on the row held. sugar-dash 5955T/9794A/1S exit0 (+6T). Future-option (NOT minted): Meter.php:89/GaugeCircle.php:170 render clamps, same double-clamp family. See backlog §E726. |
| **E727** | MED | **CLOSED ROUND-82 WAVE-3B (lane q17 `22734813c` → pick `3d8c0d533`; rv APPROVE 0C/0M/1MINOR)** — candy-wish trio: `src/Middleware/Auth/KeyboardInteractive.php:160-165` readResponses BLOCKING fgets can hang a server worker on an unexpected auth flow; `src/Channel/DefaultChannelHandler.php:209` hardcoded `['/bin/bash','-l']` (brief's `src/Middleware/...` path moved in the wish rewrite — measured at tip) — honor SHELL/passwd shell with configured fallback; README silent on ext-ffi/ext-pcntl/ptmx (finding #44, ❗ STILL LIVE at findings/candy-wish.md:365). NOT in scope: AsyncMiddleware settle-in-handle / Context-cancellation async-core restructure — FLAGGED architectural, pre-1.0 deferral stands. | Lane q17 r82 wave-3b (candy-wish; file-disjoint).  Acceptance: three fixes + pins (hung child dies within documented ceiling, mutation-demonstrated); full candy-wish suite green (measure baseline at lane base). SHIPPED: (1) CONTRACT verdict (fresh PHP process per connection under ForceCommand; synchronous chain pre-loop; timeout would fabricate policy — E713 precedent) + the EOF AUTH HOLE found live and plugged (zero-response EOF-mid-exchange now rejected BEFORE the validator, pinned); (2) FIX resolveShell: ctor → getenv('SHELL') → documented /bin/sh FALLBACK_SHELL; (3) README Runtime-requirements block. candy-wish 199T/501A exit0 (+6T). Deferred FLAG (wish async-core restructure) stands. Future-option (NOT minted): SHELL= set-but-empty not directly pinned. See backlog §E727. |
| **E728** | HIGH | **CLOSED ROUND-82 WAVE-3B (lane q18 `051675b6e` → pick `fb7ec3ac5`; rv APPROVE 0C/0M/2MINOR; weld companion `3079dbea2`)** — the six vt parser-family findings #3/#7/#9/#12/#16/#31 were marked 🔀 MOVED by q5 because the de-fork `9952e3f5c` re-homed Parser/Transitions machinery onto candy-ansi — but `findings/candy-ansi.md` (440 lines) carries ZERO inheritance marks (verified at mint): the hand-off was never CONSUMED. | Lane q18 r82 wave-3b (candy-ansi; docs-only verdict lane).  Acceptance: re-verify all six against `candy-ansi/src` at tip + mark findings IN PLACE (✅/❗/⏭️ house convention) + verdicts.md artifact; no code unless trivially fixable (then seam note). ENV: candy-ansi is UNVENDORED (phase-4 sweep) — verify link state before any suite run (r76 lesson). SHIPPED: six marks landed in findings/candy-ansi.md §10 — ✅3 (vt#7 reset flushes OSC/DCS, vt#12 OSC 8+id implemented `c69d0aff4`, vt#16 pass-through documented) ⏭️3 (vt#3 lazy static, vt#9 $g consumed, vt#31 feature-idea) ❗0; the six vt 🔀 rows carry the "r82 q18:" disposition clauses (append-only). Companion healed the OSC-52 tracking-home cite (rv m1). Docs-only negative control: 2 files. See backlog §E728. |
| **E729** | MED | **CLOSED ROUND-83 WAVE-2 (ORCHESTRATOR ACCEPTS r8 design doc — unification DECLINED with reasons; lane r8 `0f820b265` → pick `4545142c7`)** — candy-vt keeps its dual cell-grid representations BY RULING: `src/Cell.php`+`src/Cell/` (Buffer family) alongside `src/CellGrid.php` and `src/Buffer/`; unification changes the public geometry API. | Architectural — design lane + orchestrator ruling BEFORE implementation. Re-verify at lane base (w4 merge touched vt). See backlog §E729. |
| **E730** | MED | **CLOSED ROUND-83 WAVE-2 (lane r6 `176a15725` → pick `40646bf86`; rv APPROVE 0C/0M/1MINOR)** — single-settle restructure SHIPPED: sole `PromiseAwait::settle` site (DispatchesMiddlewareStack trait, token-census-enforced), contract preserved (transport reject pins byte-unmodified), entry guard raises on done Context, zero new timers; closes the r82 q5 #25 CRITICAL flag; 30s ceiling pre-existing-kept. See backlog §E730. | Architectural — design lane first (cancellation ownership, settle semantics, subclass migration order). See backlog §E730. |
| **E731** | MED | **CLOSED ROUND-85 W2 (2026-09-17) (minted 2026-09-15 at `052025282`)** — sugar-dash findings D-2: 35 files in `src/Plot/Chart/`, no shared `AbstractChart` base (re-verified absent at tip); COMP-2: `SystemModule.php:157` Linux `/proc` reads unchanged. | Architectural + feature-policy; largest sibling suite (5955T). RULING paragraph + literal-twin dedup (ChartGridGeometry) LANDED via r7 `79abe8461` → pick `c1f618a04` at the wave-2 weld. ROUND-85 W1: per-family trait sweep LANDED (u1 `a6adc27f6`+`70d0a5e53` → picks `b8b702618`+`11992bf81` + companion fold `1f92f70c8` — five traits, 17 pins + getHeatChar divergence pin, render-dump byte-identity); COMP-2 (b) ADOPTED per orchestrator ruling and IMPLEMENTED (u6 `0eba02549` → pick `0f26082fe`: `ProcAvailability` probe-once memo + `'n/a'` sentinel render, dash 6006T/10076A/1S/0W; rv-u6 2 MINORs recorded not-minted: race-leg non-latch UNPINNED, readMemLoad clamp kernel-invariant). AbstractChart big-bang stays DECLINED (r83 ruling; trait-extraction path exhausted). Row CLOSED. See backlog §E731. |
| **E732** | MED | **CLOSED ROUND-83 WAVE-1 (lane r3 `5ea8bdc94` → pick `3acd2e76d`; rv APPROVE 0C/0M/2INFO)** — attr landed 1 line in house slot; baseline RECONCILED to 890/3793/14S at lane base (r82's 775/3531 predated `61b28175c`); flip surfaced ZERO warnings; gate proven live by @-pair at a NEW site. KEY CAVEAT: PHPUnit 10.5 failOnWarning is @-blind (Collector `wasSuppressed()`) — cannot police @-hygiene (267 sites). Weld vcr 890T/3793A/14S exit0. Tree-wide adoption minted as §E738. See backlog §E732. | Cheap hygiene but MEASURE-first lane; re-verify `SrcExitCensusTest` child-pin vs the r82-q9 determinism law while in file. See backlog §E732. |
| **E733** | MED | **CLOSED ROUND-83 WAVE-1 + COMPLETION (r1 pair `5e979e696` → pick `141d5e227`; ProgressRing completion r5 `e4f6e7e23` → pick `76218c66e`; both rv APPROVE 0C/0M/2MINOR) — 3/3 SITES, FAMILY CLOSED** — Meter+GaugeCircle+ProgressRing ctor-boundary folds shipped; behavior-identity verified at rv (all sites clamped the SAME ctor input); edge pins + source censuses landed; family census post both welds: all four ratio charts carry exactly one `min(1.0,`. Completion weld dash 5964T/9823A/1S exit0 (lib-local). See backlog §E733. | Exact lines verified at tip. One small lane per file; sibling sweep for a third render-clamp included — it landed as the completion. |
| **E734** | LOW | **CLOSED ROUND-83 WAVE-1 (lane r2 `c093c58bd` → pick `06cc8a7e9`; rv APPROVE 0C/0M/0MINOR)** — both env polarities pinned (`SHELL=''` → `/bin/sh` fallback argv-spy; set → honored), finally-scoped putenv restore, mutation complementarity proven (neuter-blank→2 reds / neuter-trim→1 red). wish 201/503 → weld 201T/503A exit0. See backlog §E734. | Tiny pin lane, both polarities, putenv save/restore. See backlog §E734. |
| **E735** | MED | **CLOSED ROUND-84 W1 (2026-09-16; six lanes t1–t6 all reviewed; picks `e0bff28cc`(t2 stash) `5499c50bd`(t3 spark) `676e597d7`(t4 glow) `bb2e28cfb`(t6 dash goldens) `35f327d5b`(t1 dash) `7a3280759`(t5 docs, figure-amended per rv)** — every actionable residual of the 19-step tail landed or verdicted: **03.09** already-shipped + 3 defect fixes (state-losing withers, stale example); **06.01** verify-only Done-line — corrected scope attribution `--filter Subscription` 14T/38A, class alone 12T/36A; **10.06** DiffHighlighter shipped; **10.16** C1 truth pass, 8 defects; **10.25** GlamourTheme JSON themes + pager auto-reload + width pins; goldens-carry CURED — 68d73f9ba downstream family fully swept, zero stale blues tree-wide. Remaining 13 phase-12 steps PAUSED-BY-USER-DECISION, never auto-resume (leftover/README.md:88-89,283 "pause indefinitely per user"); 03.13 NOT-ACTIONABLE (E731 literal-twin dedup landed r7). History: minted PHASE-5-KICKOFF at `052025282` — 19 of 169 `step-*.md` lacked a done mark; the "~110 undone" framing FALSE at tip. See backlog §E735. | Scoping pass EXECUTED at the r84 weld — DO steps landed across t1–t6, phase-12 DEFER-by-user-standing, 03.13 DROP-as-deduped. See backlog §E735. |
| **E736** | LOW | **OPEN PHASE-5-KICKOFF (minted 2026-09-15 at `052025282`)** — `docs/MATCHUPS.md` carries 8 🟡 partial + 2 🔴 stub ports (recounted at tip). Umbrella row for finish-or-decide, NOT 10 rows. ROUND-85 W1: candy-forms F1+F3 LANDED (u2 `ef54fafb0` → pick `23aea4386`, u3 `2fc82e0c5` → pick `be0b010d6`; Phases 1/3/4/7/8 done modulo 8.3-prereq-2.3; merged-tree suite 1851T/3035A/0W). ROUND-85 W2: candy-forms COMPLETED — F2 landed (u4 picks `c1cebb634`/`5461ded56`/`2aba19b05`; forms **1891T/3164A/0W**), E741 CLOSED; Phases 1–4/6–8 done, Phase 5 = product-scoping decision. SOLE ACTIONABLE SURVIVOR (census 3→1); next lib (sugar-bits candidate) requires MANDATORY per-lib re-derivation probe before cutting. STAYS OPEN. See backlog §E736. | Per-row verdict pass (COMPLETE/SCHEDULE/DECLINE-with-why); ports follow the full add-a-lib checklist; per-lib wave cadence (one library per wave). See backlog §E736. **ROUND-86 rulings (v6):** forms Phase-5 DECLINED wholesale (zero consumers; reopen on real demand); shine 7.1/7.3 DECLINED, 5.1 record-only; layout 1.2 / charts 3.1 / bits 4.3 declined (STOP-class x2, behavior ruling x1) — see backlog §E736 RULINGS paragraph. **ROUND-86 WAVE-1 WELD: shell CLOSED-subscope (12/12 rows, 334/694 weld-corrected); layout CLOSED-subscope (P5/P6 shipped; 1.2 ruled; Expression seam frozen); bits SUBSTANTIAL-CLOSE (residue shipped; rulings: 2.4 BUILD→r87 w2, 3.1 DECLINED, 4.3 ruled-at-lane); charts P2–P4 CLOSED (P5 verify→r87 w3; 3.1 DECLINED v6); vt RE-DERIVED (22/31 non-actionable, 9-row tail; r87 w1 cheap-lane cut endorsed by rv-v5 w/ 6.2 gating + 6.4 carve-out).** Row STAYS OPEN (r87 tail: vt 9 rows + bits 2.4 + charts P5). **ROUND-87 WAVE-1 WELD (vt tail + bits 2.4 + charts P5): w1 built 4.1/12.3/4.3/4.4/6.1/6.3/2.3, gated 6.2 no-build, 6.4 deferred-with-trigger (needs new public Buffer row-shift API), §E742 MINTED from rv-w1 seam; w2 CLOSED bits 2.4 with POLICY-G ellipsis-aware walk (MAJOR-1 fix history + P5 walk-form lesson); w3 dispositioned charts P5 4/4 (5.1 landed, 5.2-5.4 ruled-skip) + Sixel key hardening. ZERO crush files — floor exact-carry 4th round. ACTIONABLE = 2 by row census (E736 + E742). Row STAYS OPEN — remaining shape is ruling/feature, not defect.** **ROUND-89 WAVE-1 WELD: r88 phantoms paid — E742 CLOSED via y1/`9bc98d6b8`, E743 CLOSED via y2/`2fc904421..8058799b4`, forms cluster-A BUILT via y3 (12 picks, forms 2182/3,945 ×2, prompt 314A), shine 7.1+7.3 BUILT via y4 (4 picks, shine 573/1,075, r86 decline revoked). Sole remaining UNBUILT plan row: shine 7.2 emoji-shortcode map (plan_candy-shine.md:672 PENDING) — net-new feature; row stays OPEN naming that line.** |
| **E737** | MED | **CLOSED ROUND-83 WAVE-3 (lane s4 `12470eb32` → pick `757327bdd`; rv APPROVE 0C/0M)** — the orchestrator BEHAVIOR ruling shipped: `SUGARCRUSH_MCP_DISABLE` gate (MCP_DISABLE_ENV, '1'/'true'/'yes' i-case, MCP_ABSENT shape, zero new emitters) + shard-only command prefix in `scripts/parallel-tests.sh` + per-test `McpLaunchEnabledTrait` arming for the 37 launch-asserting tests; weld serial 12,027T/172,610A exit0, K=8 conservation +0 after the durations row landed, MCP family 833T/3714A/1S byte-identical. Residual (3) (`array_keys` bucket-snapshot, rv MINOR-1) is ruled OPTIONAL-NOT-MINTED — recorded, no row. History: **PARTIAL ROUND-83 WAVE-1 (test half CLOSED; lane r4 `8725ea5ec` → pick `5c815306c`; rv APPROVE 0C/0M/2MINOR)** — mechanism isolated (shard cwd = monorepo root → root `.mcp.json` funnels via `getcwd()` and seeds foreign pid-bucket keys; whole-pid asserts false-red in K=8 by design-shape mismatch). Key-scoped to own decision-path key + NEW cross-root isolation pin (non-vacuity mutation-proven); r4 carried its own IN-LANE sextet re-pin 12,009/172,461 — weld serial 12,009T/172,461A/0F/0E/1S exit0 EXACT. OPEN: operator-server LAUNCH under root `.mcp.json` (3 remedies in r4 REPORT, orchestrator ruling), `/.mcp.json` gitignored at wave-2 (r11 `16249b740` → pick `30760dae6`; the +32 pollution RE-ATTRIBUTED to cold/warm fixture rebuild, not `.mcp.json`); operator-server LAUNCH ruling + optional `array_keys` strengthening (MINOR-1) remain. See backlog §E737. | Reproduce deterministically in-bucket first, then per-test reset/isolation seam; gate = K=8 shardcheck green WITH a planted root `.mcp.json`. See backlog §E737. |
| **E738** | LOW | **CLOSED ROUND-83 WAVE-3 — 58/58 at the weld** — live recount `grep -l 'failOnWarning="true"' */phpunit.xml | wc -l` → 58 of 58 (census-by-command); the three residual flips landed via s2 `f45b52a21` + s3 `a04c947c5` + s1 `7d5b03f9d` with the learnings companion `99727c5f6`; per-tip pick-message census errata (each lane tip moved 55→56, not the claimed 56→57/58-of-58) recorded in backlog §E738. History: **OPEN PHASE-5-WAVE-1 (minted 2026-09-15 at the r83 wave-1 weld, pick-chain tip `141d5e227`)** — tree-wide `failOnWarning="true"` adoption: census at the weld = **5 WITH** (candy-vcr via r3 + sugar-charts/crush/dash/gallery) **/ 53 WITHOUT** of 58 `phpunit.xml` files — AGENTS skeleton mandate unmet repo-wide. r3's method is the template (measure-at-base → flip → measure-under-gate → trigger_error pair proof); caveat per r3: the attr is BLIND to `@`-suppressed warnings (Collector `wasSuppressed()` early-return) so it never surfaces @-hygiene. | Cheap measure-first lanes à la r3, batchable per lane; candy-vcr — the densest @ lib (267 sites) — surfaced ZERO. WAVE-1 TALLY at wave-2 weld: 55/58 adopted (r9 30-kept/3-reverted `59ec5a6e3`, r10 20/20 `d4e4df464`, prior 5; corrected tallies 11,277T/101,983A); residuals minted §E739/§E740; pty attr blocked on documented 1W base-carry. See backlog §E738. |
| **E739** | HIGH | **CLOSED ROUND-83 WAVE-3 (lane s1 `e86525c4a` → pick `7d5b03f9d`; rv APPROVE 0C/0M)** — both shebang arms re-delimited `~..~` with POSITIVE behavioral pins (dead feature made live), FileOutputWriter refuse-by-inspection door (no `@`), attr in the house slot; suite 332T/718A/3W → 349T/750A/0W exit0 (+17T/+32A). Deliberate un-minted NOTE: the direct-path name table carries no shell entries (zsh→text asymmetry) — orchestrator product call. History: **OPEN PHASE-5-WAVE-2 (minted 2026-09-15 at the r83 wave-2 weld, from r9/E738 revert evidence)** — candy-freeze `LanguageDetector.php:168/:181` shebang preg arms are DELIMITER-BROKEN (unescaped `#` closes the pattern, `!` parses as modifier) — both arms NEVER match; interpreter sniffing through them has never worked. + `FileOutputWriter.php:18` fopen-before-throw. | Fix patterns + POSITIVE behavioral pins + clean the fopen door + adopt failOnWarning attr (E738 → 56/58). See backlog §E739. |
| **E740** | MED | **CLOSED ROUND-83 WAVE-3 (lane s2 `056366f62` → pick `f45b52a21`; rv APPROVE 0C/0M)** — probe-before-open door + attr in the house slot; suite 173T/457A/1W → 174T/464A/0W exit0 (weld re-confirm 174/464); corrected census prose: this flip moved 55→56 (the pick message's "56→57" is recorded as an erratum under §E738). History: **OPEN PHASE-5-WAVE-2 (minted 2026-09-15 at the r83 wave-2 weld, from r9/E738 revert evidence)** — candy-metrics `src/Backend/PrometheusFileBackend.php:317` fopen tmp-path raises an unsuppressed E_WARNING before the correctly-checked `=== false` refusal (same checked-false family as E712's fixed door). | Door-probe/fix + attr adoption (E738 → 57/58); full suite green under the gate. See backlog §E740. |
| **E741** | MED | **CLOSED ROUND-85 W2 (2026-09-17; lane u4 `6029c8fb7` → pick `ac82cf822`: shared `CarriesNonCtorState` trait wired into 6 Field classes / 25 `new self(` sites, fail-closed roster, 17-pin carrier-neuter mutation proof per rv-u4; Confirm private-carrier coexistence fold-note recorded; façade trio UNMOVED). MINTED ROUND-85 W1 WELD (2026-09-17, from lane u2's disclosed seam; anchors re-verified at weld tip `be0b010d6`)** — candy-forms `Input` trait-drop family: `Input::withValidator` (candy-forms/src/Field/Input.php:325) + `Input::mutate()` (:636) rebuild via `new self(...)` and silently DROP the `HasHideFunc`/`HasDynamicLabels` closures (class uses both traits at :30-31; zero `carryNonCtorState`) — exactly the defect class u2 fixed in `Confirm` (data-loss: dynamic title/hide closures vanish across focus/blur/mutate paths; probe-confirmed user-visible). | Fix = u2's `carryNonCtorState` recipe (Confirm.php; mutation pin ConfirmTest:143); audit ALL `new self(` sites in Input.php; per-trait survival pins + carrier-drop mutation; forms green, façade trio unmoved. **Owner: lane u4 (F2)** — shares Input.php with Phase-2/6 wiring; fold there. See backlog §E741. |
| **E742** | MED | **CLOSED round-89 wave-1 (operator ruling m0519 correct-to-xterm; lane y1 `f7d35a60a` → pick `9bc98d6b8`; canonical 0..255 table, slot 196=0xff0000, ramp 216..231, pins re-blessed; render-dump baseline flips to `6f9dc048b4cbc2e321a18698e1069b7c` = 12 intended theme rows; vt 949/12773; consumers pty/vcr/crush-window green)** — candy-vt Theme cube palette mis-alignment: the union `[16 base]+cubePalette()` shadows cube slots 0..15; `palette[196]=0xff87d7` ≠ xterm `0xff0000`; grayscale slots 216..231 absent → rgb() fallback. Pre-existing at base `cfb26f8f2`; NOT auto-built (user-visible behavior change — remove/behavior-change STOP posture). | RULING required: correct-to-xterm (user-visible; goldens + ansi-index census ripple) vs document-fallback (record-only). Then implementation-or-record per ruling; vt + consumer set (pty 663/1862/17S, vcr 948/4082/14S, crush `Vt|Terminal|Buffer` 305/21034) re-green. See backlog §E742. |
| **E743** | LOW | **CLOSED round-89 wave-1 (lane y2 `8a408f088`/`c967b5ce7`/`ae2ed208f` → picks `2fc904421`/`e20ab706b`/`8058799b4`; single private `computeBarLayout()`, two-way capture proof 485,509 B / 4,424 configs, sole-site census pin; layout docblock truth-flip comment-only; bits 519/1,097)** — sugar-bits `Progress::view()` Line/Slim/Block branches carry ~80 lines of duplicated width/percent math (`src/Progress/Progress.php:210-319`); extraction to `computeBarLayout()` + byte-capture pins still unbuilt tree-wide (pickaxe-proved). | Build per plan row 3.1; capture-identical proof mandatory. See backlog §E743. |
| **E744** | MED | **OPEN (minted 2026-09-17 at the r88 weld from rv-x5 MINOR-1)** — sugar-crush cannot reach the shipped candy-forms clipboard/mouse features: Chat::delegateToInput drops widget Cmds, Ctrl+C intercepted pre-delegation, PasteMsg consumed at Chat.php:1586, MouseMsg never forwarded to ItemList zones, input box painted from Renderer not TextArea::view(). Host-side adoption seam, product-shaped. | Follow-up crush lane; key-routing precedence + OSC52 plumbing + mouse forwarding + view convergence. See backlog §E744. |
## `PARTIAL` — 41

| id | conf | evidence | note |
|---|---|---|---|
| **E25** | MED | **CLOSED ROUND-74 (lane if `da38ed361` → pick `72079870d` + companion `96c01bbab`; r74-rv-if APPROVE 0C/0M/3MINOR)** — piece-2 PROJECT-scope writer shipped: `src/Context/ProjectMemoryWriter.php` (repo-local `.sugar-crush/memory/`, `forRoot` never creates, POST-mkdir containment gate, oversized notes REFUSE at project where home accepts — disclosed in docs/MEMORY.md), `MemoryBlock::capture(?projectStore)` folds repo-local first, `Chat::memoryAdd()` project-first; seams → r75 jb (E694-mint-or-fold: project-scope list/search/delete/clear/edit; null-home-store fold; no agent memory tool). Prior state: fixed: src/Context/MemoryBlock.php:385 returns clip(PromptFence::escape($line)), pinned by tests/Context/MemoryBlockTest.php:525-540; not done: renderEntry() still does not mark an imported entry. **R68/ec: piece-1 VERIFIED already-landed** (renderEntry=clip(PromptFence::escape), pin MemoryBlockTest:513); piece-2 deferred — PROJECT-scope writer = design item (r69+ carry) | sub-claim (2) is a decision that only matters once a PROJECT-scope importer exists; ForeignMemoryImporter still writes Local scope; entry's old Evidence sentence stayed stale until this stamp |
| **E45** | MED | **CLOSED ROUND-67 wave-2 (dh → `b08a96cf1`, lane `0575d4080`)** — (a) belt-braces comment shipped at the mcpConfigDecision trust arm, recorded load-bearing BEYOND reachability (strict_types: deleting the arm = TypeError on tools(); raw-$root fallback weakens the premise); (b) was already CLOSED. Prior row: (a) OPEN: the `$canonicalRoot === false \|\|` arm survives at src/Cli/Bootstrap.php:5004 with no comment naming it deliberate belt-and-braces; (b) CLOSED: $mcpClients docblock at :795 is now scoped to "clients whose servers are running" and mcpClient():5210-5214 explains why refusals are not memoized | CITATION DRIFT: the guard moved from mcpClient() to mcpConfigDecision(); its unreachability premise is also weaker now, since $path falls back to raw $root when realpath fails |
| **E54** | HIGH | **CLOSED ROUND-69 (lane fj `80dff1595`→pick `c3605f9e6`)** — the clip half shipped: `renderAgentView` cuts header+strip to cols via `clipRowsToCols` (renamed+generalized from `clipOverlayToCols`; verified call sites src/Renderer.php :1320-1322/:2057-2069) BEFORE `markPaneHeader`, so the cut never splits a `Mark::zone` sentinel (~27-cell overhead resolved in-lane); the `max(40,\u2026)` floor POLICY KEPT per the entry; geometry captures re-pinned — identity zone 60→80 (header natural width 61), NEW sweep test widest==cols across 24..130, `MovedTwiceE64ThenTheE54HeaderCut` + postE54Frame44 live-verified twice. | seam carried: `src/Tui/AgentStatusBar.php` renders on its own unbounded budget (outside fj ownership) |
| **E74** | MED | **CLOSED ROUND-67 wave-2 (dh → `b08a96cf1`) — REAL fix, not comment-only**: the refused answer now FREEZES — the catch caches [] under the resolved config path, so a repaired-mid-process config can no longer widen a grant the process refused (reproduced RED pre-fix; keystone folded into testACorruptUserConfigCostsTheProjectLayerAndNotTheRead, +0T/+3A; reviewer non-vacuity pin :494; the ??= documented as measured-unreachable-today insurance; property+method docblocks rewritten). Prior row: FIXED half: README.md:211 retraction + LayeredSettings.php:371 / Bootstrap.php:5919 / docs/SETTINGS.md:368 'eight' retracted. OPEN half: Bootstrap.php:2962-2972 still fills trustedSettingsRoots inside the try | Its own sub-finding ('freeze does not engage until permissionConfig() succeeds', FIX deferred) is unaddressed: negative outcome still uncached and the property docblock (Bootstrap.php:609-631) still lacks the parseable-config caveat |
| **E111** | MED | **CLOSED ROUND-67 wave-1 (lane dd → master `4d58dadd7`)** — new ClaimFamiliesDocumentationDriftTest 5T closes the remaining families (skills-twelve/preset-six/exit-tables-three/flags-synopsis two-directional, word-number pins, codes-only, --prompt exemption); PREMISE ERRATA: claims live in SKILLS.md/README/TROUBLESHOOTING, not SETTINGS.md (documented in class docblock). Prior evidence: env-roster family now pinned by tests/Config/EnvRosterDriftTest.php:11-90; no test reads docs/SETTINGS.md for the --flag list, exit-code table, twelve built-in skills or six agent presets | 1 of 5 claim families closed; the other four still have no oracle |
| **E125** | HIGH | **CLOSED ROUND-67 wave-1 (lane dd → master `4d58dadd7`)** — the fourth copy routed with E144; delegation proven by breaking the canonical (Chat oracle 2 reds). Prior evidence: all three named copies now delegate: tests/Config/{ConfigWriteProducerDocumentationDriftTest.php:263,ThemePersistenceFramingTest.php:73,GlobFigureDriftTest.php:173} return DocumentParagraphs::of() | a fourth independent splitter exists at tests/Chat/ChatConfigChangeDoorsDocumentationDriftTest.php:386 - not named by the entry [SUPERVISOR: CLOSED->PARTIAL; 4th splitter found by batch05; E144 open on same defect] |
| **E134** | MED | **CLOSED-BY-PRACTICE 2026-09-13 (supervisor disposition, post-r70 close)** — the law now has tree artifacts in RESUME APPENDIX V: (t) backup written BEFORE the mutation + restore VERIFIED by empty `git status --porcelain` + dirty-tree check before every merge; (u) revert technique `git show <sha>:<path> > <path>`, never `git checkout <sha> -- <path>`; and the round-38 dirty-tree prohibition (never `git checkout --`/`git restore`/`git stash` a lane to "clean up") which the trim had truncated/dropped — restored VERBATIM from `/home/sites/crush-r61-artifacts/resume-trim/BEFORE.md` line 3820 at this disposition, with the r64 lane-ab mutation-revert-destroy incident named as the third instance. Prior row: docs/plans/crush_code_worklog.md:11116-11121 mut.sh refuses a dirty tree + git clean -fdq + scratchpad copy | harness pattern adopted and named as the model, but "explicit in EVERY brief" is a per-round prompt with no artifact in the tree; worklog records it as E134s THIRD instance — the missing artifact landed 2026-09-13 |
| **E154** | HIGH | **CLOSED ROUND-67 wave-2 (dh → `b08a96cf1`) — both remainder sites now accounted**: Bootstrap :5722 ALREADY-PREFIXED via MCP_PARTIAL_START_LOG_FORMAT since 3b0ba8fe6 (the census seam comment asserting otherwise contradicted its own MESSAGE_SHAPES roster for three weeks — erratum comment shipped, mutation-proven load-bearing: prefix-strip reddens StderrEmitterCensusTest exactly once); Chat detach site disposed by de's E154c (2da286745). Prior row: tests/Cli/StderrEmitterCensusTest.php:272-310 — roster now 23 sites / 12 files; WorktreeManager 4->0, Dsml 11->7, Minimax 7->3, CommandLoader 5->1, Sglang 3->1 routed or funnelled | entrys "38 sites" census is stale; **R67-W1:** db half CLOSED (`0e9ce9e05` — 21 `sugarcrush:` prefixes routed, census counts CALLS vs MESSAGE_SHAPES literals) + (c) site landed via de/E175 (`2da286745`); **remainder = one site at `src/Cli/Bootstrap.php:5722` → wave-2 dh — kept PARTIAL deliberately, NOT stamped CLOSED** |
| **E158** | HIGH | **CLOSED ROUND-69 (lane fe `b6c2c8283`→pick `41b19f5e5`)** — the AutoloadGuard class doc-block now names the raw-fwrite channel as ONE channel and forward-cites `StderrEmitterCensusTest::testTheDirectFwriteStderrRosterIsUnchanged()` + the inherited-census agreement method (verified 2 cross-refs at tip); the census regex that re-reads this prose keeps its exactly-1 match (fe's StderrEmitterCensus filter green). | seam: `tests/Cli/StderrEmitterCensusTest.php:55` still quotes the old 'is ELEVEN' phrase — stale, round-70 ga |
| **E172** | HIGH | **CLOSED ROUND-70 (lane gc `9de0e3fd6`→pick `d7be5733f`)** — the drain half shipped: `Bootstrap::chat()` feeds `CommandLoader::skippedFiles()` into the `$commandSkips` dedup set; `reportCommandSkips()` emits ONE aggregate transcript row via `COMMAND_SKIP_NOTICE_FORMAT` (count-only; paths behind `SUGARCRUSH_DEBUG_COMMANDS=1`, shared with the loader feeder); the dormancy pin flipped to a drain-pin (roster `['src/Cli/Bootstrap.php']`); keystone RoutingTest asserts the transcript+stderr pair, count 1, no paths; census trio IN-STEP (seam census 21→22 + ten prose sentences, StderrEmitterCensus Bootstrap 28→29, BootstrapLaunchFormatConstants 15→16); +1T. | M1 mutation: strip the `reportCommandSkips()` call → keystone red exactly 1 while the seam census stays green (counts seam calls, not the wrapper — correct discrimination) |
| **E174** | HIGH | **CLOSED ROUND-67 wave-1 (lane dc → master `18daf2d55`)** — depth-walk's three copies consolidated into new tests/Support/SplitsTopLevelArgumentsTrait (topLevelArguments+balancedClose, shared #[ default); ZERO-move census (215 stderr / 1706 lifetime sites). Prior evidence: flattened() consolidated to tests/Support/FlattensSourceProseTrait.php:70 and significantTokens() to tests/Support/DropsInsignificantTokensTrait.php; the depth walk is still duplicated: topLevelArguments() at tests/Cli/HeadlessPermissionPromptAttachmentTest.php:495, tests/Support/ChildStderrCaptureScanner.php:744, tests/Support/ChildLifetimeScanner.php:1114 | Two of the three helpers the Step named are consolidated; the depth walk now has three copies |
| **E176** | MED | **CLOSED-MEASURED-STAMP (zero code) ROUND-69 (lane fn `5d050411d`→pick `f2c2c2327`)** — sink consolidation REFUSED by measurement: `src/Cli/Bootstrap.php` carries 4 raw `fwrite(STDERR)` sites (incl. warnPermissionConfig + reportPrunedSessions — forbidden ownership) whose census flips live in ff's StderrEmitterCensusTest; the NonInteractive sites carry TWO INTENTIONAL in-file stamps (E78c :390-399/:799-807 DECISION-STAMPED round-62 `0373308a3`; noticeRefusal DECISION-REJECTED settable-stream :1016-1029). The brief's `tests/Cli/StdinReaderCensusTest.php` is a PHANTOM — the real file is `tests/StdinConstantReaderCensusTest.php` (fd's, `3555a3940`). Second half already landed: ChildStderrCaptureTest 'Cli/' OUT_OF_SCOPE row (verified :203, minOffenders 3). |  |
| **E228** | MED | **CLOSED ROUND-69 (ff, same SHAs)** — the sweep half SHIPPED: `testEveryZeroShapedFixtureInTheCensusFamilyCarriesAKnownPositiveArm` (verified :1603) live-walks the 12-file Census|Roster family and polices every assertSame/assertEquals/assertCount ZERO whose subject is a census instrument — measured 31 sites / 14 method×instrument pairs / 1 unpaired, and the unpaired one was FIXED (a `census('prefixed')` presence arm), not licensed; `ACCEPTED_ZERO_FIXTURES` ships EMPTY with a staleness-delete arm; anti-blindness floors family>=12 / policed>=31 (dc lower-bound semantics). Mutations M1a/b/c red-as-intended. | lexer laws recorded in-file: a return-type colon lexes as STRING ':' (T_NULLABLE undefined); static:: is T_STATIC while self:: is T_STRING — scope-word walks must carry both halves |
| **E235** | HIGH | **CLOSED ROUND-67 wave-1 (lane dc → master `18daf2d55`)** — OUT_OF_SCOPE rows became {minOffenders, reason} extent data (mins 1,1,3,10,2,57,1,1,1,5,6,2), lower-bound semantics: growth silent, shrink reds, zero keeps move-to-SCOPE message; stale numeral prose de-numeraled. Prior evidence: tests/Support/ChildStderrCaptureTest.php:142 SCOPE now includes 'Providers/'; OUT_OF_SCOPE at :175 still carries 12 of the 13 prefixes | One of thirteen obligations discharged (Providers/ adopted). Remaining rows: Cli/ Commands/ Config/ Context/ Diagnostics/ Hooks/ Renderer/ Sessions/ Tools/ Workflows/ BaseSystemPromptTest.php ChatTest.php |
| **E246** | MED | **CLOSED-VERDICT (refuted-plus-replaced) ROUND-69 (ff, same SHAs, +0 code)** — the row's doubt ('deliberate reversal or oversight... not recorded anywhere') is answered: `2ab990e99` + `463c74699` (SHAs verified) + the doc-block :51-95 record the decision, and ff's tip re-proofs are STRONGER than the row knew — mutating DENIAL_HOOK to 'Nope:' reddens FOUR arms including the derivation pin `testEveryDeprecatedRosterAliasCarriesTheTagAndStillDerivesFromTheEnum`; a case respell reddens exactly the spelled-out backing-values test while derived arms stay coherent by design. The kept tests are load-bearing, not leftovers. |  |
| **E257** | MED | **CLOSED ROUND-69 (lane fe `b6c2c8283`→pick `41b19f5e5`)** — cost stated DE-DIGITALIZED (verified at tip: `tests/Tools/BuiltInToolCorpusTest.php:19` 'WHAT ADDING A SOURCE FILE COSTS (E257, round 69...)' + the crushSourceFiles() provider pointer): one added src/ file moves the suite total by +1 beyond the lane's own test methods — class doc-block, instantiation sweep, and provider all say it without a rotting numeral. |  |
| **E267** | MED | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`)** — the is_file()/directory arm landed with the readOrFail wiring. Prior evidence: tests/Cli/StderrEmitterCensusTest.php has zero file_get_contents left - all 10 reads go through readOrFail() (tests/Support/RefusesAnUnreadableSourceTrait.php:76) with an assertIsString arm and a fixture at :94 | the is_file()/directory arm the Step also asked for is NOT there - a directory roster entry still reads as empty string and scans as zero |
| **E270** | MED | **CLOSED ROUND-67 wave-1 (lane db → master `0e9ce9e05`)** — general guard/inventory for assertion-message-only helpers landed. Prior evidence: the named instance is fixed - tests/Cli/StderrEmitterCensusTest.php:2385 testTheRosterFailureMessageNamesEveryFileThatMovedAndBothCounts; no general guard/inventory for assertion-message-only helpers (grep across tests/ finds none) | only the general Step remains |
| **E272** | MED | **CLOSED ROUND-67 wave-2 (di -> `a94628c5b`, re-cut lane `9df5ac6e3`)** — ALREADY-LANDED with discriminators: the guard IS the ask (E232's general shape); mutation pair proves it live at base; doc-block-drift half rode the E481 two-polity work. Prior row: tests/Support/DuplicatedTestHelperDriftTest.php exists and anchors on T_FUNCTION (:1140) with synthetic fixtures - constraint 1 met; constraint 2 not: T_DOC_COMMENT is dropped at :2256/:2383/:2449 so divergent doc-blocks are not flagged separately | handover was taken up; the doc-block-drift half of it was not |
| **E283** | MED | **CLOSED ROUND-67 wave-1 (lane dc → master `18daf2d55`)** — the rot-proof-extent open question answered the E235 way (cluster rows carry minOffenders data). Prior evidence: KNOWN_GAPS now records the opener SET per row (tests/Support/InterpolationOpenerTokenTest.php:165-212) intersected with the live roster | Fixed half done; the entry’s open question (rot-proof extent for ChildStderrCaptureTest::OUT_OF_SCOPE rows describing clusters, e.g. Context/) is still unanswered — those rows still record no extent |
| **E291** | HIGH | **CLOSED ROUND-69 (lane fg `0dd5a61fa`→pick `bce05c166`)** — the census now walks src/ + tests/: scanner gained `functionKey()` (one copy — DuplicatedTestHelperDrift discipline honoured; verified via the convention test's cross-cite), never-helper tail RESOLUTION (a helper ending `exit()` reports bare-exit AT the caller — BackgroundSessionRunner::run; cycle/unresolvable fail CLOSED to unclassified), 'forkProcess' joined FORK_SPELLINGS + the return-wrapper rule makes a wrapper's CALLER the scanned site (AgentWorkerPool::startAgent); `ACCEPTED_BARE_EXIT` 5 rows licensed with verbatim-verified reasons (2 tests + the 2 src rows + WorkflowResumption sentinels split per-fn 99/98). | M5 mutation (::run→::runX) reddens BOTH arms naming the src site — the pin is alive (E229/E295 protocols honoured) |
| **E319** | HIGH | **CLOSED 2026-09-12 r68/ee `bf78d13d2`→pick `e0bbed2ab`** — stale trio healed (ForkedChildTest :182+:275, ChatTest:846): bootstrap.php fd0 truth = fclose + /dev/null replacement :535-541, stream_set_blocking still load-bearing via Tty(null); E655 keystone precondition stamped (TerminalSizeFallbackIsolationTest); NonBlockingVocabulary header de-numeraled + \b-justification re-measured mutation-proven; E319 causal renotes (candy-core `dd6edc699`, `descriptorForStream`). Review token-stream proof: exec-identical 4/5 files + 1 inert fixture string. Prior: **ROUND-67 wave-2 (dj → `00c8bc14b`) — the inversion is DEAD; entry STAYED PARTIAL.** All 8 inverted messages rewritten (measured 4/2/2, not 3x2 per file); E342/E358 CLOSED. Remaining -> round-68 ee (LANDED NOW): three stale same-comment-line claims ('bootstrap.php repairs descriptor 0 with exactly that flag', present tense, FALSE at HEAD) at ForkedChildTest:182 + :275 and ChatTest:846 (THREE sites, not two), the NonBlockingVocabularyTest docblock exemplar prose, and this header's causal claim ('that set flag IS the fd-0 repair' describes the superseded attempt-2). Prior row: still inverted: tests/Support/ForkedChildTest.php:203,230,292,315; tests/ChatTest.php:873,894; tests/Backend/EngineBackendTest.php:402,419 | Fixed since recording: SuiteChildStdinIsolationTest.php:133 and bootstrap.php:420-423 now state the flag sense correctly; ee healed the last three sites — row closed in place |
| **E322** | MED | **CLOSED ROUND-69 (lane fe `b6c2c8283`→pick `41b19f5e5`)** — TtyStream's own fix VERIFIED present (:603/:683 offender fixtures, already-landed shape, no edit); the GENERALISATION shipped where the real hole was: WiringTest's root-scrape was all-green, making its classifier branch invisible — closed with the `bareGetcwdSpellingFixtures` provider (verified :1077) + cross-exclusion test, +6 rows; mutation M2 (neutered pattern) → the real-tree scan goes GREEN 327T while the fixtures RED exactly 1 = exactly the discrimination the Step demanded. | tree-wide offender-fixture sweep beyond fe's files = fk/ga family; carried |
| **E331** | HIGH | **CLOSED ROUND-67 wave-2 (di → `a94628c5b`) — NO-OP-WITH-EVIDENCE**: tree carries exactly TWO significantTokens() declarations (trait + recorded Config outlier — declaration-DOWN probe); family pinned by the E356 arm; consolidation would regress the pinned divergence. Prior row: trait extracted at tests/Support/DropsInsignificantTokensTrait.php and used by 4 of 5; tests/Config/ReadmeJsonErrorContractDriftTest.php:192 still has a private significantTokens() | Fifth copy uses PhpToken::tokenize rather than token_get_all, so it is not routed through the trait. |
| **E347** | MED | **CLOSED ROUND-69 (fb `5ea48fe21`→pick `ff2a96306`; residual closed via the E375 RULING)** — string-compare claim re-verified live (the corrected assertion sits at `tests/Cli/RefusalStderrSurfaceTest.php` :244-region — row's :243 drifted 1; the two arms' observer lines compare BYTE-IDENTICAL, the same-length-luck weakness documented fixed in the comment); the residual 'fourth case vs qualifier' question was ADJUDICATED (B-QUALIFIER) and SHIPPED by fn — see E375. Owned-files pair green 21T/179A. |  |
| **E356** | MED | **CLOSED ROUND-67 wave-2 (di → `a94628c5b`)** — exact declaring pair + outlier shape PINNED (testTheSignificantTokensFamilyStaysCanonicalPlusTheRecordedOutlier; file-wide str_contains false-matches — the trait's prose NAMES PhpToken::tokenize). Prior row: tests/Support/DropsInsignificantTokensTrait.php exists, but tests/Config/ReadmeJsonErrorContractDriftTest.php:192 still declares its own significantTokens() and no ACCEPTED_DIVERGENCE row pins it | Entry's Step (port it, or record and pin the divergence) is unaddressed |
| **E358** | HIGH | **CLOSED ROUND-67 wave-2 (dj → `00c8bc14b`)** — residual half fixed; the three healed INVERTED_ROSTER rows deleted IN THE SAME COMMIT (guard-mandated data edit — 'a data edit here, not a weaker check'; +0T/-6A, tests untouched). E319 stays PARTIAL for surviving comments. Prior row: SuiteChildStdinIsolationTest half done (sentence rewritten at :181-186, no row for it in INVERTED_ROSTER/UNREADABLE_ROSTER, the latter now empty); the other 3 rostered files still carry 8 inverted sentences | The remaining half is exactly E342 |
| **E366** | MED | **CLOSED ROUND-67 wave-1 (lane df → masters `aa2497e07` dash / `5b6bb6b1b` reel / `1a4c39fc4` repo; lanes c3022c9ce/2bdf1d445)** — orphaning half landed where missing: dash ExternalModule terminateBounded grace/TERM/KILL ladder in __destruct (3T readiness-gated); reel per-package Support\\BoundedReaper (6T; also removed a latent SIGTERM-constant fatal without ext-pcntl); repo gate accounts both. Prior evidence: sugar-crush HIGHs hardened (ClaudeCodeMcpClient::disconnect uses ProcessReaper::terminateAndClose; LspConnection.php:902-930 SIGTERM/poll/SIGKILL) and tests/Support/DescriptorInheritanceGuardTest.php pins the shape; sugar-dash/src/Plugin/ExternalModule.php:230 is still a bare proc_close() with no proc_terminate() | Entry filed as FINDING ONLY; later rounds fixed the sugar-crush half, sibling-lib HIGHs (sugar-dash, sugar-reel AudioPlayer) unverified/unfixed |
| **E390** | HIGH |**CLOSED ROUND-71 (lane gf `2bc88da96`→pick `0c6820f39`)** — consolidation SHIPPED: NEW `tests/Support/RunsWallClockBoundedChildTrait.php` owns BOTH const pairs (`CHILD_WALL_CLOCK_BUDGET_SECONDS` 20×2 + `KILLED_BY_THE_BUDGET` 137×2 — second pair measured live; licensés 23→**21**, the documented deviation from predicted 22; no trio — bidirectional polity rosters only) + the SOLE `timeout -s KILL %d` wrapper (shared with the constants — gb same-file tripwire stays byte-true, no resolver widening); `ACCEPTED_CONST_DRIFT` row KEPT (trait 20 vs Readme 40 live); per-suite SIGKILL refusals stay per-file (claims differ); +0T/+5A; M1/M2 cp+md5 clean; review r71-rv-gf APPROVE; HOLD-FIX `52121fcac` (ACCEPTED_INHERITED_STDERR roster row, rule 33) RATIFIED — redirect unavoidably caller-side, census cannot follow params, row states the fail-fast upgrade path. PRIOR:  **PARTIAL — law-pin + TRIPWIRE SHIPPED ROUND-70 (lane gb `fdeddc1f3`→pick `a852eb2ba`)** — the same-file-literal-only law is now pinned: `ChildWallClockBudgetTest::testTheResolverRefusesCrossFileLiteralShapesToKeepTheSameFileLaw` (positive same-file control asserts the real resolution; cross-file `Other::B` refused with reason `cannot reduce`; same-file alias `const B = Other::B` refused with reason `not an integer literal` — refusals localize the boundary, arm non-vacuous by construction) reddens any future resolver widening UNLESS the licensé drop rides the same commit; +1T. The full one-motion unblock stays BLOCKED on a non-owned byte-identical pair — `tests/Cli/BootstrapSkillSkipsTest.php:95` vs `tests/Support/RequirementDirectiveProvenanceTest.php:103` both `private const CHILD_WALL_CLOCK_BUDGET_SECONDS = 20` (drop-probe RED names the pair at DuplicatedTestHelperDriftTest:2751 — evidence gb-probe-drop.log); 'both halves or neither' honored, the licensé stands. **Prior DEFERRED (dual-guard evidence) ROUND-69 (lane fk `3a14a057c`→pick `c8b8f5437`)**: consolidation blocked by resolveArgument's same-file-literal-only reduction + di's ACCEPTED_CONST_DUPLICATION/DRIFT licensés for the name pair. | Both halves shipped in one motion (tripwire + licensé drop same-commit) — the r70 block cleared at gf; wrapper pass-through welded by the hold-fix |
| **E481** | HIGH | **CLOSED ROUND-67 wave-2 (di → `a94628c5b`) — consts half SHIPPED** (test-methods half was landed): constReport() walks every class const into TWO POLITIES ([identical] duplication / within-DRIFT_BOUND drift), bidirectional data-derived rosters 23+14, fail-closed truncated/duplicate-name/unnamed, multi-name statements walked, heredoc values compared LINE-WISE (one-token values would fake a rewrite as a 1-edit drift). Unnamed-branch-unfixture'd noted LOW. Prior row: FIXED half: tests/Support/DuplicatedTestHelperDriftTest.php:944 driftReport(..., [T_PUBLIC]) test* scan (backlog E514 L16436). OPEN half: no T_CONST walk | duplicated CONSTS still unpoliced AND undocumented - EOF_EXIT_BOUND_SECONDS/EOF_EXITING_SERVER still verbatim in LspConnectionShutdownTest.php:66,571 and StdioMcpServerWriteBoundsTest.php:697,1001 |
| **E505** | MED | **CLOSED ROUND-69 (lane fg `0dd5a61fa`→pick `bce05c166`)** — the STEP's guard shipped: NEW `tests/Support/FixtureLifetimeCensusTest.php` (file verified; 3T/15A) keeps a bidirectional 8-row roster (LIFETIME/*_SECONDS consts, code sleeps, fixture-text sleeps incl. while-ceiling), threshold READ from phpunit.xml defaultTimeLimit=60 and pinned, value pinned, synthetic known-positive tree proving the holes that are pinned-invisible (arith rejected by number-termination rules, `usleep` via (?<!u), `<limit` shapes); the guard immediately caught fg's own measurement gap — the 8th site (WriteBounds STORM_PROBE_TEMPLATE `while<120`) the manual census missed (honest sentence carried in the roster); the three sleep(30) backstops take one-line E505 annotations. | CENSUS-SLEEPS-ON-ITSELF law recorded in-file (fixture digits must be built by concatenation-with-vars) |
| **E541** | MED | **CLOSED ROUND-69 (fb `5ea48fe21`→pick `ff2a96306`)** — the prescribed mechanical exclusion BUILT: `denialLiteralsOutsideThrownThrowableMessages()` (verified :823/:1107) drops OFF-ROSTER denial frames inside `throw new <Throwable>(...)` from the walk, the class resolved from the TOKEN STREAM (namespace + use imports w/ alias, T_NAME_*, final `class_exists` + `is_subclass_of(…, Throwable)`) — no file-naming list needed, exactly what the entry asked; thrown ROSTER prefixes are NEVER excluded (Chat::invokeTool's generic catch carries getMessage() into the isDeniedResult field = a forged refusal surface); dynamic/self/assignment/unresolvable forms fail CLOSED toward reds. New both-polarities test `testAThrownThrowableMessageIsExcludedFromTheWalkWithoutNamingItsFile` (11 asserts) + rule-15 paired pre-asserts; 4 mutation families red-as-intended. | gotcha recorded: mutating a regex CONSTANT by injecting '//' mid-assignment lands INSIDE the string literal → 4 ERRORS not FAILs — needles must terminate outside string tokens |
| **E547** | HIGH | **CLOSED 2026-09-12 r68/ea `c2a9b4089`→pick `e4b6cb931`** — StreamingCommandBackendTest:1030 resolved to `Backend\StreamingCommandBackend::pump()`. Prior: sugar-crush/tests/Backend/StreamingCommandBackendTest.php:1030 {@see pump()} still bare and unresolvable | the sibling list (selectedProviderLabel, __destruct, readResponse, forPaths, flattened/significantTokens) rode ea's E583 sweep — pump() closed here; bare-citation roster for UNOWNED files now lives in SymbolCitationDriftTest::DEFERRED_BARE_CITATIONS (r69 fi) |
| **E565** | MED | **CLOSED ROUND-67 wave-2 (di → `a94628c5b`)** — ALREADY-LANDED, verified at base with discriminators (implicit-public arm + polarity twins live; runOnScaledClock shapes visible). Prior row: sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:892-912 adds the implicit-public arm naming E565; but :483 still runs the main census with the default [\\T_PRIVATE] and the T_PUBLIC run at :486 is filtered to test*-prefixed names | A protected/public non-test helper named runOnScaledClock is still outside both walks |
| **E42** | HIGH | **CLOSED ROUND-66 closeout (measured at `61cde19c5`)** — (a) sanitize injective via `_5F` escape (master `0373308a3`; live at `McpToolBridge.php:145`) | (b) /mcp wire-name surfacing CLOSED via the chain E665 `fa4f40ed4` → E678 `crush mcp --json` rows `b76fee3cd` → E689 panel `6220e5b29`/`a6768adde`. BACKLOG HEADING RECONCILED: it stood `[OPEN]` while this ledger carried PARTIAL — both rows now CLOSED; `_5F` may appear in displayed names (documented cost). |
| **E653** | HIGH | **CLOSED (FULL) — ROUND-66 lane ce (master `5010c8bb2`, lane `0aebed27d`)** — Shape A (r65 bd) + Shape B `/notices`: `src/Commands/NoticesCommand.php` (pure `compose()`, line budget = SKELETON+entries arithmetic, forgery-tested; every section header prints its count even at 0), args-tolerant `Chat.php` dispatch arm + permissions-inline transcript handler with NO raw emitter (stays out of the derived-stdout census, no *Response twin), `Bootstrap::launchNoticesDropped()` public static (store is dedup-keyed by CLIPPED msg — the `array_values` trap caught by test), registry row, `NoticesCommandTest` 12T/53A, README roster + ARCHITECTURE dispatch-arms 22→23 in-step; all five r65 census files verified UNCHANGED (554T/8323A filter green) | Shape A's `chat()`-tail drain, stderr whole/once, ≤2-row greedy pair-pack, `PermissionWarningDrainTest` 6T/47A, FIVE-file census flip set — unchanged history. **ERRATA:** the ce REPORT's '+13th suite case = SlashDispatch provider row' attribution was FALSE (that home unattributed); the figure re-pin's measured +13T supersedes the attribution sentence. The r61 collector + r62 withdrawn-seam history is why Shape A has its shape. |
| **E440** | HIGH | **CLOSED ROUND-66 closeout (measured at `61cde19c5`)** — (a) seam `d0843c42c` + `96d87cdfb` (r63 DD); (b) BOTH mount halves landed: MCP dispatch entry `b76fee3cd` (E677, r64) + LSP dispatch entry `858614688`/`ce87e4e1d` (E690, r65) — between-turns stall self-clears at the next dispatch on both transports | The literal fd-2-on-loop shape stays a documented REFUSAL per E537 (r62 lane O measured loop-mount+select and declined); closed by the mounted-seam mechanism — do not re-open from the original STEP text. |
| **E214** | HIGH | **CLOSED ROUND-69 (lane ff `899d3f9c9`→pick `664c9cfa5`, CLOSED-VERDICT +0 code)** — (a) the anchored-literal question settled long ago at `68f7af8f4` (SHA verified; ALLOWED with a sentence-window). ERRATUM: round-67's refusal rejected `9f0c5db62` as the closure evidence — correctly (it is a +4-line E20 follow-through), but the row's true (a) home was always `68f7af8f4`; verdict right, citation wrong, healed now. (b) roster widening is a MEASURED REFUSAL (503-file probe) — that decision belongs to the E686 campaign, which proceeds independently of this row. |  |
| **E261** | HIGH | **CLOSED ROUND-66 closeout (measured at `61cde19c5`)** — (a) fork-failure counter + `forkFailureCount()` accessor shipped r63 lane HH (master `74dc2ab38`); (b) the teardown `error_log` shipped r66 lane cd (master `7f352c691`) with the StderrEmitterCensus trio IN-STEP + `AgentWorkerPoolTeardownForkTotalTest` 4T/15A | E687 (r66 CLOSED) was the blocking pairing — discharged; both halves now shipped. |
| **E633** | HIGH | **CLOSED ROUND-69 (lane fh `19eb66001`→pick `d7272bd0a`)** — the remaining half (guard the CONCLUSIONS, not the digits) closed via E564's dated-record recast + the absence/substring pins — no Chat/Bootstrap wiring needed, so the row is no longer PARTIAL. Slice-1 stays as recorded (`fa0067255`: three quantitative claims guarded + 12 judged, 3 fixed incl. one provably-false ~4.9x). Mutation proof per E633 doctrine (conclusions, not figures): M1 date-anchor removal, M2 cliff inversion, M3 trade-sentence rewrite, M4 retired figure reintroduced, M5 record-heading reverted — each EXACTLY red, cp-restore md5 9bbedf173… green after. | slice-2's 9 claims were minted E685 (CLOSED r66); stale-Bootstrap totals minted E684 (CLOSED r64) |
| **E43** | HIGH | **CLOSED-DECLINED (CONFIRM FULL DECLINE) ROUND-69 (lane fj `80dff1595`→pick `c3605f9e6`)** — eb's r68 measurement-decline CONFIRMED after re-scope: adoption means either a GLOBAL fence-budget flip against the `fitToPane` wrap-not-truncate doctrine (src/Renderer.php :2288-2315) or per-block Chat state + key-routing (out of ownership), and the palette renders no code blocks — no reachable half exists. Nothing shipped; the r66 'stays PARTIAL' judgement is superseded by a ruled decline. Prior recorded halves: clipOverlayToCols `f4b3407ec` + E48 floor (r62 lane R), candy-shine fence opt-in `5ba4e022f` (r63 EE), E682 paint-parity (r65). | declined by measurement twice (r68 eb, r69 fj); a per-block reflow toggle is a product decision before it is a patch |

## `STALE-CITATION` — 1

| id | conf | evidence | note |
|---|---|---|---|
| **E9** | MED | **CLOSED ROUND-69 (lane fh `19eb66001`→pick `d7272bd0a`)** — the README Tools bullet rewritten (verified at README.md :1051): 'Eleven classes ship on every launch, and Bootstrap::tools() ships all eleven; src/Tools/BuiltIn/ holds a TWELFTH file, TaskTool.php, whose runtime name `Task` joins the set only when the launcher threads an AgentManager into the build (the E675 feed) — every live chat() threads one' — the guard-required spelled literal 'Eleven classes' KEPT (ReadmeRosterDriftTest $spelled map requires it while the shipped-set derivation yields 11 under a null manager); hunk-local to the Tools bullet only. The tracker-#86/F-3 half is evidence-only: the 'agree by construction' claim was already repaired by the corpus-derived roster at `b35c0f2df` (SHA verified). | STALE-CITATION -> CLOSED; the worklog F-3 passage stands as historical record |

## `UNCERTAIN` — 1

| id | conf | evidence | note |
|---|---|---|---|
| **E36** | LOW | **CLOSED 2026-09-12 r68/ec** — BootstrapSkillSkipsTest ISOLATED green 2× (OK 30T/61A rc=0); earlier risky observation not reproducible. Prior: cannot judge without executing `vendor/bin/phpunit tests/Cli/BootstrapSkillSkipsTest.php` in isolation, which read-only triage forbids (ec ran it — gate log /home/sites/crush-r61-artifacts/ec/gate-e36-isolated.log) | file substantially rewritten since the entry (dffa38c0b, af8b979d2); all 10 tests now carry assertions and the risky/abort question is explicitly modelled in-file |

## `SUPERSEDED` — 7

| id | conf | evidence | note |
|---|---|---|---|
| **E80** | MED | tests/Integration/MultiAgentRefactorTest.php:661-694 refutes the lock-starvation mechanism (700 runs under 48 burners, attempts=0); backlog L6313 calls it 'the second link of E80s chain' and L6447 (E142) names the forked-child time-limit cause | Root cause was diagnosed by round 45 and the test half fixed there; E80 as written is overtaken - production half now tracked by the round-45 entries |
| **E218** | MED | Step is "Say in the brief that each lane writes under scratchpad/lane-<x>/" — a brief edit, no repo file | Duplicates E204 (backlog L7643) which states the identical step; brief is outside this repo so cannot be verified here [SUPERVISOR: NO-FIX->SUPERSEDED; duplicate of E204 which is OPEN] |
| **E396** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkArgumentCensusTest.php:142-170 both rows now DescriptorSinkScanner::VARIABLE; PosixBackend.php:107 and :634 use descriptorForStream() | entrys central claim is REFUTED - sites 2 and 4 were closed with NO constructor change, and the census at :1093 records they were the last INT_CAST_VIA_VARIABLE sites in the monorepo [SUPERVISOR: CLOSED->SUPERSEDED; central claim refuted by E429; sites closed with no ctor change] |
| **E434** | HIGH | E435 refutes both quantitative halves; wobble source fixed in 6757ff852 "make ResizeRaceTest's assertion count a property of its source (E434/E435)" |  |
| **E457** | HIGH | docs/plans/crush_code_hardening_backlog.md:16508 (E516) "grep -qv silently always answers no -- FALSE" | E516 re-measured E457 headline as wrong and audited the tree: zero offenders; real divergence is .gitignore scoping |
| **E482** | HIGH | docs/plans/crush_code_hardening_backlog.md:16354 (E511) "E482 premise was inverted ... TeamTest is the detector, not the offender" | TeamTest.php:76 still asserts exact equality (STEP not taken) but E511 fixed the real offender MultiAgentRefactorTest; do not re-schedule the subtractive-diff STEP without re-deriving |
| **E490** | MED | backlog L17373 (E550), L18227 (E593), L18958 (E623) own the E490 reproduction campaign; STEP 3 landed as candy-pty/tests/Support/HangWatchdog.php armed at tests/bootstrap.php:43 | hang still unreproduced and E623 explicitly declines to retire E490 - supervisor should track it under E593/E623, not here |

## `CLOSED` — 400

| id | conf | evidence | note |
|---|---|---|---|
| **E6** | HIGH | src/Renderer.php:4095-4108 derives $inner from $chat->cols() - INPUT_CHROME_COLS and wraps via wrapToPane(); tests/Renderer/InputWrapTest.php exists | fixed as "E455" in-code; wrap rather than the entry's explicit ->width() |
| **E7** | HIGH | src/Chat.php:8305-8347 runs the workflow in a \Fiber driven by driveWorkflowFiber() off the ReactPHP loop | fix taken differs from entry's Step: fork-plus-socket was measured and rejected; residual documented (a parent-blocking stage still holds the fiber) |
| **E11** | HIGH | scan of src/Tui/Components/MenuBar.php finds no consecutive-docblock pair; the handleKey-persistence docblock is now attached to activateMenu() at :475-482 and the dropdown one to renderDropdown() at :369 |  |
| **E13** | MED | tests/Cli/BootstrapPermissionGateTest.php:338-343 and :365-375 and tests/Cli/BootstrapUserConfigTest.php:86-91 skip on posix_geteuid()===0 with a named reason | unrelated residual: tests/Cli/BootstrapLaunchNoticeRoutingTest.php:216-232 chmods 0000 with no root guard and would fail as root |
| **E18** | HIGH | src/Context/ContextCompactor.php:254 truncateOversizedExchange() (docblock cites "backlog §12.2 E18"), wired at src/Chat.php:13690, pinned by tests/Context/ContextCompactorTest.php:1101+ |  |
| **E19** | MED | src/Providers/BedrockProvider.php:162/167 send formatMessages(withoutSystemMessages(...)) plus a top-level 'system' param built at :322-347 | the entry's optional second half (coalescing consecutive same-role entries behind default => 'user' at :299) was not added |
| **E21** | HIGH | src/Chat.php:9599 scheduleParkedCompaction(), :10173 applyModelCompaction(), :6859 dispatchTurn() all present as the entry's CLOSED paragraph describes | entry self-declares CLOSED and the tree agrees |
| **E22** | HIGH | src/Renderer.php:1124 fitToPane($body, $contentWidth) is the single choke point over the whole transcript; wrapping at :2288-2310; tests/Renderer/PaneWidthInvariantTest.php exists | composition moved from Chat::view() to Renderer::renderView(), so the entry's cited site no longer exists |
| **E23** | HIGH | src/Context/ContextCompactor.php:94-104 now says the harmlessness was MEASURED and cites the six E23 tests at tests/Context/ExchangeSummaryTest.php:337+ |  |
| **E24** | HIGH | src/Providers/ProviderInterface.php:42-52 states "STREAMED USAGE IS PER-DELTA, NOT CUMULATIVE" as a requirement; src/Runtime.php:1384-1394 documents the sum | guard deliberately not added, exactly as the entry's Step directs |
| **E31** | HIGH | src/Chat.php:9599 scheduleParkedCompaction(..., ?string &$capNotice) + tests/Chat/AutomaticCompactionModelSummaryTest.php:1572 | closed-with-variant-shape per d22e2e2c4; entry's compactNow shape measured UNSOUND and deliberately not used |
| **E32** | HIGH | src/Chat.php:9673-9681 arms a CancellationToken threaded into buildSummarizationRequest -> completeAsync (src/Chat.php:9449) | /compact's own probe still passes null at :9338, which is the asymmetry the entry's Blocked-on predicted |
| **E33** | HIGH | src/Chat.php:6919-6921 unconditional strip + conditional append; withoutContextReminders() at :13357; tests/Chat/ContextReminderDedupTest.php:75 asserts the QUANTITY | fixed by dedup, not the entry's preferred render-from-state shape; the rejection is argued at src/Chat.php:13326-13337 |
| **E34** | HIGH | src/Backend/StreamingCommandBackend.php:617-700 drives pipes off a periodic timer instead of wrapping blocking complete(); CommandBackend.php:271 same | the entry's `-p` aside still holds: src/Cli/NonInteractive.php:246 calls complete($history, null, ...) with no $onToken |
| **E35** | HIGH | src/Backend/StreamingCommandBackend.php:678 polls $cancellation?->isCancelled() inside the timer and abort()s the child |  |
| **E38** | HIGH | src/Context/ContextCompactor.php:1233 isRegeneratedReminderRow(), applied at :1270 and :1302 to decline the reminder rider |  |
| **E44** | HIGH | tests/Support/SuiteSkipRoster.php with tests/SuiteSkipRosterTest.php:205 (CI shape) and :240 (Packagist shape); an off-roster skip exits non-zero (:564) | the suite now asserts which wiring it ran under instead of leaving it to a skip count |
| **E46** | HIGH | candy-core/src/Util/Width.php:826-851 — compute() no longer prefers grapheme_str_split() and walks nextCluster(); class docblock :15 states one splitter for both paths |  |
| **E51** | HIGH | src/Chat.php:4705 refuseMouseDispatch(), called at :4483 and :4517; pinned by tests/MouseModalGuardTest.php | arm order mirrors update()'s keyboard order and the wheel-scroll decision is taken explicitly, per the entry's Step |
| **E52** | HIGH | candy-core/src/InputReader.php:439-441 rebuild now ORs alt/ctrl/shift; candy-core/tests/InputReaderTest.php:342 unshiftedModifiedKeyProvider and :373 testTheSameModifierParameterDivergesOnTheFinalByte |  |
| **E53** | HIGH | src/Tui/AgentViewPane.php:400-424 truncate() walks clusters() and measures each unit with visualWidth(); the per-codepoint charWidth() sum is gone from that path |  |
| **E55** | HIGH | src/Tools/BuiltIn/Grep.php:284-297 budgets the instruction section FIRST via instructionSectionCeiling(), inside maxOutputBytes; Read.php:288-293 given its own reserve as the Step required | Read's total is bounded at 1.375x $maxBytes by design (Read.php:277-280), which is stated rather than hidden |
| **E56** | HIGH | src/Tools/BuiltIn/Glob.php:412 instructionSectionCeiling() and :554 instructionSection() computed after the list is clipped — Grep's shape, as the Step asked |  |
| **E63** | HIGH | tests/ChatTest.php:71 recordReservations(true) / :112 strandedReservations(); the glob(sys_get_temp_dir().'/sc_chat_tool_*') window snapshot is gone | the entry's cross-lane supervisor protocol ("fails only on tearDownAfterClass" needs --filter disambiguation) is worth keeping until every lane carries the fix; also: never `rm -f /tmp/sc_chat_tool_*` |
| **E64** | HIGH | src/Tui/AgentViewPane.php:196 measures $rightWidth and :269 clamps the composed label to max(0, $width-$rightWidth-1); tests/Tui/AgentViewPaneGeometryTest.php:317 and :376 | the tab-vs-Width::string residual the fix uncovered is carried separately as E69, not here |
| **E65** | HIGH | src/Hooks/ScriptHook.php:312 MAX_ENV_ENTRY_BYTES and :744 stagePayloads(); tests/Hooks/ScriptHookTest.php carries the named oversize tests | entry's own round-40 stamp verified against the tree; CRUSH_TOOL_OUTPUT is covered too |
| **E66** | HIGH | sugar-crush/src/Skills/SkillPathNudge.php:107,155,377 (MAX_ENTRY_BYTES/MAX_ENTRIES/maxBytes) + Grep.php:480 cross-ref now says E66 | Residual it left open (Bootstrap-cap guard) became E78, itself closed |
| **E67** | HIGH | sugar-crush/src/Skills/SkillRegistry.php:204 $this->skills[$skill->name] = $skill |  |
| **E68** | HIGH | candy-core/src/Util/Width.php:843-855 graphemes() walks nextCluster(); regional-indicator pair rule at :871 | Deliberate residual (ESC-Fe + Extend, worst +1) still backstopped by Renderer::hardFit and pinned |
| **E69** | HIGH | candy-core/src/Util/Width.php:76 TAB_WIDTH=4, :879-883 graphemeWidth charges it, :390 wrapAnsi routed | Its bimodal residue was split out as E73, also closed |
| **E73** | HIGH | candy-core/src/Util/Width.php:129-140 compute() is a plain sum of graphemeWidth over graphemes(), no ZWJ state |  |
| **E75** | HIGH | sugar-crush/README.md:140-144 block quote retracting 'the deprecated name' |  |
| **E76** | HIGH | sugar-crush/src/Chat.php:5092-5140 rewritten docblock + tests/App/HostedFrameReadsThePaneTest.php:171 | Fix survived the prompt_plan Chat.php rewrite - verified by symbol, not line |
| **E78** | HIGH | tests/Integration/SkillPathScopingWiringTest.php:249 and :424 (ceiling + dead-band floor guards) |  |
| **E81** | HIGH | sugar-crush/src/Config/LayeredSettings.php:41-56 names both the palette row and /model, with the write traced |  |
| **E82** | HIGH | sugar-crush/src/Config/LayeredSettings.php:63-81 persistence framing, pinned by ThemePersistenceFramingTest | Its RULE-12 sub-note is also satisfied: PHP 8.3.6 stamps at docs/SETTINGS.md:340 and Bootstrap.php:5907 |
| **E83** | HIGH | tests/Config/ReadmeRosterDriftTest.php:144-664 pins tool roster, slash commands, subcommands, layer table, permission modes, chords and the launch-report sample | Prose deliberately left unpinned with the reason recorded in that file's class docblock |
| **E84** | HIGH | bin/sugarcrush:177-185 hand-rolls the JSON error document under --output-format json |  |
| **E85** | HIGH | sugar-crush/src/Skills/SkillRegistry.php:577-597 compiles a PCRE via PathGlob; the three str_replace rewrites survive only in legacyPathMatch() at :650-658 |  |
| **E86** | HIGH | sugar-crush/src/Cli/Bootstrap.php:5387-5391 routes the notice through warnPermissionConfigInTranscript() |  |
| **E87** | HIGH | sugar-crush/src/Skills/SkillPathNudge.php:126-154 records the decided resolution order + largestEntryCountWithin(); guarded by SkillPathScopingWiringTest | Entry's figure is wrong: raising MAX_ENTRIES to 20 does NOT red the guard - tree measures the tipping point at 27 |
| **E89** | HIGH | tests/Tui/RendererTest.php:287 testRenderWithADifferentPaneRendersThatPanesBodyAndNotJustItsCaption, with the old name retracted in the docblock at :257 |  |
| **E90** | HIGH | sugar-crush/docs/SKILLS.md:256-280 documents the leading ** change and names the three affected built-ins; pinned by tests/Skills/PathsGlobDocumentationTest.php |  |
| **E91** | HIGH | sugar-crush/src/Tools/Concerns/TruncatesOutput.php:85-92 cites SkillPathNudge::maxBytes() and smallestUnclippedCallerCap() |  |
| **E92** | MED | tests/Skills/SkillPathPatternTest.php:442 (POSIX class translated) and :642 (escaped-] left to legacyPathMatch) | The one remaining uncompilable shape is deliberate and documented at SkillRegistry.php:613-632; nothing to schedule unless someone finds a new uncompilable input first |
| **E94** | HIGH | sugar-crush/README.md:501-511 retraction + :322 exit-2 row naming error.type installation; pinned by ReadmeJsonErrorContractDriftTest:1435 |  |
| **E96** | HIGH | tests/Integration/ParallelToolCallsTest.php:1779-1782 strandedRuntimePayloads() now uses ToolIpcFiles::strandedReservations() (identity, not a /tmp window) |  |
| **E97** | HIGH | src/Cli/Bootstrap.php:488 const TRANSCRIPT_SEAM_CALL_SITES=20; all three cited sites are now PROSE_SITES rows in tests/Cli/BootstrapTranscriptSeamCallSiteCensusTest.php:245-280 | count is 20 now, not 16; entry's 'assert it' suggestion also implemented |
| **E98** | HIGH | README.md:400-401 schema union names installation/not-found/mcp-config; README.md:407-409 maps installation to exit 2 |  |
| **E99** | MED | src/Skills/SkillRegistry.php:95 MAX_COMPILED_PATTERNS=1024 with wipe at :590-591 | entry said 'no action today'; a cap has since landed so nothing is left to schedule |
| **E101** | MED | tests/Integration/SkillPathScopingWiringTest.php:519-522 discovery is ReflectionClass::hasProperty('skillNudge'); the intdiv regex only reads the budget afterwards | step ('widen discovery to the property') implemented; the residual it names (a spender without the property) persists by design |
| **E103** | HIGH | src/Cli/Bootstrap.php:5919-5930 three-part rewrite naming the glob; no 'one remaining site' sentence left in docs/SETTINGS.md; census asserted empty |  |
| **E104** | HIGH | src/Cli/Bootstrap.php:261-461 public const stderr/transcript formats; tests/Cli/BootstrapLaunchFormatConstantsTest.php exists; bootstrapMethodSource/soleMatch gone from tests/Config/ReadmeRosterDriftTest.php |  |
| **E106** | HIGH | src/Chat.php:6001-6008 enumerates all FOUR DOORS including /model, with retraction, pinned by tests/Chat/ChatConfigChangeDoorsDocumentationDriftTest.php |  |
| **E108** | HIGH | tests/Config/GlobFigureDriftTest.php:342 normaliseSeparators() folds U+2011/2013/2014 and :410 collapses whitespace; fixtures at :691-693 |  |
| **E112** | HIGH | bin/sugarcrush:263-273 last two sentences rewritten in three-part form, ends 'E112' |  |
| **E113** | HIGH | tests/Config/ReadmeJsonErrorContractDriftTest.php:164-178 walks all of src/ plus bin/sugarcrush, three shapes (:335 shape A onwards) | entry's own mid-body CLOSED note is corroborated by the tree |
| **E118** | HIGH | src/Cli/Bootstrap.php:488 public const TRANSCRIPT_SEAM_CALL_SITES = 20, asserted against the token scan at tests/Cli/BootstrapTranscriptSeamCallSiteCensusTest.php:422-432 |  |
| **E119** | HIGH | src/Cli/Bootstrap.php:3358 now reads 'nineteen other sources. THIS SAID ELEVEN'; PROSE_SITES row present; testTheKnownStaleSentenceOutsideThisLaneIsStillStale has 0 definitions |  |
| **E120** | MED | tests/Cli/StderrEmitterCensusTest.php:32-49 'WHY THIS FILE EXISTS (E120)' pins the src/+bin/ stderr emitter census by channel | runtime 62-line roster deliberately NOT pinned (declared a harness property); closed by a source-side census instead - supervisor may prefer PARTIAL |
| **E122** | HIGH | tests/Config/ConfigWriteProducerDocumentationDriftTest.php:406-415 scopes the search to the enumeration SPAN and records the round-45 mutation showing the retraction can no longer satisfy the rule | closed by span-scoping rather than the proposed isRetraction() port; method renamed to testTheEnumerationNamesBothDoorsOfEveryPersistedKey |
| **E123** | MED | tests/Config/EnvRosterDriftTest.php:778-794 mention census over README.md + docs/*.md with the PROMISE-vs-MENTION split argued at :708-712 and asserted at :936 | the design question the entry posed was answered exactly as it asked |
| **E131** | MED | sugar-crush/src/Config/LayeredSettings.php:396-405 — 66/68 survive only inside a WHAT-IT-SAID/WHAT-IS-TRUE-NOW correction | entry says fixed by "dropping the two cardinalities"; tree instead keeps them as a correction and ships a NEW cardinality (71/73 at 98d59bfb) that is itself re-stalable |
| **E132** | HIGH | tests/Config/ReadmeJsonErrorContractDriftTest.php:458-469 exitCodesAfter() is shape-agnostic; ternary fixture at :1165 |  |
| **E133** | HIGH | src/Runtime.php:1712 "Phase 1 — gate the whole group, and reserve every payload name"; tests/Integration/ParallelToolCallsTest.php:830-838 settle window gone |  |
| **E135** | MED | docs/plans/crush_code_RESUME.md:1599 "E135S FIX WORKED, TWICE — KEEP LANE-PREFIXED PROVISIONAL IDS"; also :1402, :1501 | residual named in the title (round-44 report-c prose citing lane-local numbers) is unrepairable: those lane reports are not in the repo |
| **E138** | HIGH | tests/Support/ForkedChildExitConventionTest.php (+ tests/Support/ForkedChildExitScanner.php) is the asked-for guard |  |
| **E141** | MED | tests/Integration/ParallelToolCallsTest.php:1707 clamps $seen = $peers at the barrier; ledger at :1595 and :1618 | fixed differently than the entry asked: peers<=callers is asserted (not peers==callers), and the overshoot is removed by the clamp instead; timeout path still reports the raw max, deliberately |
| **E142** | MED | tests/Support/ReapsForkedChildrenTrait.php implements the entrys "cheap 90%" pid ledger + tearDown SIGKILL | phpunit.xml deliberately untouched, which the trait doc-block states as the decision |
| **E147** | HIGH | tests/Agents/TeamManagerTest.php:52-88 sandbox HOME + "a Team test wrote into the real ~/.sugar-crush" assertion; TeamTest.php:54 and AgentManagerTest.php:71 do the same |  |
| **E152** | HIGH | tests/Config/ReadmeSettingsTierClaimTest.php:442-455 launchReportSample() renders from Bootstrap::PROJECT_TIER_TOOL_REMOVAL_FORMAT; no sprintf(disabled retyping left |  |
| **E153** | HIGH | tests/Cli/BootstrapToolAndPermissionSettingsTest.php:634-660 asserts the format body against real child stderr; :690-745 testAProjectThatRemovesEveryToolReportsTheNoSurvivorsBranch |  |
| **E155** | HIGH | src/Cli/HeadlessPermissionPrompt.php:77 "## Why none of the four go on the transcript seam (E155)" — the routing analysis is recorded per shape |  |
| **E160** | HIGH | tests/Support/FlattensSourceProseTrait.php:70 declares flattened(); used at tests/Cli/StderrEmitterCensusTest.php:234 and tests/Cli/BootstrapTranscriptSeamCallSiteCensusTest.php:181 | Consolidation into a tests/Support/ trait landed |
| **E161** | HIGH | tests/Cli/StderrEmitterCensusTest.php:543 const ARRAY_TOKEN_OPENERS = [T_ATTRIBUTE, T_CURLY_OPEN, T_DOLLAR_OPEN_CURLY_BRACES], documented at :534 | Opener set widened beyond what the entry asked for |
| **E162** | HIGH | tests/Cli/BootstrapLaunchFormatConstantsTest.php:1967-2013 excludes ->/::/function/new sprintf by preceding token and cites E162; fixture runs each shape | Also fixed the inverse gap (\sprintf as T_NAME_FULLY_QUALIFIED) |
| **E163** | HIGH | tests/Cli/BootstrapLaunchFormatConstantsTest.php:1978 return shape {calls, literal, interpolated, constant, other}; classifyFirstArgument() at :2135 returns 'interpolated' at :2146 | Three-way classification landed with a known-answer fixture |
| **E164** | HIGH | tests/Cli/BootstrapLaunchFormatConstantsTest.php:203-215 NAMED_FORMATS (9 promoted constants) + testTheLiteralFormatCensusHasAGenerator() at :655 | Walk completed; E186 is the write-up of the same walk |
| **E170** | HIGH | src/Diagnostics/RuntimeNoticeSink.php exists; src/Providers/ToolCallParser/DsmlToolCallParser.php:188-190 states the E170/E171 routing rule and :231 calls RuntimeNoticeSink::warn(); MinimaxXmlFallbackToolCallParser also references the sink | Closed by the entry's PREFERRED route (mid-session sink), not the readonly/degradations() fallback |
| **E171** | HIGH | src/Diagnostics/RuntimeNoticeSink.php + src/RuntimeNoticePumpMsg.php; all five named classes reference it (DsmlToolCallParser, MinimaxXmlFallbackToolCallParser, SglangProvider, Agents/AgentWorkerPool, Agents/WorktreeManager) | Mid-session notice seam landed with a Chat-side pump |
| **E173** | HIGH | src/Cli/NonInteractive.php:436-474 documents the optional `refusals` key citing E173; :555 adds it to the document; :911 format() takes $refusals | tests/Cli/NonInteractiveRefusalDocumentTest.php exists as the guard |
| **E177** | HIGH | tests/Agents/MailboxTest.php:421 ForkedChild::exitNow(0); no Agents/MailboxTest.php row remains in tests/Support/ForkedChildExitConventionTest.php:116-144 ACCEPTED_BARE_EXIT | Row deleted as the Step required |
| **E178** | HIGH | src/Workflows/WorkflowEngine.php:1949 ForkedChild::exitNow($signo === \SIGINT ? 130 : 143) on the forked-child branch; :1988 keeps a plain exit() for the installing process, argued at :1856 | Design refined vs the entry: the sentinels in WorkflowResumptionTest were deliberately KEPT and the ACCEPTED_BARE_EXIT reason rewritten (ForkedChildExitConventionTest.php:126-143) |
| **E179** | MED | tests/Support/ForkedChildReaperAdoptionTest.php:55 SCOPE = ['Agents/','Backend/','Diagnostics/','Integration/','Support/'] and :91 OUT_OF_SCOPE = [] | SCOPE is a five-prefix list rather than the '' the Step asked for, but OUT_OF_SCOPE is empty so no fork prefix is unadopted |
| **E180** | HIGH | tests/Agents/TaskListTest.php:848 and :863 both ForkedChild::exitNow(0); no Agents/TaskListTest.php row in ACCEPTED_BARE_EXIT |  |
| **E181** | HIGH | tests/Support/ForkedChildReaperAdoptionTest.php:114-126 UNTRACKED_FORKS_ALLOWED['Support/ReapsForkedChildrenTraitTest.php'] count 2 with both reasons verbatim |  |
| **E182** | HIGH | Limit 1: tests/Support/ChildStderrCaptureScanner.php:189-247 SHAPE_DISCARDED matches 2>/dev/null and variants. Limit 2: :109 uses TokenFunctionRanges::scan() to bound nearestAssignment(), documented at :261-268 | Both documented-but-unenforced limits are now enforced |
| **E185** | HIGH | tests/Cli/BootstrapLaunchFormatConstantsTest.php:155-168 states the render-from-constant claim and its pairing; the independent literal copy plus the behavioural case are at tests/Cli/BootstrapToolAndPermissionSettingsTest.php:708 and :743, re-measured at round 47 | The prescribed general form was applied to the constant that motivated it |
| **E187** | HIGH | tests/Cli/BootstrapLaunchFormatConstantsTest.php:985 testEveryDocPageQuotingAPromotedFormatIsOnTheReadersRoster() sweeps sweepableFormats() against PAGE_QUOTES | sweep implemented by whole-shape matching, not E187 own span+threshold algorithm (see E216) |
| **E192** | MED | src/Agents/WorktreeManager.php:263,338,544,663 on the seam; StderrEmitterCensusTest.php:374-392 RUNTIME_NOTICE_SITES carries the channel-6 bumps | AgentWorkerPool (src/Agents/AgentWorkerPool.php:1333,1384) and one SglangProvider site deliberately stay on error_log with the routing decision written into the doc-block - a decision, not an omission; WorktreeManager sites remain dormant in production |
| **E193** | HIGH | src/Chat.php:1181 "Arm the mid-session seam edge-driven wake-up (E193)" - init() returns runtimeNoticeWake() (src/Chat.php:1259) |  |
| **E196** | HIGH | tests/Support/FlattensSourceProseTrait.php:70, used at StderrEmitterCensusTest.php:234 and BootstrapTranscriptSeamCallSiteCensusTest.php:181 |  |
| **E198** | HIGH | tests/Tools/BuiltInToolCorpusTest.php:285 CENSUS_RESOLUTION "this file deliberately asserts NO cardinality over src/" plus RESTATEMENT_RESOLUTION guard on RepoMapBlock | the 290/240/309 literals are gone from both files |
| **E202** | HIGH | tests/Backend/EngineBackendReapTest.php:29 uses ReapsForkedChildrenTrait, :49 tearDown(), forkTracked() at 179/212/248/297; ForkedChildReaperAdoptionTest.php:55 SCOPE now lists Backend/ |  |
| **E203** | HIGH | tests/Support/ChildStderrCaptureScanner.php:388 classifySpec() returns SHAPE_UNCLASSIFIED unless fdTwoEntryIsAllLiteral($entry) |  |
| **E206** | HIGH | tests/Support/ChildStderrCaptureTest.php:142 SCOPE = [Agents/, Backend/, Chat/, Integration/, MCP/, Providers/, Support/] | Chat/ and MCP/ adopted, plus three more directories (re-confirmed r68/ec: ChildStderrCaptureTest SCOPE :83-89) |
| **E207** | HIGH | tests/Integration/MultiAgentRefactorTest.php:99-126 setUp() now redirects BOTH $_SERVER[HOME] and putenv(HOME), naming throwing-* as the leak it fixes |  |
| **E209** | MED | tests/Support/ForkedChildReaperAdoptionTest.php:90 OUT_OF_SCOPE = [] with Backend/ moved to SCOPE; known-positive replaced by assertTheOffenderWalkIsAlive() (:302) and the walk now consults UNTRACKED_FORKS_ALLOWED (:228) | both sub-facts the entry raised are addressed |
| **E210** | HIGH | src/Permissions/DenialKind.php:66-90 (Refused/Unanswered/Hook); src/Runtime.php:1969-1978 gate() decides the kind before settleAsk() flattens it; src/Cli/NonInteractive.php:1173 emits the kind field |  |
| **E211** | HIGH | src/Chat.php:5644 DENIED_ERROR_PREFIXES derives from DenialKind; producers render via DenialKind::reason() (src/Chat.php:2498,3533,3648; src/Runtime.php holds a case), pinned by tests/DenialPrefixRosterTest.php | closed by E239/E246, not by E211 own prescription |
| **E212** | HIGH | tests/bootstrap.php:342 NonInteractive::pinStdinDefault(fopen("php://memory","r+")); src/Cli/NonInteractive.php:791 readStdinIfPiped($stream = null) resolves through stdinDefault() | fixed with a static default pin rather than a run() parameter, but both halves of the Step are discharged |
| **E219** | HIGH | src/Cli/NonInteractive.php:241 self::noticeRefusal(...) in the refusal observer; comment at :220 "the gap is closed and it was NOT closed in Runtime" | Closed differently from the Step: the fix went to NonInteractive's observer, and Runtime's stderr silence is now a deliberate SAFETY property (see E244) |
| **E221** | HIGH | src/Agents/AgentWorkerPool.php:663 $this->warnForkFailed(); implemented at :1356 with pcntl_get_last_error/pcntl_strerror |  |
| **E222** | HIGH | src/Agents/WorktreeManager.php:356 `if (!$this->removeDirectory($worktreePath))` with "THE REGISTRY ENTRY STAYS"; removeDirectory() returns bool at :1120 |  |
| **E223** | HIGH | src/Chat.php init() doc-block (~L1205) states the wake-up is a Program feature and a host owns pumping; pinned by tests/Chat/HostedRuntimeNoticeWakeTest.php |  |
| **E224** | HIGH | tests/Support/FlattensSourceProseTrait.php:70 holds flattened(); used via `use FlattensSourceProseTrait;` at StderrEmitterCensusTest.php:234 and BootstrapTranscriptSeamCallSiteCensusTest.php:181 | Did not re-verify each consumer still keeps its own known-positive control (E125 half of the Step) |
| **E226** | HIGH | token_get_all sweep of the four cited files + Chat.php + bin/sugarcrush finds zero stacked T_DOC_COMMENT pairs; guard widened to src/+tests/+bin at tests/Diagnostics/RuntimeNoticeSinkDeliveryTest.php:1455 (phpSourceRoster) | Round 48 left this open; a later round (57 per the doc-block) closed it and widened the guard past what the Step asked |
| **E227** | HIGH | tests/Providers/SglangProviderReachabilityTest.php exists; live path is ProviderFactory::createSglang() at src/Providers/ProviderFactory.php:706 | Round 48 left this open; later round answered the reachability question the entry asked |
| **E232** | HIGH | tests/Backend/EngineBackendTest.php:428 isRaw() now carries the -F/-f + fd-2-to-file fix identical to tests/Support/ForkedChildTest.php:93; guard shipped as tests/Support/DuplicatedTestHelperDriftTest.php |  |
| **E233** | HIGH | tests/VhsTapeContractTest.php:4002 and :4829 both increment on \T_DOLLAR_OPEN_CURLY_BRACES; the KNOWN_GAPS row is gone (tests/Support/InterpolationOpenerTokenTest.php:133 "WHAT THIS SAID while it had one row") | Both halves of the Step landed together as the entry required |
| **E234** | HIGH | tests/Support/ChildStderrCaptureScanner.php:384 `return self::positionalShape($spec);` with positionalShape() at :477 | Entry self-declares "Fixed in the same round"; tree agrees |
| **E236** | HIGH | src/Chat.php:2508, :3533, :3648 all build reasons via DenialKind::Refused/Unanswered/Hook->reason(); src-wide scan test at tests/DenialPrefixRosterTest.php:818 |  |
| **E238** | HIGH | tests/RuntimeTest.php:551 hook message is now 'this tool is not allowed' and :564 asserts assertStringStartsWith(Runtime::DENIAL_HOOK . ' ', ...) | Did not verify the mirror test testAskWithNoApproverFailsClosedAndSaysPermissionWasRequired got the same treatment |
| **E239** | HIGH | src/Permissions/DenialKind.php exists as the neutral leaf; src/Chat.php:5644 DENIED_ERROR_PREFIXES is a projection of it and :5683 delegates to DenialKind::classify() | The Step's "the coupling test becomes unnecessary" tail is tracked by E246, not here |
| **E241** | HIGH | src/Sessions/BackgroundSessionRunner.php:318 $this->noticeRefusal($event); with the observer implemented at :455 |  |
| **E243** | HIGH | src/Cli/HeadlessPermissionPrompt.php:314 `$this->in = $in ?? NonInteractive::stdinDefault();` and tests/Cli/HeadlessPermissionPromptStdinDefaultTest.php exists | Entry already carries its own green CLOSED stamp; tree confirms it |
| **E245** | HIGH | tests/Cli/StderrEmitterCensusTest.php:1596 public function testTheInheritedCensusStillAgreesWithTheScan(): void — cardinality-free name as prescribed |  |
| **E247** | HIGH | tests/DenialPrefixRosterTest.php:918 testTheOneOffRosterShapeInSrcIsAThrowableMessageAndNotAFourthKind + filesNamingBoth() discriminator | trace done and pinned by a test rather than by a doc-block on the exception; src/Agents/TaskBlockedException.php doc-block itself unchanged |
| **E250** | HIGH | src/Permissions/ToolRefusal.php:52 carries public DenialKind $kind; src/Cli/NonInteractive.php:1173 adds "kind" => $refusal->kind->token() to the refusals JSON | kind is an ADDED key as the entry required; ToolFinished itself still carries only the ToolResult, which the entry allowed |
| **E251** | HIGH | tests/Cli/StderrEmitterCensusTest.php:1596 testTheInheritedCensusStillAgreesWithTheScan (cardinality-free); E245 is marked [CLOSED] at backlog L8638 | the rename this process entry was tracking has landed |
| **E252** | HIGH | tests/Tools/BuiltInToolCorpusTest.php:510-585 records the literals removed; grep for 290/309/240/26 finds no assertion; testRepoMapBlockNoLongerRestatesTheSourceCensus():889 | option (b) taken - totals dropped, per-file secondary map kept, RepoMapBlock restatement now forbidden rather than asserted |
| **E254** | HIGH | tests/DenialPrefixRosterTest.php:128 DENIAL_SHAPE is /(?<![A-Za-z])[A-Z].../ - no ^ anchor | FAMILY_SPELLINGS map has since been replaced by a whole-src walk (tests/DenialPrefixRosterTest.php:818) |
| **E255** | HIGH | tests/Cli/RefusalStderrSurfaceTest.php:305-316 approver attached unconditionally with the reason in a comment |  |
| **E256** | HIGH | tests/Cli/RefusalStderrSurfaceTest.php:200 testBothArmsDoubleAndTheseAreTheBytesTheyWrite derives both figures; doc-block copies retired |  |
| **E260** | HIGH | src/Agents/WorktreeManager.php:1128 !is_dir($path) \|\| is_link($path) and :1153 is_dir($itemPath) && !is_link($itemPath); tests/Agents/WorktreeRemovalReportingTest.php:300,335 | entry self-declares closed and the tree agrees |
| **E263** | HIGH | tests/Diagnostics/RuntimeNoticeSinkDeliveryTest.php:1173 stackedPairsIn($files, $root) + :1271 testARosterEntryTheScanCannotReadFailsRatherThanScoringZero with an is_file arm |  |
| **E265** | HIGH | tests/Support/ProcessUniqueTempNameTest.php:491 and :509 accept T_NAME_FULLY_QUALIFIED alongside T_STRING; :455 names the qualified arm in a failure message |  |
| **E266** | MED | tests/Support/ProcessUniqueTempNameTest.php:85 SCOPE = [tests, src, bin/sugarcrush] with the widening reason in the doc-block above it | the five src/Workflows/WorkflowEngine.php sites are still argument-less but are now inside SCOPE and carried by an argued inventory roster rather than being silent |
| **E273** | MED | tests/Cli/StderrEmitterCensusTest.php:2154 the numeral is read back out of the doc-block and compared with count(self::selfCountAnchors()); sibling BootstrapTranscriptSeamCallSiteCensusTest.php:320 elides its own count | the general Step (check any anchor list added later) is by nature ongoing and cannot be closed by inspection |
| **E274** | MED | tests/Cli/StderrEmitterCensusTest.php:1710 records the thirty-five correction; testEveryChannelInThisProviderHasARowADeadScanWouldRed() at :2046 replaces the total | the general Step (sweep other doc-blocks that justify a fixture by a total) has no artifact and is not verifiable |
| **E279** | MED | tests/Support/DuplicatedTestHelperDriftTest.php:1273 testRelaxingTheBoundToTwoTokensBringsInOnlyAReceiverSpelling() | Bound 2 now measured every run via isReceiverSpelling predicate; bound 3 and renamed-helper holes still unexplored |
| **E282** | HIGH | tests/Support/DuplicatedTestHelperDriftTest.php:1657-1715 pins the T_CURLY_OPEN dormancy and :1808 pins the deprecated opener behaviourally |  |
| **E285** | HIGH | tests/Support/DuplicatedTestHelperDriftTest.php:208 ACCEPTED_SIGNATURE_DIVERGENCE plus the signature channel of driftReport() (:250) | Signature divergence now a separate checked map rather than invisible |
| **E286** | HIGH | tests/VhsTapeContractTest.php:3681 assertSame(__FILE__, ...getFileName()) and :3734 declaredSlice(), with fixtures at :3620-3626 |  |
| **E287** | HIGH | tests/Support/DuplicatedTestHelperDriftTest.php:1194 testWideningTheVisibilityAlphabetToProtectedAddsNoHelperAtAll() | Alphabet is now a driftReport() parameter; public run also exercised (:486, :944) |
| **E288** | HIGH | tests/Support/ForkedChildReaperAdoptionTest.php:708 testNeitherMapClaimsADirectoryTheOtherAlreadyClaims() with overlappingClaims() both directions (:722,:730) |  |
| **E289** | HIGH | tests/Support/InterpolationOpenerTokenTest.php:1309 T_CASE neighbour and :1368 T_MATCH arm in comparesAgainstBrace(); synthetic switch walker fixture at :504-528 |  |
| **E290** | MED | tests/bootstrap.php:534-539 (!stream_isatty then fclose(STDIN) + /dev/null in $GLOBALS), pinned by tests/SuiteChildStdinIsolationTest.php | Suite half fixed (repair is now the descriptor replacement, not the flag — see E296 round-51 note). Production residual (unbounded NonInteractive::readStdinIfPiped) still live and deliberately untouched: src/Cli/NonInteractive.php:686 |
| **E292** | HIGH | src/Sessions/BackgroundSessionRunner.php:456 noticeRefusal() now calls ToolRefusal::fromEvent(); doc-block at :414 records both callers hoisted |  |
| **E293** | HIGH | tests/Support/ProcessUniqueTempNameTest.php:85 SCOPE = [tests, src, bin/sugarcrush]; WorkflowEngine rostered at :128 | src/Workflows/WorkflowEngine.php still has 5 flagless uniqid calls but they now carry an argued row; residual tracked as E324 |
| **E296** | HIGH | tests/bootstrap.php:534-539 option (a) shipped: fclose(\\STDIN) + fopen(/dev/null) parked in $GLOBALS[__sugarcrushSuiteStdin] | Entry’s own round-51 closure header verified against the tree |
| **E297** | HIGH | tests/bootstrap.php:119-122 now says 41/15/13 and "none of them eighteen"; no stray "eighteen test files" remains |  |
| **E298** | HIGH | tests/Hooks/AuditHookTest.php:135 testTheDefaultLogFileIsThePerUserProductionPathWithoutWritingToIt() asserts the CONSTRUCTED path; src/Hooks/BuiltIn/AuditHook.php:274 is uid-scoped | Default path further hardened by E328/E351; no write+unlink race remains |
| **E300** | HIGH | src/Permissions/DenialKind.php owns the roster; src/Sessions/BackgroundSessionRunner.php no longer reads Chat::DENIED_ERROR_PREFIXES (uses ToolRefusal::fromEvent at :456) | The merge-time repoint E300 asked for was done |
| **E301** | MED | tests/TtyStreamArgumentCensusTest.php exists with both spellings as fixtures (:471,:475,:479); no live `new Tty(null,` construction remains in src/ tests/ bin/ | Residual "Step: consider whether candy-core Tty should refuse null with an injected Termios" is unactioned — candy-core/src/Util/Tty.php:52 still does $stream ?? STDIN |
| **E302** | HIGH | candy-mosaic/src/Mosaic.php:166 now PaletteCapability::ITerm2; candy-mosaic/src/Detect.php:168,:205,:285 guard with is_resource(); candy-mosaic/tests/PaletteCapabilityReferenceTest.php present | Both listed defects fixed |
| **E304** | HIGH | src/Runtime.php:252,:263,:275 and src/Chat.php:5638 carry @deprecated; pinned by tests/DenialPrefixRosterTest.php:1070-1088 with a not-deprecated control | Both halves of the Step landed (tag + reflection pin) |
| **E305** | HIGH | tests/DenialPrefixRosterTest.php:322 testTheRostersBackingValuesAreTheThreePublishedPrefixes() | Entry’s self-declared round-49 closure verified in the tree |
| **E306** | HIGH | sugar-crush/src/Cli/NonInteractive.php:1075 refusalNotice() = "sugarcrush: [{$kind}] {$tool} was not run - {$reason}" | Gap 2 closed; gap 1 (text stdout silent on refusals) remains by design, not part of the Step. |
| **E307** | HIGH | sugar-crush/src/Sessions/BackgroundSessionRunner.php:461 logs REFUSAL_RECORD . " [" . $refusal->kind->token() . "] " |  |
| **E308** | HIGH | sugar-crush/src/Chat.php:3435 catch returns ToolResult::error($name, self::executionFailure($name, $e), ...) | Decision recorded in doc-block and pinned by tests/Chat/CallbackAuthoredRefusalTest. |
| **E310** | HIGH | sugar-crush/tests/bootstrap.php:534-540 fclose(\STDIN) + $GLOBALS["__sugarcrushSuiteStdin"] = fopen("/dev/null","r") | Prepend residual test inverted rather than deleted (see E314), which the Step allowed. |
| **E311** | HIGH | sugar-crush/src/Cli/HeadlessPermissionPrompt.php:314 $this->in = $in ?? NonInteractive::stdinDefault(); tests/bootstrap.php:509 "E243 is CLOSED" |  |
| **E312** | HIGH | sugar-crush/tests/TtyStreamArgumentCensusTest.php:203 HAZARD_CLASSES = ["Tty","PosixBackend"] |  |
| **E313** | HIGH | sugar-crush/tests/StdinConstantReaderCensusTest.php:335 FD0_STREAM_NAMES + :347 EXPECTED roster with heredoc/nowdoc fixtures |  |
| **E314** | HIGH | sugar-crush/tests/SuiteChildStdinPrependResidualTest.php:152 testBytesOnTheRunnersStdinDoNotReachASpawnedChildsPrompt, three arms |  |
| **E315** | HIGH | sugar-crush/tests/bootstrap.php:534-540 descriptor replacement removes the suite inheritance of fd 0 | Closed for the suite only; deliberately unchanged for a real sugarcrush binary. |
| **E318** | HIGH | candy-mosaic/src/Detect.php:285-290 stdinFd() returns is_resource($fd) ? $fd : null; :292+ isInteractiveTty guards injected probe | candy-mosaic/tests/DetectClosedStdinTest.php exists. |
| **E321** | MED | sugar-crush/tests/TtyStreamArgumentCensusTest.php:180-186 headline "WHAT IS STILL OPEN" + :409 testNoProgramOptionsInjectsATermiosWithoutAlsoNamingItsInput | Step was either/or; option 2 (state the residual at the headline) is taken. The general dataflow guard is still not built. |
| **E323** | HIGH | sugar-crush/tests/SuiteChildStdinIsolationTest.php:106-130 three-part history, assertion inverted at :176 |  |
| **E327** | HIGH | sugar-crush/tests/Support/InterpolationOpenerTokenTest.php:1220-1232 comparesAgainstBrace() takes case and a match arm; inverted paragraph corrected in place |  |
| **E328** | HIGH | sugar-crush/src/Hooks/BuiltIn/AuditHook.php:44 DEFAULT_DIRECTORY_STEM = "sugar-crush-audit-" (per-euid 0700 dir) |  |
| **E329** | HIGH | src/Agents/AgentManager.php:203, src/App/App.php:599, src/Hooks/ScriptHook.php:394 all uniqid($prefix.getmypid()."_", true); ClaudeCodeProvider.php:417-428 uniqid dropped | Four inventory rows deleted; the fifth (WorkflowEngine, 5 sites) remains and is E324. |
| **E330** | HIGH | tests/Support/ProcessUniqueTempNameTest.php:1828-1843 $seen now covers heredoc, class constant, unix:// URI and proc_open cwd; scanner widened at :1406+ |  |
| **E332** | HIGH | tests/Support/RefusesAnUnreadableSourceTrait.php extracted; all three named files now use it and none declares its own readOrFail | Three unrelated Config/*DriftTest files declare a readOrFail of their own; not the family E332 named. |
| **E336** | HIGH | candy-core/src/Util/TtyDetect.php:170-180 isAtty() is is_resource() then stream_isatty(), no (int) cast |  |
| **E338** | HIGH | sugar-crush/src/Cli/NonInteractive.php:793-797 one is_resource() guard on resolved $stream; stdinDefault():752-757 answers null |  |
| **E339** | HIGH | candy-core/src/Util/Tty/EnvDetect.php:189-193 guards !defined/!is_resource before stream_isatty(STDIN) |  |
| **E340** | HIGH | candy-core/src/Program.php:843-846 childDescriptor() resolves handle then constant then ['file',/dev/null] for all three arms |  |
| **E341** | HIGH | candy-core/src/Util/TtyDetect.php:168 declares the param as resource-or-null (widened as E341 asked) |  |
| **E345** | HIGH | sugar-crush/src/Hooks/BuiltIn/AuditHook.php:471 noticeRefusalOnce() with static latch at :122, called at :345 and :359 |  |
| **E346** | HIGH | sugar-crush/src/Sessions/BackgroundSessionRunner.php:462 emits '[' . kind->token() . '] '; tests asserted at BackgroundSessionRunnerTest.php:501,538 |  |
| **E348** | HIGH | tests/Chat/CallbackAuthoredRefusalTest.php pins the pass-through branch; rationale recorded at sugar-crush/src/Chat.php:3381 |  |
| **E351** | HIGH | sugar-crush/tests/bootstrap.php:278 AuditHook::pinDefaultLogDirectory($sandbox.'/audit'); pinned by tests/Hooks/AuditHookProductionPathIsolationTest.php |  |
| **E352** | HIGH | sugar-crush/src/Providers/ClaudeCodeProvider.php:433 id is 'tool_'.getmypid().'_'.bin2hex(random_bytes(8)) |  |
| **E355** | MED | tests/Support/ReflectionLineSliceReaderCensusTest.php:219 carries the known-answer table in both polarities (E322) | Residual Step (same mutation sweep over tests/Cli, tests/Config, tests/Diagnostics) needs a mutation run to judge; may deserve its own entry |
| **E359** | MED | four holes closed and residual pinned by rows at tests/Support/ProcessUniqueTempNameTest.php:1758-1782 (sprintf, other-file constant, define()d global, parent:: cross-file) | The two "cheapest" widenings the Step names are still residual rows, i.e. deliberate future work |
| **E360** | HIGH | tests/Support/NonBlockingVocabularyTest.php:496-506 grades all four affirmative combinations; the paired repair landed (SuiteChildStdinIsolationTest.php:181 sentence + no roster row) |  |
| **E361** | HIGH | tests/Support/NonBlockingVocabularyTest.php:~157 spells it "an OS-Linux requires-annotation" with no sigil and states the test never skips | Optional later-round guard (skip-without-markTestSkipped) not present |
| **E362** | HIGH | tests/Cli/BootstrapSkillSkipsTest.php:95 CHILD_WALL_CLOCK_BUDGET_SECONDS=20 with derivation, plus BUDGET_HEADROOM_SECONDS:119 and two pinning tests | Option (b) taken and the budget was lowered to 20s, not raised |
| **E363** | HIGH | tests/Support/NonBlockingVocabularyTest.php:476-482 synthetic fixture pinning the $tokens[$i+1]==='(' adjacency guard |  |
| **E365** | HIGH | candy-mosaic/tests/Support/LoopbackHttpServer.php and candy-mosaic/tests/SsrfServerLeakTest.php both exist as described |  |
| **E367** | HIGH | sugar-crush/src/Providers/ClaudeCodeProvider.php:272-282 comment + $errors read in the drain loop at :147-266 | stderr is now drained in-loop before fclose; throw interpolates $errors |
| **E368** | HIGH | grep "(int)" candy-core/src/Util/Tty/PosixBackend.php + candy-flip/src/Renderer.php:87 - every hit is now a WHAT-IT-USED-TO-SAY comment | all six sites fixed: 2/4 via descriptorForStream(), 3 via openDeviceDescriptor(), 5 via literal 0, 6 via literal 1 |
| **E373** | HIGH | sugar-crush/src/Diagnostics/RuntimeNoticeSink.php:367-372 "DE-DUPLICATED WITHIN THE BATCH, AND ONLY WITHIN IT" | the cheapest-honest option (doc-block line saying "per batch") shipped; no drainRaw() exists and none is needed |
| **E381** | HIGH | sugar-crush/tests/Cli/BootstrapSkillSkipsTest.php:318 assertSame(0, $status, ...) plus the 0/137/127/255/1 table at :386-393 | both helpers now assert the child exited 0 |
| **E382** | HIGH | sugar-crush/tests/Support/RequirementDirectiveProvenanceTest.php:113 METHOD_SKIPPING_FIXTURE + tests/Support/Fixtures/AnnotationSkipProvenance/MethodProseQuotingADirectiveFixture.php | method-level shape now in the control table; asserted at :235 |
| **E383** | HIGH | sugar-crush/tests/Tools/BuiltInToolCorpusTest.php:284 CENSUS_RESOLUTION - "this file deliberately asserts NO cardinality over src/" | the 292/311/symbol-kind literals are gone; adding a src/ file is green here now |
| **E385** | HIGH | sugar-crush/tests/Cli/StderrEmitterCensusTest.php:1119 expectExceptionMessage(never opens a body) and :1375 expectExceptionMessage(never closes) | both end-of-walk refusals now have synthetic fixtures |
| **E387** | HIGH | sugar-crush/tests/Support/RequirementDirectiveProvenanceTest.php:726-741 second scandir() enumeration anchored on __FILE__, :786 explicit stack | population floor replaced by a cross-check plus anchor |
| **E388** | HIGH | sugar-crush/tests/Support/RequirementDirectiveProvenanceTest.php:133-170 exercises requirementPredicates/requirementsOf/collectedTestFiles/partitionByResolvability inside the one absence test | comment at :138 rewritten to "the derivation is the first of four" |
| **E389** | HIGH | sugar-crush/tests/Cli/BootstrapSkillSkipsTest.php:541 assertStringContainsString($expected, $answer, $why) replacing the bare assertIsString | same shape applied to failOnRiskyFromConfig() table |
| **E392** | HIGH | grep "timeout -s KILL 60" over sugar-crush/tests returns nothing; all named sites now read 20 (e.g. BootstrapHookFileTest.php:943, McpToolWiringTest.php:1473, BinSugarcrushAutoloadGuardTest.php:637) | sugar-crush/tests/Support/ChildWallClockBudgetTest.php now asserts budget < defaultTimeLimit with headroom for every wrapper in tests/ |
| **E398** | HIGH | candy-core/tests/Util/Tty/PosixBackendRestoreLastTest.php:108 testTheSecondCallReAppliesWhatTheFirstOneSnapshotted(); only surviving assertTrue(true) is the historical comment at :28 | Round-trip half now pinned; file carries its own WHAT IT SAID retrospective |
| **E399** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkScanner.php:338-340 and :872-874 carry T_NAME_QUALIFIED/T_NAME_FULLY_QUALIFIED/T_NAME_RELATIVE |  |
| **E401** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkArgumentCensusTest.php:637 '0 + 1' fixture and :721-724 '<could not bracket the argument list>' scanSource fixture | All four UNCLASSIFIED returns plus scanSource's bracketing arm now have cases |
| **E402** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkArgumentCensusTest.php:553 assertTheInstrumentBehindTheAbsenceIsAlive() inside testNoSiteIsSpelledInAWayTheScannerCannotClassify() | Control now runs through scanTree(), the entry point the absence uses |
| **E403** | MED | candy-core/src/Util/Tty/PosixBackend.php:141-146 states the divergence as the invariant and names the harness ("4 in that harness") |  |
| **E404** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkScanner.php:115 methodSinks() seeded from Libc::cdef(); :484-533 match T_OBJECT_OPERATOR/T_DOUBLE_COLON; census test :746 testTheMethodSinkRosterIsDerivedFromTheCdef() | METHOD_SINKS step implemented and seeded from the cdef rather than by hand, as prescribed |
| **E405** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkArgumentCensusTest.php:1157 testTwoSitesSpelledIdenticallyInOneFileEachGetTheirOwnRow() and indexByKey() ordinals at :1253 |  |
| **E406** | HIGH | candy-core/tests/Util/Tty/PosixBackendTerminalDescriptorTest.php:368 requireLibcDescriptors(); no extension_loaded('ffi') gate remains outside the :345 retrospective comment |  |
| **E409** | HIGH | sugar-crush/tests/Tools/BuiltInToolCorpusTest.php:576 replaced the two declaration-count assertSame() calls with the derived relation at :590-596; :889 testRepoMapBlockNoLongerRestatesTheSourceCensus() | Both halves resolved: the exact literals are gone and RepoMapBlock's restatement is now forbidden by a test |
| **E411** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:323 stream_set_blocking($this->pipes[2], false) and :1099-1105 bounded drain guarded by is_resource(); tests/MCP/StdioMcpServerStderrDrainTest.php | Fixed exactly as the acceptance prescribed, including the is_resource() guard rather than @ |
| **E412** | HIGH | sugar-crush/src/McpMessage.php:59 public readonly mixed $result (+ :80 $resultSet sentinel); tests/MCP/McpMessageResultTypeTest.php:124 data-provided over every JSON type |  |
| **E413** | HIGH | sugar-crush/tests/Sessions/BackgroundSupervisorReapTest.php:1209 testThisSuiteIsNotOptedIntoAnyNonLinuxCiRunner() parses WINDOWS_LIBS/MACOS_LIBS and reds if sugar-crush is added | Entry cites ci.yml for MACOS_LIBS/WINDOWS_LIBS; they actually live in scripts/affected-libs.php:30-33 and ci.yml now consumes a computed matrix. Trigger is mechanised, so the entry is closed despite the mis-citation |
| **E414** | HIGH | sugar-crush/tests/Support/ChildLifetimeScanner.php exists with DescriptorInheritanceGuardTest.php and ChildLifetimeScannerFixtureTest.php beside it; no cardinality asserted, ACCOUNTED_FOR keyed File.php::method |  |
| **E415** | HIGH | sugar-crush/tests/Support/ChildLifetimeScanner.php:420-432 running-minimum brace-depth rule and :598-617 isProcCloseArgument() rejecting the `return proc_close($p);` exit-code idiom | Both shapes pinned in ChildLifetimeScannerFixtureTest |
| **E418** | HIGH | sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:432 ACCOUNTED_FOR_IN_LIBS + :828 testEveryExposedSpawnInAReachableLibIsAccountedFor(); LIB_HORIZON at :272 walks lang/ and bin/ with falsifiable mechanisms | Scope widened to the vendor/sugarcraft reachable closure; sibling tests/ and examples/ excluded with a derived, stated reason rather than by omission |
| **E420** | HIGH | sugar-crush/tests/Support/ChildStderrCaptureScanner.php:477 positionalShape() returns SHAPE_UNCLASSIFIED for a mixed spec; :495 hasTopLevelArrow() is a top-level arrow test, not str_contains; :438 records the 5,6,7 key rule |  |
| **E421** | HIGH | sugar-crush/tests/Support/ChildLifetimeScanner.php:119 CLOSING_HELPERS keyed on trailing Class::method and :583 reason names the callee found; BackgroundSupervisor::spawnSession KEPT at DescriptorInheritanceGuardTest.php:161, ClaudeCodeProvider::completeStream rostered at :497 | Corrected merge instruction was executed: the row was kept, not deleted |
| **E422** | HIGH | sugar-crush/tests/Support/ChildLifetimeScanner.php:119 CLOSING_HELPERS (name => measured reason) and :145 BEST_EFFORT_REAPERS; every row spent by a fixture at ChildLifetimeScannerFixtureTest.php:878-881 with a no-overlap check at :797 |  |
| **E423** | HIGH | sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:127 ACCOUNTED_FOR and :549 NOT_A_SPAWN both {count, reason}; staleness compares counts at :1372-1398 |  |
| **E425** | HIGH | sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:497 SHORT_VIA_HELPER receipt roster (+ :522 SHORT_VIA_HELPER_IN_LIBS for E418's scope) records every helper-bought short verdict | Doc-block records that E425 reopened once at E418's widened scope and was closed again with the sibling half |
| **E426** | HIGH | sugar-crush/tests/Support/ForkedChildExitConventionTest.php:116 ACCEPTED_BARE_EXIT carries {count, reason} (one row count 2) and :429 compares counts, not membership | The one grep the entry asked for: this roster does not have E423's boolean-membership hole |
| **E429** | HIGH | candy-core/src/Util/Tty/PosixBackend.php:452 public static function descriptorForStream($stream, ?string $fdDirectory = null), used at :107 and :640 | entry is itself a refutation of E396 whose fix shipped; E396 should be marked refuted |
| **E430** | HIGH | candy-core/tests/Util/Tty/DescriptorSinkScanner.php:115 methodSinks() / :203 sinksFromCdef() | roster is now derived from Libc::cdef() as the entry states |
| **E431** | HIGH | candy-core/src/Util/Tty/PosixBackend.php:494 and :561 clearstatcache(true,$path); fixture-table pin in tests/Util/Tty/PosixBackendStreamDescriptorTest.php:435-460 |  |
| **E432** | HIGH | candy-core/tests/Util/Tty/PosixBackendTest.php:233-241 tmpfile() probe + PosixBackend::descriptorForStream($probe), handle held open across the probe | entry body says DEFERRED / NOT DONE THIS ROUND, but both defects it names are fixed in the tree |
| **E435** | MED | commit 6757ff852 on candy-pty/tests/Integration/ResizeRaceTest.php; assertions now aggregate (readWidths() + single assertSame at :144) | the optional in-test fix shipped; the procedural half ("stop quoting the assertion count as a floor") is advice, nothing to schedule |
| **E436** | HIGH | sugar-crush/src/MCP/McpClient.php:200 catch (\Throwable) around $server->start(), and :194 for buildServer() |  |
| **E437** | HIGH | sugar-crush/src/McpMessage.php:62 readonly bool $resultSet, :80 array_key_exists('result',...), toJson() :219; StdioMcpServer.php:360 and :551 test !$response->resultSet |  |
| **E438** | MED | tripwire at sugar-crush/tests/Sessions/BackgroundSupervisorReapTest.php:1209; the unresolved STEP is now shipped as tests/Support/SuiteSkipRoster.php (PHPUnit event subscriber rostering skips by name) | entry's STEP asked for a derived skip assertion; SuiteSkipRoster does exactly that, so nothing left |
| **E439** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:727 writeLine(string $json, ?float $deadline = null); deadline passed at :619 and :638, omitted at :652 (callTool) as the STEP asked |  |
| **E441** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:430 $this->closePipes() before proc_terminate()/proc_close() at :432-:446, with the measured 1.05s vs 0.010s justification inline |  |
| **E443** | HIGH | sugar-crush/src/LSP/LspConnection.php:734 writeMessage(array $payload, ?float $deadline) with select+drainStderr+framingBroken; src/ClaudeCodeMcpClient.php:567 sendMessage() now uses writeAll() with fragment tracking | both out-of-lane sites named by the entry were ported |
| **E444** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:794-796 childIsRunning() \|\| $consecutiveSelectFailures >= MAX_CONSECUTIVE_SELECT_FAILURES returns false; constant documented at :177 |  |
| **E453** | HIGH | sugar-crush/composer.json:80 extra.sugarcraft.deferred-wiring row for sugarcraft/candy-kit; tools/check-path-repos.php:592 $deferredWiring reads it and stops it counting as a finding | resolved in the keep direction as the entry demanded (not pruned); CI greenness itself not re-run here |
| **E455** | HIGH | sugar-crush/src/Renderer.php:4095-4112 renderInput() wraps via wrapToPane($draft,$textWidth) at max(1,$chat->cols()-INPUT_CHROME_COLS) with 2-cell continuation indent |  |
| **E456** | HIGH | sugar-crush/src/Runtime.php:1343-1349 $onProgress fires on any content==='' chunk (covers tool-call-only); src/Backend/EngineBackend.php:1284 writeFrame kind 'reasoning', consumed at :1133 |  |
| **E458** | HIGH | sugar-crush/tests/Support/SuiteSkipRoster.php + tests/SuiteSkipRosterTest.php + bootstrap.php:43 install() |  |
| **E459** | HIGH | candy-pty/tests/Integration/ResizeRaceTest.php:283 readWidths() + :196 testTheTornReadDetectorSeesATornRead() |  |
| **E460** | HIGH | candy-core/tests/Util/Tty/PosixBackendTest.php:210 testRawModeWithSttyFallbackOnRealPty + :237 descriptorForStream() |  |
| **E461** | HIGH | candy-core/src/Util/Tty/PosixBackend.php:712 $this->saved->restore() |  |
| **E462** | HIGH | candy-pty/src/Posix/PosixMasterPty.php:301-307 dup taken then released; test PosixMasterPtyTest.php:57 |  |
| **E464** | HIGH | sugar-crush/tests/Support/SuiteSkipRoster.php:15,285,292,405 TestSuite\\SkippedSubscriber + recordSuiteSkip() |  |
| **E465** | HIGH | sugar-crush/tests/SuiteSkipRosterTest.php:678 testAPlainChildProcessThatRequiresTheBootstrapExitsZero() |  |
| **E467** | HIGH | candy-core/tests/Util/Tty/PosixBackendTest.php:233 tmpfile() probe + :275 assertSame([$probeFd], $sharing) inode-uniqueness |  |
| **E468** | HIGH | candy-pty/tests/Integration/ResizeRaceTest.php:209 bare-LF, :219 bare-CR, :229 padded rows |  |
| **E472** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:430 closePipes() before proc_terminate():432; LspConnection.php:946-967 drainStderr->fclose->terminateAndClose |  |
| **E473** | HIGH | sugar-crush/src/ClaudeCodeMcpClient.php:550-586 bounded write loop with $stdinFragmentPending and byte-count error |  |
| **E474** | HIGH | sugar-crush/src/LSP/LspConnection.php:488 if ($this->framingBroken) return false; doc-block 466-477 states the meaning |  |
| **E476** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:755 is_resource($this->pipes[0]) guard, :774 fd2 guard, :972 readLine is_resource($this->pipes[1]) |  |
| **E477** | HIGH | sugar-crush/src/MCP/McpClient.php:194,200 catch (\\Throwable); pin tests/MCP/McpClientServerIsolationTest.php:213 |  |
| **E479** | HIGH | sugar-crush/src/McpMessage.php:233-259 toArray() doc-block decides "inspection view"; pinned by McpMessageWireShapeTest |  |
| **E480** | HIGH | sugar-crush/src/LSP/LspConnection.php:734 writeMessage(array $payload, ?float $deadline) - null default dropped; @param doc :728-732 | both STEP options taken (no default AND documented at the parameter) |
| **E484** | MED | sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:272 LIB_HORIZON + :1167 loop asserting each row against real files | entry self-declares RESOLVED in-lane; LIB_HORIZON and its both-direction checks are present |
| **E485** | HIGH | sugar-crush/tests/Support/ChildLifetimeScanner.php:1214-1226 refuses a literal followed by any significant token (union / ?: cases) |  |
| **E487** | HIGH | tools/check-path-repos.php:592-696 DEFERRED_WIRING + tools/tests/CheckPathReposTest.php:41,681-747 covers --unused/--no-lib-path-repos/--strict-closure locally | residual 'three of four steps have no local counterpart' is also closed by tools/tests/ now run in ci.yml:210 |
| **E488** | HIGH | tools/check-path-repos.php:602-670 walks every deferred-wiring block and emits IDLE_DEFERRAL for every lib | matches E515's account; the fix landed in the tool as E488 asked |
| **E491** | HIGH | candy-pty/src/Posix/PosixMasterPty.php:339-420 deadline recompute + wasInterrupted() numeric /Unable to select \[(\d+)\]/ fallback; behavioural test candy-pty/tests/Posix/RetryOnEintrDeadlineTest.php:91,158 | the vacuous method_exists row it criticised still exists at tests/Posix/PosixMasterPtyExtendedTest.php:89-106, but the real test now sits beside it |
| **E494** | HIGH | src/Events/ReasoningDelta.php:52; Chat.php:8126-8148 passes the fifth argument only when the backend instanceof ObservesReasoning; Renderer.php:1097 paints the live thought | step 0 was solved differently than E494 proposed - a separate Backend/ObservesReasoning interface rather than widening Backend and all four implementations; the silent-no-op gap is closed either way |
| **E495** | HIGH | src/Backend.php:68,93 and all four implementations now use ?callable (EchoBackend:25,40 CommandBackend:107,271 StreamingCommandBackend:195,617) | the entry's deliberately-unmeasured 'wider than this hierarchy' census was never taken; if the supervisor wants that, it needs its own id |
| **E496** | HIGH | tests/Backend/EngineBackendTest.php:789 testTheIdleCeilingIsReArmedByATokenFrameAndNotOnlyByAThought drives the real ceiling on ScaledClockLoop | E496 said 'not a licence to delete the source-text guard (rule 6)' and the source-text scan WAS deleted - the replacement doc-block at EngineBackendTest.php:755-787 gives a measured reason (the scan survived moving the reset call), so this looks deliberate rather than a rule-6 violation |
| **E497** | MED | tests/Backend/Support/StreamingDouble.php:5 - all four doubles now live one-per-file in namespace SugarCraft\Crush\Tests\Backend\Support | fixed by a different mechanism than the entry proposed (moved to a Support sub-namespace rather than nested/prefixed); PSR-4 one-class-per-file makes the silent-merge collision impossible |
| **E498** | HIGH | tests/SymbolCitationDriftTest.php:412,483,489-490 (tests/ root, Tests\ prefix, trait_exists/interface_exists); both src/ cites fixed at src/Config/LayeredSettings.php:445 and src/Tools/Concerns/TruncatesOutput.php:554 |  |
| **E499** | HIGH | src/MCP/McpClient.php:194,200 now catch \Throwable; src/MCP/HttpMcpServer.php:153 uses McpTool::tryFromArray() (McpTool.php:99), the filter having moved off StdioMcpServer per its note at StdioMcpServer.php:1186-1194 | the filter was centralised on McpTool rather than ported/copied to HttpMcpServer, which is stronger than the STEP asked for |
| **E500** | HIGH | src/ClaudeCodeMcpClient.php:302 private string $readBuffer is a property, doc at :286 'A PROPERTY, NOT A LOCAL, AND THAT IS A FIX' |  |
| **E501** | HIGH | src/McpMessage.php:243 doc-block now says 'This was an inspection view from its first line'; tests/McpMessageWireShapeTest.php exists and resultSet is still emitted at :273 |  |
| **E502** | HIGH | src/MCP/StdioMcpServer.php:1099 absorbStderr() now guards on $this->stderrOpen && pipes!==null && is_resource(pipes[2]); the E476 mechanism prose is corrected in the same doc-block |  |
| **E504** | HIGH | src/LSP/LspClient.php:149-151 and 199-201 - definitions()/references() are now one-line forwards to their For() twin; isConnected() branches down from ten to five |  |
| **E506** | HIGH | all three capped on EngineBackend::MAX_FRAME_BYTES: ClaudeCodeMcpClient.php:748, MCP/StdioMcpServer.php:141,1156, LSP/LspConnection.php:133 |  |
| **E507** | HIGH | tests/Diagnostics/RuntimeNoticeSinkDeliveryTest.php:1147 roster is src/ AND tests/ plus bin/sugarcrush (asserted at :1157-1171), with a rule-15 known-positive fixture in both polarities at :1185-1199 |  |
| **E508** | MED | tests/MCP/McpWriteBudgetShapeTest.php:10-42 measures the STEP and PINS the healthy-write half | E508's own premise was measured FALSE: payload size is irrelevant (10 MiB to a live reader = 0.028s; 65629 bytes to a deaf one = 15.006s). The bound is still not threaded to the caller, but the entry's stated reason for wanting that is gone |
| **E509** | HIGH | tests/SymbolCitationDriftTest.php:920 testTheScraperFindsEveryShapeInASourceItHasNeverSeen builds every marker by concatenation (:922-925) | resolved by the stronger option: rather than excluding the file BY PATH, the file is IN its own roster and written so no literal cite appears (doc at :78-83) |
| **E510** | HIGH | tests/SwallowingCatchCensusTest.php:90 testTheScannerIsAliveInBothPolarities and :247 testNoTestSwallowsTheFailureStandingNextToIt; plus tests/Support/AssertionSwallowingCatchTest.php | the STEP census guard landed, not just the one-file fix |
| **E511** | HIGH | tests/Integration/MultiAgentRefactorTest.php:125-126 moves both $_SERVER['HOME'] and putenv; tests/Support/OneSidedHomeSandboxTest.php:73 NOT_YET_MIGRATED roster with :310 both-directions check | INDEPENDENTLY CONFIRMED: I agree E511 inverts E482's premise. tests/Agents/TeamTest.php:54-58 sandboxes both spellings and :80 asserts the real ~/.sugar-crush was untouched - TeamTest is the detector, not the offender. The ~3133 already-leaked dirs under the real home are still there and need a human |
| **E512** | HIGH | candy-pty/tests/PtyPoolReactLoopTest.php:173-174 cancels both cap and cycle; tearDown() residue guard at :43-59 asserts the shared loop is clean |  |
| **E513** | HIGH | candy-pty/tests/Support/HangWatchdogTest.php:233 gives beat() a reader; :136 testANonPositiveBudgetDeclinesToArm replaces the vacuous zero-budget row; corrected mechanism at Support/HangWatchdog.php:37-44 (rc 137, no summary line) |  |
| **E514** | HIGH | tests/Support/DuplicatedTestHelperDriftTest.php:589,601-603 testNoCopiedTestMethodHasDriftedUnrecorded on the T_PUBLIC/test* alphabet; isSubjectSpelling() classifier at :768-832; :697 pins the wide-alphabet unreadability | INDEPENDENTLY CONFIRMED: E514 does record the public-test-method half of E481 landing, and the tree agrees - both the widened alphabet and the classifier that replaced the 12 prose rows are present |
| **E515** | HIGH | tools/check-path-repos.php:602-670 validates every deferred-wiring row in every lib and prints IDLE_DEFERRAL naming which condition failed | same fix as E488; supervisor may want to merge the two ids |
| **E517** | HIGH | sugar-crush/tests/Support/OneSidedHomeSandboxTest.php:405 usesHomeSandboxTrait() token-stream; :389 envSuperglobal axis; :378 ??= and .= alphabet |  |
| **E518** | HIGH | sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:1537 namesItsOwnSubject() exact-if-declared-type, prefix otherwise |  |
| **E519** | HIGH | sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:483-498 both walks unioned in testEveryAcceptedDivergenceStillDescribesADriftedPair() |  |
| **E520** | HIGH | candy-pty/tests/Support/HangWatchdog.php:131 clearInheritedState() in install(); :285 armedAt stamped in parent | entry cites candy-pty not sugar-crush |
| **E521** | HIGH | candy-pty/tests/Support/SharedLoopResidue.php:79 signals in census(); SharedLoopResidueTest.php:220-241 asserts it |  |
| **E522** | HIGH | sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:697 testTheWidePublicAlphabetIsUnreadableAndTheTestStarOneIsNot() derives both halves |  |
| **E523** | HIGH | OneSidedHomeSandboxTest.php:63 and DuplicatedTestHelperDriftTest.php:111 both `use TestFileWalkTrait;` |  |
| **E525** | HIGH | sugar-crush/tests/Backend/EngineBackendTest.php:788 testTheIdleCeilingIsReArmedByATokenFrameAndNotOnlyByAThought() replaced the source scan |  |
| **E526** | HIGH | src/Workflows/Workflow.php:53-54 now ?WorkflowStatus/?bool; BackendSignatureNullabilityTest.php:842 T_ATTRIBUTE case; :351 testNoFileInSrcHasAnImplicitlyNullableParameter | src/ widening actually landed |
| **E529** | HIGH | src/Context/RepoMapBlock.php:335 "NO FIGURE IN THIS FILE RESTATES src/s CENSUS ANY MORE"; pinned by tests/Tools/BuiltInToolCorpusTest.php:889 |  |
| **E530** | HIGH | tests/Backend/ReasoningPaintTest.php:465 testBothPathsDropTheEmptyDeltaBeforeItReachesTheInbox(); src/Chat.php:3051 liveToolEvents() accessor now exists |  |
| **E531** | HIGH | tests/Backend/BackendSignatureNullabilityTest.php:910 PARAM_KINDS; :397 testTheParameterAlphabetOverSrcIsEnumerated; :447 testEveryDeclaredParameterKindIsReachable | doc-block at :78 names E531 explicitly |
| **E532** | HIGH | src/MCP/McpClient.php:192-196 match moved into buildServer() inside startServer() try; McpTool.php:99 tryFromArray() |  |
| **E534** | HIGH | src/LSP/LspClient.php:296-299 symbols() delegates to symbolsFor(); tests/LSP/LspClientBodyDuplicationTest.php:69 MAX_SHARED_TAIL_TOKENS = 40 |  |
| **E535** | HIGH | src/LSP/LspConnection.php:133 + StdioMcpServer.php:141 + ClaudeCodeMcpClient.php:273 derive MAX_FRAME_BYTES; LspConnection.php:1103 $contentLength < 1 floor |  |
| **E538** | HIGH | tests/MCP/McpFrameCapTest.php:99,156 catches only record; fail() moved out of try (doc-block :41-46) |  |
| **E540** | HIGH | tests/MCP/McpClientServerIsolationTest.php:280 testAMistypedConfigValueIsReportedWithoutAbortingTheOthers + :303 data rows; src/MCP/McpClient.php:194 catch stays \\Throwable |  |
| **E542** | HIGH | tests/LSP/LspClientBodyDuplicationTest.php:81 BRACE_OPENERS includes T_DOLLAR_OPEN_CURLY_BRACES; tests/Support/InterpolationOpenerTokenTest.php pins it |  |
| **E543** | HIGH | tests/LSP/LspConnectionFrameCapTest.php:193,238,271 three rows on refuseAnOversizedFrame() |  |
| **E544** | HIGH | (a) LspConnectionFrameCapTest.php:271 dormancy row; (b) all four MCP swallowers now record-then-assert: McpMessageResultTypeTest.php:418-428, StdioMcpServerHandshakeTest.php:261-273, StdioMcpServerStderrDrainTest.php:460-470, StdioMcpServerTest.php:439-452 | entry filed (b) as NOT fixed; a later round fixed all four |
| **E545** | HIGH | src/MCP/StdioMcpServer.php:916-929 readLine() doc-block now carries the frame-cap survivor paragraph |  |
| **E546** | HIGH | tests/SwallowingCatchCensusTest.php:247 testNoTestSwallowsTheFailureStandingNextToIt() asserts an EMPTY offender list tree-wide with no self-exclusion | entry was DEFERRED with 22 sites; a later round fixed all of them and added the tree-wide guard |
| **E548** | HIGH | /home/sites/sugarcraft/CONTRIBUTING.md:69-74 now lists tools/tests/, --unused, --no-lib-path-repos, --fix --strict-closure + recheck |  |
| **E549** | HIGH | tools/gen-docs.php:83 --check mode; .github/workflows/ci.yml:259 runs it; tools/tests/GenDocsTest.php exists |  |
| **E551** | HIGH | /home/sites/sugarcraft/tools/tests/StackedDocCommentTest.php exists and is the exact shape the entry prescribed (guard in tools/tests/, scanner copied deliberately) | tools/tests/BareCitationDriftTest.php covers the citation half too |
| **E552** | HIGH | .github/workflows/ci.yml:205 `tools: phpunit:10` with PINNED TO 10 DELIBERATELY comment at :198 |  |
| **E553** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:1133 resolvedParametrisedBudgets() + doc at :1385 "THIS IS THE ARM E553 EXISTS FOR" |  |
| **E554** | HIGH | sugar-crush/tests/MCP/StdioMcpServerToolListRobustnessTest.php:325 HAND MIRROR block now sits directly above testTheTypeFilterStillMirrorsEveryKeyMcpToolReads() at :355 |  |
| **E555** | HIGH | sugar-crush/tests/SymbolCitationDriftTest.php:407 namespaceDirectoryFor(), used at :560; testAnEmptyNamespaceDirectoryIsNotANamespace() at :868 |  |
| **E556** | HIGH | sugar-crush/src/Context/RepoMapBlock.php:53 "WHAT THIS SAID: Measured on this checkout it finds 58 packages"; no 297/316 remain in the file |  |
| **E557** | HIGH | sugar-crush/tests/Backend/BackendSignatureNullabilityTest.php:203 parameterListKeywordSpellings() now exists; :111 PARAMETER_LIST_KEYWORDS=[T_FUNCTION,T_FN]; :290 fully-qualified \\null fixture | Both previously-dead widenings are now pinned in both directions (:319-:326, :463-:468) |
| **E560** | HIGH | grep "catch (\\Throwable" sugar-crush/tests/Backend/*.php hits only fixture strings in AwaitPromiseDiagnosticArmTest.php:190-225 | The diagnostic-arm guard now pins the narrowed arms |
| **E563** | HIGH | sugar-crush/tests/Backend/StreamingCommandBackendTest.php:984 catch (\\PHPUnit\\Framework\\AssertionFailedError $e) now precedes the catch (\\RuntimeException) at :1001 |  |
| **E567** | HIGH | sugar-crush/tests/Backend/BackendContractWideningTest.php:523 bounded stream_select in compileInFreshInterpreter(); 256 KiB stderr known-positive at :448-455 | Residual (a revert hangs rather than reds) is stated in the entry and still true by design |
| **E569** | HIGH | sugar-crush/src/ClaudeCodeMcpClient.php:194-232 doc-block rewritten to "THE INHERITANCE IS NOW A LANGUAGE FACT" with a WHAT THIS SAID record at :201 and the FrameCapFamilyTest citation at :216 |  |
| **E570** | HIGH | sugar-crush/tests/DenialPrefixRosterTest.php:228 term narrowed to (?:permission\|approval\|consent)s?\\s+(?:is\\s+\|are\\s+)?required, with the E570 rationale at :167-:203 |  |
| **E571** | HIGH | sugar-crush/src/Backend/EngineBackend.php:186 `public const MAX_FRAME_BYTES`; all three claimants now derive it (StdioMcpServer.php:141, LspConnection.php:133, ClaudeCodeMcpClient.php:273) |  |
| **E576** | HIGH | sugar-crush/src/ClaudeCodeMcpClient.php:201 WHAT THIS SAID record replaces "inherited rather than invented"; :218 now points at the derived roster instead of the hand list | Same fix as E569 - the two entries are the same paragraph |
| **E579** | HIGH | grep 'derives the TOOL SET the glob leaves' sugar-crush/tests/Config/GlobFigureDriftTest.php -> single hit at :26 |  |
| **E580** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:405 resolvedParametrisedIn(); fold-in at :1902 array_merge(literal, resolved) |  |
| **E581** | HIGH | tools/tests/StackedDocCommentTest.php:72 CANONICAL const + :176 text-read/token-compare; corrected roster prose at :48-61 |  |
| **E582** | HIGH | CONTRIBUTING.md:81 property form; grep '10 tests\|45 assertions\|52 files\|lib count' -> no hits |  |
| **E585** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:296-300 token filter T_CONSTANT_ENCAPSED_STRING/T_ENCAPSED_AND_WHITESPACE with classified budget grammar at :289-294 |  |
| **E587** | HIGH | tools/tests/GenDocsTest.php:425-429 assertSame(1, $result['exit']) ahead of the bytes-unchanged assertion |  |
| **E588** | HIGH | tools/tests/ToolsEnvRosterTest.php:14 rosters SUGARCRAFT_CHECK_PATH_REPOS_ROOT and SUGARCRAFT_GEN_DOCS_ROOT; roster+walk cross-check at :419-429 | entry filed it as deferred; a later round shipped the tools/tests/ guard it prescribed |
| **E589** | HIGH | .github/workflows/ci.yml:185 separate job 'tools/ guards (+ drift pins that read candy-pty/ and sugar-crush/)' running tools/tests/ at :210 | the prescribed split out of path-repo-check was done; rationale comment at ci.yml:152-183 |
| **E590** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:343-352 unresolved arm reports $at($tail[1][1]) — group 1, not the whole match |  |
| **E591** | MED | docs/plans/crush_code_RESUME.md:541-544 'all three laneScopeOutputs empty, outOfLaneFiles [] everywhere' — scope carried into stages via structured output | the artefact fixed (round-orchestration script) is not in the repo; judged from the round-59 record, not from the script |
| **E598** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:693 shadowedByAnArrowFunction() with refusal wired in at :587-590 | the other nineteen files were deliberately untouched per the entry |
| **E599** | HIGH | sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:2249 carriesVisibility($anAbsentModifierIsPublic) + name-read-first at :2142-2188; pin at :934 |  |
| **E600** | HIGH | sugar-crush/tests/Support/DuplicatedDocBlockLineTest.php:9-60 instrument present; GlobFigureDriftTest.php:26 duplicate gone |  |
| **E602** | HIGH | DuplicatedDocBlockLineTest.php:262,278-298 FULLY EMPTY continuation fixture; grep '334' in DuplicatedTestHelperDriftTest.php -> no hits |  |
| **E603** | HIGH | DuplicatedDocBlockLineTest.php:140-147 per-root loop over a LITERAL root list, not self::SOURCE_ROOTS (:128-132 states why) | entry's NEXT CANDIDATE is still not done: tests/Backend/BackendSignatureNullabilityTest.php:365,420,427 still use single assertGreaterThan floors over everySourceFile() |
| **E604** | HIGH | DuplicatedDocBlockLineTest.php:94 SOURCE_ROOTS includes examples+workflows; :474 isPhp() reads the shebang from the file |  |
| **E605** | HIGH | DuplicatedTestHelperDriftTest.php:2134 T_AMPERSAND_NOT_FOLLOWED_BY_VAR_OR_VARARG arm and :2142-2160 identifier-not-T_STRING name read; pins at :1045-1095 |  |
| **E606** | HIGH | DuplicatedTestHelperDriftTest.php:1113-1122 truncated-declaration fixture asserting a NON-empty unparseable list (silence direction) | entry's DEFERRED rider (a comment between 'function' and the name) is measured-zero and deliberately not done |
| **E607** | HIGH | sugar-crush/tests/Support/ChildWallClockBudgetTest.php:1030 in_array($token[0],[T_CURLY_OPEN,T_DOLLAR_OPEN_CURLY_BRACES]) opener roster present |  |
| **E608** | HIGH | ChildWallClockBudgetTest.php:704 and :708 both arms return true (refuse) with doc-block at :680-688 |  |
| **E619** | HIGH | FrameCapFamilyTest.php:396 name = last non-trivia token before bare "=", :433-456 initialiser runs to bare , or ; at bracket depth zero |  |
| **E620** | HIGH | FrameCapFamilyTest.php:461-462 skips T_COMMENT/T_DOC_COMMENT before appending initialiser text |  |
| **E622** | HIGH | sugar-crush/tests/FrameCapFamilyTest.php:1056-1057 pairs class_exists\|\|interface_exists\|\|trait_exists\|\|enum_exists |  |
| **E625** | HIGH | tools/tests/BareCitationDriftTest.php:99 ROOTS = [/tools, /candy-pty]; candy-pty/tests/Support/SharedLoopResidueTest.php:33-50 citation rewritten as a rule-7 correction | Deferred "src/ is unguarded" finding withdrawn inside the entry |
| **E627** | HIGH | tools/tests/_bootstrap.php:206 counts T_CURLY_OPEN/T_DOLLAR_OPEN_CURLY_BRACES as openers, :211 closes only on bare "}" |  |
| **E628** | HIGH | tools/tests/ToolsEnvRosterTest.php:681-689 takes min indent as the row column and skips lines off that column |  |
| **E575** | MED | ROUND 61: branch proven REACHABLE (Frontmatter::parse null/scalar/empty) + reaching test wired (1e0ded325), then each non-array fixture bound to its own error line — count(3) invalid-lines + per-name both-sides pairing regex + good.md-never-negatived (2676c1614) | Closed with a caveat: the CLOSEOUT's 4th message variant is NOT proven in-lane — the measured reachability set is null/scalar/empty; if a fourth non-array input surfaces it belongs with the untested twin branch, now filed as E661 (AgentPresetRegistry.php:~368). |
| **E631** | HIGH | ROUND 61 lane C: token gate in classNames()/nonClassSources() — in_array($class, declaredTypes($file)), zero autoload attempts — + real-composer-autoloader subprocess driver (corpus-real-autoloader-driver.php) pinned RED-before rc 255 / GREEN-after (master 37a030e2f, a4c53817c); alt-syntax walk hardened (fbb1f5123) | The exact `else if():` alt-syntax re-arm shape is PARSE-INVALID per php -l (one-word elseif IS T_ELSEIF) — guard shipped anyway + loud-by-name imbalance backstop both polarities. Rule-7 doc-blocks at guard/skip/nonClassSources/class narrative. Residual low: E662. |
| **E632** | HIGH | ROUND 61: digits 278/297 (5 sites :40,:41,:135,:509,:514) elided, narrative kept, runtime truth deferred to the secondary-map test + the census itself; tests/Tools/ swept — remaining numeric hits are labeled-historical or byte figures | No-restatement guard DOMAIN widened: restatementGuardDomain() = [RepoMapBlock, the corpus scanner], headroom test measures over the same list; ALPHABET unchanged (E635 respected). Landed in the lane-C block (master 37a030e2f..cb97ed7a7). Guard-test rename deferred: E659. |
| **E636** | HIGH | CLOSED AT SOURCE: census classifier is now public static classifyFilePsr4Symbol(), token-pregated first arm; driver PHASE C proves the reflection census walks a poisoned tree to PHASE DONE (master a4c53817c) | Balance doc-block updated: ordering moot, a regression surfaces as rc != 0 in the driver test. |
| **E637** | HIGH | ROUND 61: declaredTypes() BOUNDS paragraph states conditional declarations (depth>0) are out of scope BY POLICY, pinned both polarities — testAConditionallyDeclaredToolIsMissedByDesignAndAnUnconditionalOneIsNot + testAFileWhoseOnlyDeclarationIsConditionalIsReportedExemptNotSilentlyIgnored; colon-form conditional bodies now counted; alt-syntax (if():…endif;) bodies counted as scopes; bracketed-namespace blindness named as THIRD bound, loud-not-silent (master cb97ed7a7) | src/ measured: 0 conditional declarations, walk totals unchanged (317/336/0-exempt/12 tools). A guard that states what it cannot see (rule 14). |
| **E639** | HIGH | ROUND 61 option 1 shipped: session narrowing INTERSECTS, never-existing tools REFUSE; Bootstrap dual registries — tools() split into unfilteredTools() + filterToolSet delegate (master f5e86e2ba); doc-block wording corrected to match the narrowing unset (aa5e6ba91) | Tripwire testAPolicyNarrowedRegistryIsIndistinguishableFromATypo REWRITTEN TO PASS, not deleted; the decision is recorded in resolveGrantedTools()' doc-block as §0-NOW-62 §7 required. Sandbox range e40a11965..95495b997. |
| **E641** | HIGH | ROUND 61: WorkflowEngine +toolRegistry ctor param + resolveRequestTools at the MEASURED **5** CompleteRequest sites ([]→null normalization, unknown name fails the stage loudly) — re-derivation beat the brief's 7 — master 83518fecc; per-task refusal parity in executeParallelStage's build loop (6ada92d35); Bootstrap toolRegistry feed merged (94ef239a4); non-dispatch enforced via capturedRequests post-run at WorkflowToolsHandoffTest :222/:251 instead of never()-expects shadowed by setUp's permissive first-match stub (d5e7ea1fc) | The brief's seven-site enumeration was a citation, not a measurement. Default request superseded per-agent only when a manager is attached (lane-A seam (e), closed by 94ef239a4). |
| **E642** | HIGH | ROUND 61: warn at load when a preset names a tool the session DISABLED, refuse never-existing names at grant (master 80cd6054a); narrowedGrantWarnings() registry-vs-ceiling collector, lenient parse, pull-based, toolless-session suppression (b5fff4660); the bare-name half tests/Agents/AgentDefinitionTest.php:528 stood from before | E639's decision unblocked the open half. ROUND 62: the operator-facing DRAIN shape was withdrawn as a measured seam (chat-time, master `9a0128ecf`) — the residual rides **E653** (PARTIAL); census trio in-step doctrine recorded after `b2790b1a2`. |
| **E643** | HIGH | ROUND 61: executeSubAgent skill loop fail-closed RuntimeException — a granted-but-unresolvable skill throws instead of silently skipping; 2 tests, refusal + positive polarity (master b47391f1f) | Direct-path executeAll builds no per-agent skills at all — that guard's absence is E654. |
| **E644** | HIGH | ROUND 61: AgentWorkerPool::executeAll(+?\Closure $toolGrantResolver = null) at the per-agent request seam; AgentManager::executeAll resolveBatchGrants fail-BEFORE-dispatch + honest settleAbandoned reason; 4 manager + 2 pool tests (master d7ed85bcc) | Cross-fork enforcement remains DORMANT parent-side (names-only wire) — E660. |
| **E645** | HIGH | ROUND 61: fromClaudePrefixDialect() translates (x:*)→(x *) in toolList() at foreign import; unit + matcher + end-to-end chain tests (master e5bb9de76) |  |
| **E646** | HIGH | ROUND 61: LEASE frame in both loops — non-streaming worker heartbeats 3600s — NO request cap anywhere, standing rule kept (master ff1521c8d); both polarities real-clock (alive+leased survives 18s>15s; silent-unleased reaped ~15s; dead-lease-holder reaped <10s); review fix clamps forged oversized leases to LEASE_MAX_SECS=3600 at both accept sites :273/:468, test band (now+3000, now+3600] (d64a5436f) | Containment is by lease, never by a total-request timeout. |
| **E647** | HIGH | ROUND 61, **DECISION (i)**: name+schema rehydrate CHILD-side over the additive 'toolSpecs' startup-frame wire key; public static rehydrateTools is data-only (execute() throws, half-parsed throws); frame test EXTENDED with the names roster untouched; child accept/refuse tests; legacy frames (no key) unaffected (master ff1521c8d) | Forked workers still ignore request.tools parent-side (encodeTools names-only) — E660 carries the dormant cross-fork seam. |
| **E650** | HIGH | ROUND 61: dead child detected before the execute fwrite → Failed AgentResult, zero surfaced warnings, incl. reflection-pinned no-leftover-registration; 3 tests (master ff1521c8d) | The failOnWarning="true" suite-red trap the entry predicted is closed at the detect site. |
| **E651** | HIGH | ROUND 61: Renderer agentSplitWidth doc-block re-anchored to symbols pre-lane-cut (dc2ac0b48); tests/Tui swept — 4/4 numeric citations FALSE or DRIFTED (WorkflowEngine.php:1254, AgentManager.php:681 ×2, Style.php:969-970, Renderer.php:1663), all converted to symbol anchors, 0 residual File.php:NNN in the dir, EXACT-floor suite (36055b80b) | The tree-wide sweep continues as E656 (AgentManagerTest), E657 (src prose), E658 (~20 test dirs). |
| **E1** | MED | ROUND 62 lane I: README headline figure refreshed from the measured lane-I run (master `ed1136758`) | Refreshed figure is correct at `1a6ef5f60` but remains a hand-written literal — generator candidate recorded as **E669**. |
| **E12** | HIGH | ROUND 62 lane W route-(b): `paletteStandsDown` gate in `App::delegateToChat()` at BOTH doors — hosted palette stands down under keyboard-owning shell views (master `3819a62ef`); behaviour pin flipped 6 to 13 under the same name; `Renderer` untouched | Composite-clipping follow-up + palette-opened-then-left-open leak recorded as **E666** (lane-W seam). (re-confirmed r68/ec: `paletteStandsDown` KeyboardHandler.php:378 + App.php:1172) |
| **E14** | HIGH | ROUND 62 lane S: `Tool`/`ToolSignature` moved out of the root namespace to PSR-4 `\Registry` — NO `class_alias` facade (master `f58f881b4`; prose errata `86f07a7b0`); census roster row `ToolRegistry.php=>2` removed + 3 AbsentHasKey pins | Deliberately no in-lane suite — review + merged suite were the gate (merged green @`1a6ef5f60`). |
| **E15** | HIGH | ROUND 62 lane X: candy-vcr tape lexer ports the vhs JSON/REGEX token kinds `TOKEN_JSON`/`TOKEN_REGEX` (master `89496a208`) | Wait-pattern AST consumption of `/re/` tokens still open — recorded as **E668**. |
| **E27** | HIGH | ROUND 62 lane Q: typed `ProviderException` for ClaudeCode throw sites (exitCode on a property, NOT `getStatusCode()`), Vertex truncated-arguments `json_last_error` transient discriminator (master `4f753dffa`); `TransientFailureTest:335` comment flipped in-step | `ClaudeCodeInvocation.php:126` bare throw left open by lane Q — recorded as **E664** (round-63 CC lane). |
| **E29** | MED | OBSOLETE, measured: `tests/Cli` as a DIRECTORY re-timed 2026-09-10 at 16.7s — the ">4min hang" instruction retired from `crush_agent_rules.md` (master `c6178b17c`) | Closed obsolete-by-measurement, not by a fix; lane J. |
| **E30** | HIGH | ROUND 62 lane V: prose figures pinned by `TransientFailureTest::testTheProseFiguresInThePlanMatchTheConstantsTheyQuote()` (+3 literal asserts, DOMAIN docblock); root `crush_code.md` Phase-5-8 sentence swapped to the `totalBackoffMicroseconds()` phrasing at this closeout (master `1a6ef5f60`) | Rule-7: plan prose now names the constants; the pin reddens the day one moves. |
| **E37** | HIGH | ROUND 62 lane I: whole env roster documented on `--help` (master `04d1b06c9`) + help env guards anchored to the entry column + reverse screen census added (review fixes `416e06774`) | Anchored-regex guards, mutation-proven at review. |
| **E40** | MED | ROUND 62 lane T: threat-model paragraph — realpath is INERT against hardlinks — added at the decision site (`Bootstrap::mcpConfigDecision()` region) (master `0373308a3`); the three resolves deliberately UNCHANGED | Decided paragraph-only per the E639-class recommendation; the entry's `mcpClient()` locus was itself citation drift, judged at the true site. |
| **E47** | HIGH | ROUND 62 lane R: `clipOverlayToCols()` runs before `Veil::composite()` — overlays no longer escape width clipping (master `f4b3407ec`); PaneWidth pins flipped accordingly | E43's horizontal-offset step is the still-open sibling — see PARTIAL. |
| **E48** | HIGH | ROUND 62 lane R: frame-height floor with option-2 rationale documented in `renderStatusBar()`'s docblock (master `f4b3407ec`) | |
| **E49** | HIGH | ROUND 62 lane X: candy-shine per-column width budget for tables, `withTableColumnBudget()` (master `c4d251e60`) | Unblocks the candy-shine half of E43 — see PARTIAL E43. |
| **E50** | HIGH | ROUND 62 lane X: candy-core `SgrState` tracks SGR 58 + OSC 8 state, additive (master `388bf044d`) | sugar-crush `Renderer::balanceSgr()` still carries its own URI tracking — delegation seam recorded as **E667** (EE lane). |
| **E115** | MED | ROUND 62 lane J: `SkillRegistry` path-match perf note re-taken with spelled-out 1/12/24-char segments; ratios measured **0.175–0.229x** (master `5cbad71bd`) | Supersedes the stale 0.31–0.33x band; measurement-only. |
| **E116** | MED | ROUND 62 lane V: keep-kebab + mixed-case-by-history decision recorded in the README error.type paragraph (master `1a6ef5f60`); rename option PRICED and declined (ownership + 7+ same-string hits in the unrelated `WorkflowRegistry` loadYaml-not-found domain) | Supervisor may still overrule with the rename; intended order recorded in lane-V's REPORT. |
| **E124** | MED | ROUND 62 lane I: `EnvRosterDriftTest` mention surfaces widened to the monorepo landing page `docs/_data/sugar-crush.body.html` (master `b36a40735`) | Scope decision taken: the drift guard covers the landing page now. |
| **E475** | HIGH | ROUND 62 lane O: owner-pumped bounded idle stderr drain — `readResponse()` drains every iteration, public `pumpStderr()` = one bounded 16x8192 non-blocking drain (master `70461bd2d`); loop-mount + select REFUSED with E537 evidence; cadence prose narrowed to what runs (`6cd6976ed`) | New `StderrIdleDrainTest` (flood handshake, both polarities). Between-turns parent wiring is the E537-retargeted residual (Chat.php turn-boundary). |
| **E649** | HIGH | CLOSED round 62: E652 feed (`6a6413bcd`) + E663 engine-pool spec (`fcca5d5ff`) + merge-time reconcile of the 4 seam-markers and WORKFLOWS.md (`1f30aab02`, `b2790b1a2`) | The `/workflow-run` worker finally has a provider end-to-end; the FAILED-vs-fabricated behaviour note is live in WORKFLOWS.md. E649 family (E649/E652/E663) FULLY closed. |
| **E652** | HIGH | ROUND 62 lanes E+N: `AgentPoolConfig` inherits the session provider spec into forked sub-agent workers feeding BOTH `ProcessExecutor` AND `AgentWorkerPool` (master `6a6413bcd`); engine pool gets the launch worker spec (`fcca5d5ff`); review fixes — `${VAR}` placeholders not resolved secrets in the spec, both-feed pin pairs (`dc6d86495`); E652 helpers relocated below `provider()` for the stacked-doc-comment scanner (`9a0128ecf`) | Honest pool-feed pins disprove the fabricated-completion shape; doc half of the behaviour change landed with it. |
| **E655** | MED | ROUND 62 lane G: polluter = `TuiRenderer::$terminalSize` process-global cache left NULL by tests/App tearDowns — victims then read ambient `Tty(STDOUT)`; fixed with a 60x200 `tests/bootstrap.php` pin + re-pins in the three App classes + `TerminalSizeFallbackIsolationTest` (master `9cf8cd8d8`, honest scope `3c18d8c8a`, tty-half keystone `169ebdf37`); victims never touched — **ROUND-67 CLOSE re-issue note: the later "E655-F2 KeyboardHandlerPaletteTest vacuous-skip heal" bundle measured PHANTOM — that file never existed in any ref (pickaxe zero) and no matching skip shape exists tree-wide; the keystone already uses the early-return guard (no `markTestSkipped`; `SuiteSkipRoster` one-named-skip holds); a re-issue needs a real target brief** | Honest scope: the pin holds until the first `resetSizeCache` AFTER tests/App — >=10 `tests/Tui` tearDowns + `WorkflowLivePaneTest` (carve-out) still reset-null, path-sorted after both victims; pinned-default polarity catches reintroduction deterministically only on TTY runners. Published-domain at lane tip `d8efb147b`: 11,326 / 172,823 / 1S / exit 0. |
| **E656** | HIGH | ROUND 62 lane E: three false `File.php:NNN` citations re-anchored to symbols in live-outputs tests (master `b46e609f0`) | Tree-wide citation residuals continued as lane H (E657/E658: `8fa49004c`..`ec0f82593`). |
| **E663** | MED | Filed + CLOSED in the same round: lane N gave the `/workflow-run` engine pool the launch worker spec (master `fcca5d5ff`); seam markers reconciled at merge (`1f30aab02`) | N deliberately landed NO executor — injection would flip the pool's sync dispatch; last seam of the E649 family. |
| **E41** | HIGH | ROUND 63 lane DD (E41c pair): a present-but-broken .mcp.json reports instead of vanishing (master `f7dd47203`); throw-assertions capture before they assert (master `97b662d1e`) |  |
| **E156** | MED | ROUND 63 lane DD: launch harness records why per-spawn redirection is load-bearing (master `2a7f39d91`) |  |
| **E184** | HIGH | ROUND 63 lane DD: notice-routing assertions derive from the Bootstrap format consts (master `18766d09e`) |  |
| **E407** | HIGH | ROUND 63 lane DD: both shutdown ladders folded onto ProcessReaper (master `2e7329e9d`), docblock repoint (master `7b4f770a4`) | Residual grace-ladder families (ScriptHook / StatusLineCommand / BackgroundSessionRunner) minted as **E676**. |
| **E483** | HIGH | ROUND 63 lane DD: deadline roster derived from tokens and threaded through send() (master `b51b164a2`); scanner counts interpolation brace openers (master `7c9b471e9`) — unbounded send() fixed with deadline DEFAULT NULL, verified NOT a blanket timeout (E646 rule) |  |
| **E664** | MED | ROUND 63 lane CC: ClaudeCodeInvocation subprocess throws retyped to ProviderException + launch-pin props hoisted (master `2f60e1696`); real spawn-failure branch driven in review fix (master `2d538c61f`). 12-bare-throw adjudication: 2 fixed / 10 KEEP, each justified |  |
| **E665** | MED | ROUND 63 lane DD: MCP wire names surfaced where the rules are written (master `fa4f40ed4`) | Operator-facing /mcp TUI panel + `crush mcp --json` wire-name rows minted as **E678**. |
| **E666** | MED | ROUND 63 lane JJ: an abandoned palette stands down at the delivery choke point (master `353e6a828`) | Composite adoption minted **E682**; KeyBindingRegistry prose + mouse pane-jump first-key-closes seam minted **E683**. |
| **E667** | LOW | ROUND 63 lane EE: SgrState owns the row-boundary balance contract (master `bb58148d6`); Renderer::balanceSgr delegates (master `19a52c065`) — rowOpen/rowClose/osc8 byte-identical; applyOsc two-field OSC 8 delta documented (master `e40fe6ab2`) |  |
| **E668** | LOW | ROUND 63 lane EE: Wait /re/ gets its AST directive and a truthful compile (master `386f1aa26`); TOKEN_REGEX mapping rationale corrected (master `cedef8ee9`) |  |
| **E669** | LOW | ROUND 63 lane FF: README headline suite figure pinned by generator+guard (master `fa0067255`); refresher whole-suite claim made true, never-silent qualified (master `9bcd7dddf`); merge-time census re-pin of README + suite-figure.json + artifact at 11,438/175,211 (master `8c52b26e5`) | First RED-vs-merged run caught by a lane's OWN guard — the pinned-headline system working; census trio extends to the suite-figure.json+README+artifact triple. |
| **E189** | HIGH | ROUND 63 lane GG: the two misdescribing failure messages fixed (master `74b0e10bf`) |  |
| **E213** | HIGH | ROUND 63 lane GG: the two Failures: counts replaced with the failing test names (master `b8294462f`) |  |
| **E215** | HIGH | ROUND 63 lane GG: markdown collector reads .md case-insensitively and the package root via scandir (master `983db89b1`) | Monorepo-root half measured out of lane remit — no live miss. |
| **E217** | HIGH | ROUND 63 lane GG: adjacent conversions keep their field wildcard; adjacent-conversion fixture landed (master `f136ce903`) |  |
| **E276** | HIGH | ROUND 63 lane GG: methodBody() given the full interpolation-opener roster incl. T_CURLY_OPEN; its KNOWN_GAPS row deleted (master `133ed62ba`) | The latent under-count is closed. |
| **E28** | HIGH | ROUND 63 lane HH: tool calls evaluated once per accepted stream (master `887c449c3`) |  |
| **E136** | HIGH | ROUND 63 lane HH: roll back a claim whose worktree step died (master `f9eaaf4c8`) |  |
| **E137** | HIGH | ROUND 63 lane HH: every flock wait bounded — timed LOCK_NB in TaskList + WorktreeManager (master `f9eaaf4c8`) | The helper duplication minted **E679**; Session.php adoption minted **E680**. **R67-F1 heal:** `351d5a915` fixed the latent fopen-before-mkdir precondition at TaskListTest.php:99 (systemd-tempfiles-clean had deleted `<tmp>/task_locks`, journalctl-proved 16:39:26 — NOT order-pollution; mutation-proved heal-disabled reds at the fopen line). The briefs' "E42b" label for this heal has no ledger row and no git trace — the commit cites E137; recorded as a naming errata. |
| **E262** | MED | ROUND 63 lane HH: src/-wide catch/false arm audit recorded with the E295 cluster (master `74dc2ab38`) |  |
| **E295** | HIGH | ROUND 63 lane HH: drain the child's inherited buffers before exit(0) (master `74dc2ab38`); review fix pins the child buffer-drain polarity at the parent-observable level (master `05454b5f1`) |  |
| **E660** | MED | ROUND 63 lane BB: the Task tool rides executeAll per-agent grants (master `e5f597705`) — per-agent grant enforcement is no longer a names-only dormant wire on the batch path |  |
| **E661** | LOW | ROUND 63 lane HH: AgentPresetRegistry's non-array-frontmatter guard reached (master `62dd9814c`) |  |
| **E654** | MED | ROUND 63 lane HH: per-agent system prompt on the batch path (master `887c449c3`), pool side (master `74dc2ab38`), then the RAW declared prompt spliced with census rows in-step (master `f3f61e15f`) — the batch path builds real prompts instead of refusing |  |
| **E2** | HIGH | ROUND 63 lane II: ESC b / ESC f routed to word motion (master `ea3e04184`) |  |
| **E8** | HIGH | ROUND 63 lane II: stop counting the pre-flight in 'Stages completed' (master `6b8143614`) |  |
| **E324** | HIGH | ROUND 63 lane II: WorkflowEngine's five SubAgent ids given real entropy (master `e79e90b84`) |  |
| **E349** | HIGH | ROUND 63 lane II: the flagless-uniqid walk got its walk-control fixture (master `bb61be847`) |  |
| **E568** | HIGH | ROUND 63 lane II: Workflow stopOnFirstFailure paired with an XSet sentinel (master `461324ba3`) |  |

## `NO-FIX` — 143

| id | conf | evidence | note |
|---|---|---|---|
| **E26** | HIGH | entry states the line "was deliberately NOT added. This records the prerequisite so the item is a decision rather than an oversight", Severity None | state unchanged: grep for additionalDir\|additionalWorking\|extraDirs\|workingDirs\|additionalDirectories over src/ bin/ is 0 hits, absence still pinned by EnvironmentBlockTest:168 |
| **E39** | MED | "It is a trap for future rounds, which is the reason to record it" / Step is "Before diagnosing, re-run" | an unreproducible operational note with no code change to schedule; keep as a re-measure-if-it-recurs record |
| **E62** | MED | "The FIX is deferred; the FINDING is not" plus "DECIDED 2026-08-21 by the user: do NOT build the askpass route" | Phase 9's interactive PTY is unbuilt (no candy-pty use in src/), so nothing here is schedulable; it is a bar to apply IF option (C) is ever built. Separately, Phase 9 step 1's env work (GIT_TERMINAL_PROMPT / DEBIAN_FRONTEND / PAGER) is absent from src/ — that is a plan item, not this entry's step |
| **E77** | HIGH | Entry: 'Step. None while the requirement stands'; candy-core/composer.json:34 still hard-requires ext-intl | Becomes live only if ext-intl is relaxed to a suggestion |
| **E78b** | HIGH | Entry: 'Step. Leave it.'; tree matches (Bootstrap.php:6249 summary via seam, :6256 raw id rows) |  |
| **E79** | HIGH | Entry: 'Why it is not a fix' - semantics decision; shape pinned by candy-sprinkles/tests/StyleTest.php:1235 | Only a foundation-wide semantics decision remains; no defect to fix |
| **E88** | HIGH | No producer in src/ or bin/ (src/App/App.php:686,1088,1354 only); pinned by tests/App/HostedFrameReadsThePaneTest.php:171 | Recorded dormant seam, deliberately kept; only a behavioural wiring decision remains, which the entry says needs its own item |
| **E95** | HIGH | tests/Integration/McpToolWiringTest.php:561-583 - decision made to accept the line permanently, re-argued in round 44 and bounded by a map-size assertion |  |
| **E100** | HIGH | tests/Skills/SkillPathPatternTest.php:306-330 classifier unchanged and still pinned jointly; entry: 'correct-by-inspection and pinned only jointly' |  |
| **E102** | HIGH | grep launchChatInChild: still exactly 2 call sites, both in tests/Integration/McpToolWiringTest.php; entry says 'Measured, and cheap today' | trigger (a third caller) has not fired |
| **E105** | HIGH | tests/Config/ConfigWriteProducerDocumentationDriftTest.php:45-52 still states the fifth-door limit; entry: 'No cheap fix' |  |
| **E109** | HIGH | tests/Config/ReadmeRosterDriftTest.php:620 still uses '\S+' for the sample path; entry Step: 'None unless the sample path becomes load-bearing' | state unchanged and deliberately so |
| **E110** | MED | no test pins the README Keys table row by row (tests/Commands/KeyBindingDriftTest.php pins the in-app reference, not the README table); entry: 'Until then the table stays prose' | latent step not taken: KeyBinding::$id is a row id, not a canonical chord id |
| **E114** | MED | process incident (shared round-44 scratchpad); load-bearing half was E96, and the shared-/tmp diff it names is gone (see E121 evidence) |  |
| **E117** | HIGH | timing/memory figures still in src/Skills/SkillRegistry.php:14-120 doc-block; no env-gated benchmark test exists; entry: 'Do NOT add an unbanded assertion' |  |
| **E121** | HIGH | re-ran the entry's census (grep -rnE '(glob\|scandir)\s*\(' src tests \| grep -i tmp): no live shared-/tmp before/after diff; tests/Integration/ParallelToolCallsTest.php:810 is now a retraction doc-block | informational entry; residual is a re-run-when-X-appears note |
| **E126** | HIGH | tests/SymbolCitationDriftTest.php:71 'citations of PRODUCTION symbols are out of scope, which is the larger' - stated limit, unchanged |  |
| **E128** | HIGH | entry text: "Step. None, unless widening" — a recorded decision |  |
| **E129** | HIGH | entry text: "Step. None for now. … The trade is taken deliberately." |  |
| **E130** | MED | scripts/affected-libs.php:55 PHP_VERSIONS = [8.3, 8.4]; entry step is "read that run", not a code change | 8.4 CI leg exists; whether anyone read it is outside the tree |
| **E139** | HIGH | entry header: "Severity: none, correction. Re-measured as instructed." |  |
| **E145** | HIGH | entry text: "the machinery was deliberately not built"; "Step, when it first reds" |  |
| **E146** | HIGH | entry header "Severity: informational"; pin exists at tests/Config/ReadmeJsonErrorContractDriftTest.php:1352 |  |
| **E150** | HIGH | entry text: "Step. None. Recorded so a merge-time failure … is read as prose drift"; anchor still live at ConfigWriteProducerDocumentationDriftTest.php:430 |  |
| **E151** | HIGH | entry text: "Documented and pinned this round; NOT fixed"; pins present at tests/Config/DocumentParagraphsTest.php:66 and :211 |  |
| **E157** | HIGH | Entry text: "Recorded so the zero is not shipped as a finding by a later round"; Step is "Confirm each by mutation before acting on it" | Methodological caution, no code change requested |
| **E159** | HIGH | Entry text: "Severity: process note, not a defect" + "Step at merge" | Merge-time procedure, nothing to schedule |
| **E166** | HIGH | Entry text: "Severity: methodological"; Step is "State the scope of every mutation verdict in the round's report" | Reporting convention; already observed in tree (E169's scope caveat is written into StderrEmitterCensusTest) |
| **E167** | HIGH | Entry text: "Severity: process" + Step "Measure the floor at the commit the lane copies are cut from" | Supervisor process rule, no code change |
| **E168** | HIGH | Entry text: "Severity: medium, data loss. Observed" + Step is a three-part harness/supervisor checklist | Round-harness procedure, not a tree defect |
| **E169** | HIGH | Entry text: "Step. None. Do not re-derive the 58"; guard present at tests/Cli/StderrEmitterCensusTest.php:1450 testTheNaiveGrepCountReconcilesWithTheTokenScan() | Recorded measurement; the named guard still exists |
| **E183** | MED | Entry text: "Measured, and deliberately not closed ... Neither is worth a round on its own"; fix present at tests/Support/ReapsForkedChildrenTrait.php:198 pcntl_waitpid($pid,$status,\WNOHANG) inside the no-signal branch | Code fix is in the tree; grep for canSignal in tests/ returns nothing, so the branch is still unpinnable on this box exactly as the entry says |
| **E186** | HIGH | Entry text: "Step. No action now. The trigger for revisiting any one of them is a second party reproducing a whole rendered SENTENCE" | Informational; both self-amendments are already in place. NAMED_FORMATS at BootstrapLaunchFormatConstantsTest.php:203 is the live census |
| **E188** | HIGH | entry Step reads "No code change. When a doc-block cites a mutation verdict, cite the failing test names." | the rule was later mechanised anyway by the E214 guard classTotalsIn() |
| **E190** | MED | docs/plans/crush_code_RESUME.md:1454 "Put it in the fix-agent brief: re-derive HEAD with git log --oneline <base>..HEAD" | brief itself lives outside the repo; only the handoff record is verifiable here |
| **E191** | MED | docs/plans/crush_code_RESUME.md:1466 states the PREDICATE-not-SCOPE rule verbatim | process rule, no code |
| **E197** | MED | entry Step reads "Supersede E172 rather than schedule it"; verified at source src/Commands/CommandLoader.php:577 is the single error_log() | supervisor should stamp E172 (backlog L6975) SUPERSEDED - nothing marks it today |
| **E200** | HIGH | entry Step reads "Otherwise leave it and keep the non-coverage stated in the test doc-block, which it now is" | non-coverage still stated at tests/Diagnostics/RuntimeNoticeSinkDeliveryTest.php:287 |
| **E201** | HIGH | entry says "Status. Fixed in the tree" and its Step is a process lesson, not a change | renamed guards present at tests/Support/ForkedChildExitConventionTest.php:361,418 |
| **E216** | HIGH | entry Step reads "None; recorded so the span-plus-threshold design is not retried." |  |
| **E220** | HIGH | Entry says "**Step.** None — recorded so the pattern is countable." |  |
| **E225** | HIGH | Entry says "**Step.** None — recorded so the count of wrong prescriptions stays honest." |  |
| **E229** | HIGH | Entry is a Status record — "Pinned behaviourally by ForkedChildExitConventionTest::testAPlainExitInAForkedChildRepublishesTheOutputBufferItInherited()"; that test exists at tests/Support/ForkedChildExitConventionTest.php:504 | Contains its own in-round CORRECTION about Loop::set(); nothing to schedule |
| **E230** | HIGH | Entry retracts E208's hazard ("there is no 8.4 deprecation notice to risk") and records Status "Handled by construction"; tests/Support/InterpolationOpenerTokenTest.php:1410 phpFilesToScan() walks tests+src as the correction states |  |
| **E231** | HIGH | Entry is a measurement retracting its own first census ("Re-run with a one-line window: 0") and says "**Not fixed.** E205's argument stands unchanged" | The fix obligation lives on E205, not here |
| **E237** | HIGH | Entry says "**Step.** None — fixed in DenialPrefixRosterTest, which now asserts both token kinds" | Entry also corrects its own figure (3 -> 4 constant hits); nothing to schedule |
| **E240** | HIGH | Option measured and rejected: src/Cli/HeadlessPermissionPrompt.php:178 "## The pair of lines, and why the terse one stays (E240)" — the no-tty arm doubles too, so dropping shape 4 fixes neither arm |  |
| **E244** | HIGH | Conditional deferral: "When Runtime acquires a proc_open, decide then"; src/Runtime.php's only two proc_open occurrences (L447, L532) are doc-block prose, not calls | stderrWritesIn() at tests/Cli/NonInteractiveRefusalDocumentTest.php:673 is still regex-based, as the entry says it should stay for now |
| **E248** | HIGH | entry text: "Step. None - recorded so the entry second paragraph is not acted on" | backlog correction to E238; nothing to schedule |
| **E249** | HIGH | entry text: "Step. None - the pair STAYS"; tests/Cli/RefusalStderrSurfaceTest.php exists and pins both arms | decision recorded; reopening needs a new measurement |
| **E253** | HIGH | entry text: "Step. None on Chat/App."; tests/Chat/HostedRuntimeNoticeWakeTest.php:171 exists | entry correction to E223; residue already pinned |
| **E264** | HIGH | entry text: "Step. None proposed. Leave the assignment; do not spend a fixture on it."; src/Agents/WorktreeManager.php:1159 assignment still present as intended |  |
| **E268** | MED | entry text: "Not done here: it would rewrite ~70 rows across two files for a guarantee the measured matrix already supplies"; providers still 3-column (tests/Cli/StderrEmitterCensusTest.php:1743) | option measured and rejected; the compensating per-channel guard exists at :2046 |
| **E271** | HIGH | entry is a re-derivation confirming E231 at zero; "E225 not acted on, as recorded" | informational; nothing to schedule |
| **E275** | HIGH | tests/Cli/BootstrapTranscriptSeamCallSiteCensusTest.php:729 countSeamCallSites(?string $source = null) still has no mode/channel argument, so the entry conditional has not triggered | conditional step; re-check if that scanner grows a mode |
| **E277** | HIGH | entry is a correction of a round-49 brief figure ("Both figures are higher ... the brief quoting a stale pair") | Process/measurement record; no code change implied |
| **E278** | HIGH | "Severity: informational. Measured." — records that E232 guard had to use a synthetic positive | Reusable-lesson entry; nothing to schedule |
| **E280** | HIGH | entry says "No action is required today"; row still live at tests/Support/DuplicatedTestHelperDriftTest.php:149 | awaitPromise now has 5 copies (CommandBackendTest, StreamingCommandBackendTest added) all at 10.0, so the 10.0-vs-15.0 row is still accurate |
| **E284** | HIGH | "Three figures retracted" — all three corrected in place with generators | Process rule (derive every element of a measured set); no scheduled code change |
| **E294** | HIGH | doc-accuracy correction of E244; re-verified: src/Runtime.php has zero real proc_open/popen/shell_exec/passthru/system calls (both grep hits are doc-block prose) |  |
| **E299** | HIGH | entry says "Step. None; recorded because the class of error is worth remembering" | Entry body says "the $GLOBALS line is GONE"; that sentence is now stale — round 51 re-shipped the descriptor replacement and tests/bootstrap.php:539 parks the handle in $GLOBALS again. The measurement itself still stands |
| **E303** | HIGH | entry says "Step. None outstanding."; the replacement census test tests/TtyStreamArgumentCensusTest.php is in the tree | Methodology record |
| **E316** | HIGH | "Severity: informational, closing an open question" - entry answers E299 open question, no Step | Entry says the $GLOBALS line "is gone and nothing replaced it"; E310 re-added one at tests/bootstrap.php:539 - snapshot stale, nothing to schedule. |
| **E317** | HIGH | "Step. None beyond remembering it." |  |
| **E320** | HIGH | "Step. No code change." - vendor/ is gitignored and deliberately not rostered |  |
| **E326** | HIGH | "Severity: process" - a retrospective about brief assembly, no Step |  |
| **E333** | MED | "Do NOT chase a mechanism from one observation" + a conditional next-time capture recipe | Unexplained flake with no reproducer; nothing schedulable until it reds again |
| **E334** | HIGH | "Step. None. Recorded so the next brief writer does not re-derive the same swap" | candy-mosaic/tests/DetectClosedStdinTest.php confirmed present; no candy-palette/src/Probe/Detect.php exists. |
| **E335** | HIGH | "Severity: evidence, NOT mechanism ... it did not reproduce"; Step is conditional ("If anyone ever needs to justify ...") |  |
| **E337** | HIGH | entry says "**Step.** None." and is a self-refutation ("WHAT IS TRUE... git ls-files returns nothing") |  |
| **E344** | HIGH | entry is a correction of an already-fixed entry; its stated deliverable tests/Hooks/AuditHookConcurrentAppendTest.php exists |  |
| **E350** | HIGH | entry states "No action wanted today"; both amendments visible at tests/Hooks/AuditHookTest.php:469 and :625-629 |  |
| **E354** | MED | entry is a corrected measurement plus practice rules; its only code option (c) is conditional and no per-file-census registry guard exists in tests/ |  |
| **E364** | HIGH | "Step. At merge, ... recompute the expected assertion figure" - a supervisor merge procedure, no code change |  |
| **E371** | HIGH | entry says "Step. None as a code change." | methodology note about pgrep -f only |
| **E372** | HIGH | entry says "Step. None wanted. Re-open only if a build target appears where the temp directory can be networked" | cited pin is now named testEveryWriteInTheClassCarriesBothFlagsInItsOwnArgumentList (AuditHookConcurrentAppendTest.php:282), not testTheWriteStillCarriesBothFlags - stale method name in the entry |
| **E374** | HIGH | premise correction of E346 with no Step section | record only |
| **E377** | MED | entry: "Nothing in production reaches 116 characters here, which is why this is recorded rather than fixed"; Step is conditional | described shape still present at sugar-crush/src/Hooks/BuiltIn/AuditHook.php:557 (remedy is still the last clause) |
| **E379** | HIGH | entry: "No code change wanted" / "Step. None on the code." | one-off build-box cleanup only |
| **E380** | MED | named fix present: sugar-crush/tests/Cli/BootstrapSkillSkipsTest.php:95 CHILD_WALL_CLOCK_BUDGET_SECONDS=20, :119 headroom, :639 testAnAbortAtThatCeilingStillFailsTheRun() | entry says it does NOT close the stray-SIGALRM hypothesis, but that residue is a diagnosis heuristic, not a schedulable code change |
| **E384** | HIGH | a recorded measurement (cost table at e8227070) with no Step section | table is sha-stamped and must be re-derived, not adjusted |
| **E393** | HIGH | process rule ("on ANY stage failure, git log <base>..HEAD before deciding what to re-run"), no code change |  |
| **E394** | HIGH | process rules (read live lane briefs before out-of-band dispatch; pick one implementation whole), no code change | includes an explicit instruction NOT to record the ~136-orphan figure as inflated |
| **E395** | HIGH | entry is a refutation of E368s Step for site 3 plus a record of the shipped fix; no Step of its own | shipped fix verified present: candy-core/src/Util/Tty/PosixBackend.php:174 openDeviceDescriptor + :275 declaration |
| **E397** | HIGH | entry: "was not judged worth the API surface for a two-line finally"; size() at candy-core/src/Util/Tty/PosixBackend.php:89 still takes no device-path param, finally still at :183 | Structural unpinnable invariant, step explicitly declined; nothing to schedule |
| **E400** | MED | Recorded measurement; instrument committed at candy-core/tests/Util/Tty/DescriptorSinkScanner.php + DescriptorSinkArgumentCensusTest.php:1212 presentLibraries() | Residual: presentLibraries() still globs */src only, so bin/ tools/ scripts/ remain unguarded; the stated reason for deferring (E404 merge hazard) is gone now that E404 is closed |
| **E408** | HIGH | entry: "This is a recorded SURVIVING mutation, written down so nobody re-derives it as a hole"; sugar-crush/src/Sessions/BackgroundSupervisor.php:290 reapIfExited($proc) and tests/Sessions/BackgroundSupervisorReapTest.php:586 |  |
| **E410** | MED | Drift resolved and guarded by sugar-crush/tests/Support/DuplicatedTestHelperDriftTest.php:372/:241; entry is explicitly "informational" | Residual named in the entry is still open: pidFile()/selfReportedPid() are still copied in FOUR files (tests/LSP/LspConnectionShutdownTest.php, tests/ClaudeCodeMcpClientShutdownTest.php, tests/Sessions/BackgroundSupervisorReapTest.php, tests/Providers/ClaudeCodeProviderStreamReapTest.php) and nothing was extracted to tests/Support/ |
| **E416** | HIGH | entry: "Severity: process / merge hazard. NOT A CODE FINDING"; the durable part is a Step for how to write the next round's brief |  |
| **E427** | MED | entry text: "STEP: per-round directories ... Round 54 uses r54a/r54b/r54c" | harness-only: the lane script lives outside the repo; nothing in the tree to change or verify |
| **E428** | MED | grep -rn "pgrep -af 'php -S'" hits only docs/plans/crush_code_worklog.md:337 | instrument spelling for the supervisor's invariant block; no repo code involved |
| **E433** | MED | entry header: "Severity: merge hazard. Stated, not fixable." | standing cross-lane hazard; the failure text already carries the resolution |
| **E442** | MED | entry text: "this is explicitly not a 'deduplicate them' step. It is a note" | hygiene note only |
| **E446** | HIGH | entry header: "Severity: finding, deliberately not implemented ... Recorded so the next reader does not re-derive it" | refutes E417; option measured and rejected |
| **E449** | MED | entry text: "This is a scope decision, not a defect, and it should not be settled inside a guard's doc-block" | needs a one-time scope ruling; couples to E448 if the answer is "executable" |
| **E450** | MED | STEP is a recovery-brief wording rule for the supervisor's harness; no repo file named | structural process finding, will recur on every interruption |
| **E451** | MED | STEP is a measurement precondition ("zero orphans with no suite running"); no repo artifact holds the invariant | sharpens E428 |
| **E452** | MED | STEP: "do not reach for resumeFromRunId on a pipeline() round" - harness recovery procedure |  |
| **E454** | MED | STEP is a restore procedure ("restore in ONE atomic pass ... never re-run --fix between the append and the update") | no idempotency fix was ever scheduled inside check-path-repos.php --fix; the hazard is still live if someone re-runs it |
| **E463** | HIGH | entry says "Severity: process. Stated, not fixable by a lane." |  |
| **E470** | HIGH | entry falsifies E439; pin exists at sugar-crush/tests/MCP/StdioMcpServerWriteBoundsTest.php:247 | recorded measurement, no code change owed |
| **E471** | HIGH | sugar-crush/src/MCP/StdioMcpServer.php:186 "THE LIVENESS HALF ... IS DORMANT BY"; pin StdioMcpServerWriteBoundsTest.php:511 | dormancy documented and pinned as the entry describes |
| **E478** | HIGH | entry says "STEP: none required today. Record it so the next person ..." |  |
| **E489** | HIGH | entry: 'Recorded because the DEMONSTRATION is reusable, not because the defect is still open' | fix confirmed live anyway at sugar-crush/tests/Support/DescriptorInheritanceGuardTest.php:238-249; only a soft 'check other rosters' recommendation remains |
| **E492** | MED | subject files crush-round-56.js / crush-round-56-recover.js are not in the tree (find returned nothing); entry says 'Fixed in the recovery script' | process finding about round-orchestration scripts outside the repo; the only durable output is the prose rule about re-reading a prepped brief at launch |
| **E503** | HIGH | entry: 'E475's severity claim holds, measured, and is now pinned' plus 'NOT FIXED: fd 2 is still unread for the whole idle gap' | INDEPENDENTLY CONFIRMED: E503 does re-measure E475 as NOT FIXED. Pin is live at tests/MCP/StdioMcpServerStderrDrainTest.php:275,526-546; StdioMcpServer only drains fd2 at :807 (write loop) and :1014 (refill), never between exchanges - E475/E440 remain genuinely open |
| **E516** | HIGH | entry: 'MEASURED, not fixed - there is nothing in the tree to fix' | INDEPENDENTLY CONFIRMED: E516 does re-measure E457's grep -qv headline as FALSE, and I re-ran the audit - /usr/bin/grep for grep -qv\|-vq\|-q -v\|-v -q\|--files-with(out)-match across .github, scripts, tools finds ZERO; the only hits are prose in docs/plans/*.md. E457's headline should be treated as retracted |
| **E524** | HIGH | entry says "MEASURED, NOT BUILT" and "Why it is filed rather than fixed" | leaves an unowned handoff: no CURLOPT_PROGRESSFUNCTION/progress option anywhere in src/Providers/ or src/Runtime.php |
| **E527** | HIGH | entry is "Filed as a fact about this package"; src/Backend/ObservesReasoning.php exists as the named answer |  |
| **E528** | MED | EngineBackendTest.php:859 and ReasoningProgressTest.php:737 both still declare runOnScaledClock; duplication taken deliberately | entry says "noted in both" - the note is only in EngineBackendTest.php:845-854; ReasoningProgressTest has no such note |
| **E533** | HIGH | entry: "Corrected before it was committed as final" - process finding | both catches keep distinct meanings at src/MCP/McpClient.php:194 (report) and :200 (silent) |
| **E536** | HIGH | entry: "Measured, three takes. Documented, deliberately NOT changed" | src/ClaudeCodeMcpClient.php:128 WRITE_IDLE_SECONDS still 15.0 as intended; tests/MCP/McpWriteBudgetShapeTest.php exists |
| **E537** | MED | entry: "Verified in the tree ... Do not reopen this against StdioMcpServer or LspConnection"; lane O shipped the bounded drain + public `pumpStderr()` seam (master `70461bd2d`) | Re-stamped ROUND 62 (lane Y): stays NO-FIX, STEP blocked-on RETARGETED — (a) loop-register parent fd2 and (b) child-side pump are PERMANENTLY rejected on the process model (forked children inherit the long-lived pipes; two destructive readers; no in-lane parent safe point). Residual between-turns parent drain = a Chat.php turn-boundary lane calling `pumpStderr()` on owned connections. |
| **E539** | HIGH | entry: "Severity: census correction. Measured." - no code change asked | tests/LSP/LspConnectionStdinWedgeTest.php:1318 sleep(120) confirms the corrected figure |
| **E550** | HIGH | "this round does NOT distinguish the leaked-timer fix closed it from it did not happen to fire" - recorded measurement, no code change | Closing needs ~230 clean takes; a measurement task, not a fix |
| **E558** | HIGH | "Both halves of the finding refuted with measurements" - E528 retracted as a seam, not a drift | sugar-crush/tests/Backend/ScaledClockHelperSeamTest.php exists and visibility is no longer part of the question (:123) |
| **E559** | HIGH | "The round brief states ... False in both halves" - retraction of the E527 premise | sugar-crush/tests/Backend/BackendContractWideningTest.php exists as the promised measurement |
| **E561** | HIGH | "Round 58, lane a. Reported, not resolved." - a fact about a round brief, no tree change | Process finding: map/file-list reconciliation rate 2/6 |
| **E562** | MED | "a verdict it cannot compute must be a DISCARD" - a fix to a per-session mutation harness that is not in the repo | grep -rl "No tests executed" over sugar-crush/tools/scripts hits only vendor PHPUnit; harness is scratchpad-only |
| **E573** | HIGH | git ls-tree 535d721ff sugar-crush/tests/ \| grep -i stacked returns nothing - confirms the claim; a fact about a round brief | sugar-crush/tests/StackedDocCommentTest.php still does not exist at HEAD either |
| **E574** | HIGH | "Harmless here because the missed row needed no fix" - a correction to a brief distribution, no code change |  |
| **E584** | HIGH | entry phrase: 'Measured compliance with a RED before-anything-else instruction: 0 of 1 ... The rate is the part worth keeping' | process finding; consequence became rule 50 (see E593) |
| **E586** | MED | entry phrase: 'The count is taken in Python now' / 'A signal death ... is now named as such' — mutation harness, not a repo file | harness lives outside the repo; not verifiable from tree, entry records it already fixed |
| **E592** | HIGH | entry phrase: 'The standing consequence: a brief item that ASSERTS A MECHANISM must cite the measurement or say it is unverified' | process finding; rule-47 rate now tracked at RESUME:576-579 |
| **E593** | HIGH | entry records 240 clean takes and states 'WHAT THIS DOES NOT SAY: that E490 is fixed'; names no code change | RESUME:511/583 still lists E490 as answered-not-closed and asks for 60 more takes — a measurement, not a code item |
| **E594** | HIGH | entry phrase: 'NOTHING IS LEFT UNVERIFIED, and that is a stronger result than the audit expected' | audit completed; only recorded artefact is the awk generator in the entry |
| **E595** | HIGH | entry corrects E562's mechanism statement by measurement; 'what this round's harness carries' already refuses No tests executed |  |
| **E596** | HIGH | entry phrase: 'The generator is f59a_apply.py, invoked by the harness rather than copied into it' — scratchpad harness, not a repo file | records E586's pin as already taken |
| **E597** | HIGH | entry phrase: 'The entry is CONFIRMED and now carries its scope' — E408 re-measured SURVIVED at whole-suite scope | general shape became standing rule 53 at RESUME:566-568 |
| **E601** | HIGH | entry records a handoff mis-measurement (159286 was a mutated failing run's total; tree is 10274/159290) and three handoff facts; names no code change | recorded process finding about restart briefs |
| **E613** | MED | entry is a recorded measurement: "466 files scanned, 0 hits"; no code change prescribed | No transitive-assert guard was built; cross-file/parent/trait expansion still uncovered |
| **E614** | MED | tests/Skills/SkillLoaderTest.php:55/:68 suppressErrorLog()/restoreErrorHandler() still declared with zero callers; entry says "a guard here would be pinning a property the tree already has" | Dormant seam persists exactly as measured |
| **E618** | HIGH | FrameCapFamilyTest.php:1082-1120 testTheConstantReaderIsAliveInBothPolarities asserts isPublic() and states the lazy-evaluation mechanism | Recorded measurement, nothing to schedule |
| **E621** | MED | entry is a refutation of the round-59 review: "nothing lost coverage", attribution measured via --log-junit | Its one recorded suggestion is undone: SymbolCitationDriftTest.php:144-150 root() still asserts on every invocation |
| **E623** | MED | entry is a recorded campaign measurement plus a decision: "Recommended disposition: (ii) KEEP OPEN, with the pooled bound recorded" | Supervisor action: mark E490 1-in-76 SUPERSEDED, record 0.5553%, decide the +60 takes explicitly |
| **E624** | HIGH | retraction of a brief claim; ChildWallClockBudgetTest.php:96-104 already reports unresolved and reads encapsed-string tokens, as the entry says | E585 confirmed already fixed |
| **E626** | HIGH | entry is a settled measurement ("28 tests, 136 assertions, rc 0. The brief is correct") |  |
| **E630** | HIGH | candy-pty/composer.lock present, 115257 B, gitignored via candy-pty/.gitignore:1 (git check-ignore confirms) | Lock is now dated 2026-09-10 and 115 KB (entry said 113 KB / 2026-08-22) - it has been regenerated since, i.e. someone did run composer in that dir |
| **E634** | HIGH | entry says "No code fix; retract the instruction" - do not cite a constant per-file assertion delta |  |
| **E635** | HIGH | option measured and REJECTED; the narrow alphabet and the headroom measurement are documented at tests/Tools/BuiltInToolCorpusTest.php:873-885 |  |
| **E638** | HIGH | entry text: "**Step.** No fix. Re-measure the table if MAX_SECTION_BYTES or MAX_SOURCE_FILES moves" | all three bounds still present at BuiltInToolCorpusTest.php:752/760/776/787, so the recorded measurement is not stale |
| **E640** | MED | GRANT_PROBES at tests/Agents/AgentDefinitionTest.php:381 still holds only the one can-fire probe; no refuse table | conditional defer: only argument-scoped preset decl is still Bash(git *) (AgentDefinition.php:86), so over-fire cannot arise; re-open if a preset ships another argument-scoped grant |
| **E648** | MED | round-60 fix verified: src/Agents/ProcessExecutor.php:597-599 autoloadPath() returns BackgroundSupervisor::autoloadPath(); READ_PATHS PROCESS_DERIVED rows still present in ReadPathCensusTest | residual is a watch note (re-open if the worker protocol gains a second writer); nothing to schedule now |

