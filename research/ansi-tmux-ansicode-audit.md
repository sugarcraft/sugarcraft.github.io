# ANSI conformance audit — SugarCraft vs tmux `tools/ansicode.txt`

> Research-only audit (docs track). Reference: tmux [`tools/ansicode.txt`](https://github.com/tmux/tmux/blob/master/tools/ansicode.txt)
> (779 lines, fetched 2026-09-15). Every **WRONG** finding below was confirmed by executed `php -r` probes against the
> checked-out sources; observed bytes are quoted inline. Companion detail table (every sequence family):
> `/home/sites/sc-briefs/ansicode-findings.md` (audit brief artifact).
> Scope: [candy-ansi](../../candy-ansi), [candy-vt](../../candy-vt), [candy-core](../../candy-core),
> [candy-input](../../candy-input), [candy-palette](../../candy-palette), [candy-freeze](../../candy-freeze),
> [sugar-spark](../../sugar-spark). No `src/` or `tests/` file was modified by this audit.

## Top defects (ranked by user-visible impact)

> **Status re-verified against master `6d135b634` (probes executed 2026-09-16).**
> The wave-4/wave-5 tracks landed after this section was written, so every claim below was
> re-derived by executing throwaway `php` probes against current master (worktree isolation,
> hard-linked vendors, `ScreenHandler`/`Parser`/`EscapeDecoder`/`Palette`/`Ansi` driven through
> their public APIs). Each claim keeps its original 2026-09-15 wording; a dated marker is
> appended in place:
> **✅ FIXED** — the probe contradicts the claim (fixing PR number **and** merge SHA quoted, plus the deciding observed value);
> **⚠️ STILL TRUE** — the probe re-confirms the claim on this master, so it is live and not stale;
> **◐ PARTIAL** — the claim splits; the fixed half is cited and the live half re-confirmed.
> Every cited SHA was checked with `git merge-base --is-ancestor <sha> master`.
> This re-verification changes **no `src/` behaviour**; the only code it adds is
> `candy-vt/tests/Handler/DecalnResetPinTest.php` (pinning DECALN's last two unpinned resets,
> which is why defect #34's marker below can now point at a test rather than only at a probe).

### Real bugs — wrong emitted or wrong consumed bytes

| # | Defect | Lib · evidence | Severity | Fix |
|---|--------|----------------|----------|-----|
| 1 | **DECAWM defaults OFF** (VT100 boots with autowrap ON, ansicode:460). 30 chars into a 20-col screen: chars 21–30 are silently **discarded** (observed: row 0 filled, cursor `0,19`, tail gone). Every full-width TUI repaint clips its right edge. | [candy-vt](../../candy-vt) `src/Mode/Mode.php:44` + `src/Handler/ScreenHandler.php:94` | Critical | `bool $autoWrap = true`; set on RIS/DECSTR. ✅ **FIXED (PR #1417, merge `3ae78d4a3`)** — probe 2026-09-16 on `6d135b634`: `mode->autoWrap` default = `true`; 30 × `X` into 20 cols → row 0 `XXXXXXXXXXXXXXXXXXXX`, row 1 `XXXXXXXXXX`, cursor `1,10`, 30 glyphs on screen, none discarded |
| 2 | **No deferred (pending) wrap** — both engines advance the cursor eagerly when printing at the right margin: `ESC[1;20H`+`X` on 20 cols leaves cursor at `1,0` (xterm: `0,19` with wrap pending). CPR replies, overstrike repaints (`…\r`+`CR`+`B`) and status bars diverge from real terminals; in the vcr engine the wrap fires even with `?7l`. | [candy-vt](../../candy-vt) `src/Handler/ScreenHandler.php:154-162`, `src/Parser/CsiHandlerImpl.php:108-119` | Critical | add `wrapPending` to `Cursor`, consume on next printable, clear on cursor motion. ✅ **FIXED in both engines** — probe 2026-09-16: TEA `ESC[1;20H`+`X` → cursor `0,19` with `wrapPending=true`, next `Y` → cursor `1,1`, cell(1,0)=`Y`; vcr renderer (`Vt\Terminal`) is byte-identical (`0,19` → `1,1`), and with `?7l` it now clamps at `0,19` leaving row 1 blank instead of wrapping. TEA half PR #1417 (merge `3ae78d4a3`), renderer half PR #1420 (merge `e8b03ca30`) |
| 3 | **`ESC c` (RIS) and `CSI !p` (DECSTR) unimplemented** — probe `"ABCDEF\x1bc"` → screen still `ABCDEF`. An app reset leaves corrupted state (modes, palettes, regions) behind. | [candy-vt](../../candy-vt) `src/Handler/ScreenHandler.php:271-280`; grep: zero RIS code in lib | Critical | implement RIS + DECSTR (also fixes #1's persistence). ✅ **FIXED (PR #1417, merge `3ae78d4a3`)** — probe 2026-09-16: `"ABCDEF" + ESC c` → grid `          \|          \|          ` (all blank), cursor `0,0`; DECSTR (`ESC[?7l` + `ESC[2;4r` + `AB` + `ESC[!p`) → `autoWrap` back to `true`, cursor `0,0`, margins left at `1..2` — DECSTR deliberately keeps DECSTBM/charsets/tab stops, the reset matrix owned by `candy-vt/tests/Handler/ResetTest.php`. ⚠️ Scope note: this is the TEA `ScreenHandler`; `ESC c` is still a no-op in the vcr renderer, see #31 |
| 4 | **candy-input poisons the input stream on any private CSI reply** — `decode("\x1b[?1;2c")` returns 0 events and *keeps buffering*; the next `decode("hello")` also returns 0 events (observed `remainder()` = 12 bytes; everything discarded at the 128-byte cap). DA1/DECRPM/kitty-flag/OSC-4 replies hang the keyboard. | [candy-input](../../candy-input) `src/EscapeDecoder.php:366-373,459,250-253` | Critical | parse and swallow `CSI ? … final` replies instead of gating on a CPR-shaped regex. ✅ **FIXED (PR #1416, merge `40e5d5d0d`)** — probe 2026-09-16: `decode("\x1b[?1;2c")` → `[Reply(family=device-attributes, raw="\x1b[?1;2c")]` with `remainder()` = 0 bytes, and the *next* `decode("hello")` → 5 `Key` events (audit observed 0 events + a 12-byte remainder) |
| 5 | **X10 mouse reports decode as keystrokes** — `"\x1b[M".chr(32).chr(35).chr(37)` → `Key(' ')`, `Key('#')`, `Key('%')` (observed). Mouse mode 1000 turns every click into printable-character spam. | [candy-input](../../candy-input) `src/EscapeDecoder.php:341-377,542-543,627` | Critical | implement `ESC [ M` 3-byte payload branch. ✅ **FIXED (PR #1416, merge `40e5d5d0d`)** — probe 2026-09-16: `"\x1b[M".chr(32).chr(35).chr(37)` → `[Mouse(press,btn=0,3,5)]`, zero `Key` events (audit observed `Key(' ')`, `Key('#')`, `Key('%')`) |
| 6 | **kitty keyboard decoder inverted** — real frames are `CSI code;mods u` (no `?`); they are parsed *only* behind a `?` gate: `"\x1b[97;5u"` → *no event* (observed), while `CSI ? u` *queries* are instead executed as SCO restore-cursor on the emulator (probe: kitty query reply teleports candy-vt cursor from `4,6` to `1,2` — also `HandlerAdapter.php:83-84` / `ScreenHandler.php:199` prefix-blindness). | [candy-input](../../candy-input) `src/EscapeDecoder.php:459-470`; [candy-vt](../../candy-vt) | Critical | decode non-`?` `u` finals as kitty events; gate cursor `s`/`u` on `$prefix===0`. ◐ **PARTIAL** — decoder half ✅ **FIXED (PR #1416, merge `40e5d5d0d`)**: probe `decode("\x1b[97;5u")` → `[Key(a,mods=4)]`, i.e. a real un-`?` kitty frame now yields an event. Emulator half ⚠️ **STILL TRUE on `6d135b634`**: `ESC[9;9H` + `ESC[?s` + `ESC[1;1H` + `ESC[?u` → cursor `8,8` (a prefix-gated `s`/`u` would leave it `0,0`), i.e. the private-mode queries still execute as SCO save/restore — `ScreenHandler::csiDispatch()` `case 's'` / `case 'u'` never inspect `$prefix`. ✅ **FIXED (PR #1438, head `6d0ffc4b4`; merge pending)** — both cited halves gated on `$prefix === 0`: the emulator (`ScreenHandler::csiDispatch()`) AND the vcr engine adapter this row cites at `HandlerAdapter.php:83-84` (the prefix cannot reach `CsiHandler::scosc()`/`scorc()` — they take no marker argument — so the gate must live in the adapter; +1 candy-ansi mock test). Probe 2026-09-16: `ESC[9;9H` + `ESC 7` + `ESC[1;1H` + `ESC[?u` → cursor stays `0,0` and the ESC 7 slot survives; `CSI s`/`CSI u` unmarked round-trip unchanged. `CSI ? u` is now inert, not answered — no kitty reply channel, by design |
| 7 | **CPR on row 1 fabricated as a key** — `"\x1b[1;20R"` → `Key(F3, SHIFT|ALT)` (observed; row ≥10 correctly swallowed). Every `ESC[6n` reply that vim-style apps receive injects a phantom Alt+Shift+F3. | [candy-input](../../candy-input) `src/EscapeDecoder.php:547-575` (`'R'=>'F3'`) | Critical | require `R` to be non-keystroke when params are present. ✅ **FIXED (PR #1416, merge `40e5d5d0d`)** — probe 2026-09-16: `"\x1b[1;20R"` → `[Reply(family=cursor-position)]`, no `Key(F3)` (audit observed `Key(F3, SHIFT\|ALT)`) |
| 8 | **candy-palette corrupts colours on degrade** — capture-group mis-index: `ESC[38;2;200;100;50m` → `ESC[38;5;162m` (true value 173); `ESC[38;2;0;205;0m` (green) → `ESC[34m` (**blue**, observed). Plus: every `38;5;n` round-trips through `fromAnsi256Index()` which has no idx<16 branch → `ESC[38;5;9m` (bright red) → `ESC[38;5;16m` (**black**, observed). | [candy-palette](../../candy-palette) `src/Palette.php:261-270`, `src/Color.php:205-215` | Critical | fix group indices; add 16-colour table branch. ✅ **FIXED (PR #1419, merge `48014ef17`)** — probe 2026-09-16: ANSI256 `38;2;200;100;50` → `38;5;167` (the audit's "173" was naive linear rounding; 167 is the nearest cube vertex) and ANSI `38;2;0;205;0` → `32m` **green**, not `34` blue; `38;5;9` stays `38;5;9` under ANSI256 (no black `16`) and → `91m` under ANSI; `1;4;38;2;255;0;0` → `1;4;38;5;196` so bold+underline survive the rewrite |
| 9 | **`Ansi::strip()` / `Sanitize::untrusted()` blind to DCS/APC/PM/SOS and 8-bit C1** — `strip("A\x1bP0;1;0q\"1;1;2;2~abc\x1b\\B")` → `A0;1;0q"1;1;2;2~abcB`; `strip("a\x9b31mb")` byte-identical to input (observed hex `619B33316D62`). Consequences: `Width::of()` returns 21 instead of 2, `Width::truncate()` emits payload text, and `untrusted()` (documented "safe for terminal output") passes tmux DCS bodies through: `untrusted("A\x1bPtmux;echo\x1b\\B")` → `Atmux;echo\B` (observed). Companion bypass: a lone C1 byte makes the `/u` preg fail and the `??` fallback lets **all** C0 controls survive — `untrusted("a\x9b\x07b")` → `a\x07b` (hex `610762`). | [candy-core](../../candy-core) `src/Util/Ansi.php:642-693`, `src/Util/Sanitize.php:111`, `src/Util/Width.php:131,165,193,300` | Critical (security-adjacent) | rewrite strip over the candy-ansi state machine; pre-normalize/strip C1 in untrusted(). ✅ **FIXED (PR #1418, merge `028b6dbe5`)** — probe 2026-09-16: `strip("A\x1bP0;1;0q\"1;1;2;2~abc\x1b\\\\B")` = `"AB"`; `strip("a\x9b31mb")` = hex `6162`; `untrusted("A\x1bPtmux;echo\x1b\\\\B")` = `"AB"`; `Width::of("\x1b[31mhello\x1b[0m")` = 5. The lone-C1 bypass is gone too: `untrusted("a\x9b\x07b")` = hex `61` — the BEL is gone and the `b` is consumed as the *final* of the CSI that `\x9b` opened (a terminal would ring the bell there; a sanitizer discards it), so the audited "all C0 controls survive" (`610762`) bypass is gone |
| 10 | **`Ansi::kittyGraphicsBegin()` emits DCS `ESC P q`** — observed `1B5071613D742C663D313030…`; ansicode:277 defines `Pq` as **SIXEL**, so the header is byte-identical to a DECSIXEL start and chunks (`kittyGraphicsChunk()` returns a bare unframed `m=1,AAAA`; `kittyGraphicsEnd()` a lone ST) never live in a string context. Real protocol is APC `ESC _ G` — which `kittyGraphicsClear()` correctly uses (`1B5F47`). | [candy-core](../../candy-core) `src/Util/Ansi.php:743-810` | Critical | frame each chunk as `ESC _ G a=d,f=100;b=<m>;p=<id>;<b64> ESC \`. ✅ **FIXED (PR #1418, merge `028b6dbe5`)** — probe 2026-09-16: `kittyGraphicsBegin($opts)` hex now opens `1B5F47` (`ESC _ G`, APC) and ends `…2C6D3D313B1B5C` (`,m=1;` + ST); `kittyGraphicsChunk('AAAA', true)` = `1B5F476D3D313B414141411B5C` (self-framed `m=1;AAAA` + ST), `kittyGraphicsEnd()` = `1B5F476D3D303B1B5C`, `kittyGraphicsClear(5)` = `1B5F47613D642C693D351B5C`. No `1B50` (`ESC P`) introducer remains anywhere in the `kittyGraphics*` family — `Ansi::DCS` is still used, correctly, only by the sixel emitter (`Ansi.php:1238`) and the generic `dcs()` helper (`:1370`) |

### Also-real, one tier down (condensed)

- **IL/DL undelivered in the TEA emulator** (`ScreenHandler.php:198-212` switch omits `L`/`M`; probe `ESC[1L` → grid unchanged) — VT100 *baseline* (ansicode:363-364, 769-771). **Major** (candy-vt) ✅ **FIXED (PR #1417, merge `3ae78d4a3`)** — probe 2026-09-16: `ROW0..ROW3` then `ESC[1;1H ESC[1L` → `"|ROW0|ROW1|ROW2"` (blank row 0 inserted, rows shifted down), `ESC[1M` shifts them back up (region was full screen here); both finals reach `insertLines()`/`deleteLines()` from the wire.
- **vcr path is a second, weaker engine**: `HandlerAdapter::escDispatch()` empty → ESC 7/8/D/E/M/H/c all no-ops; DECTCEM **inverted** (`?25l` → visible=1, observed) and prefix-blind; `cup()` clamps rows into DECSTBM even with DECOM off (`ESC[4;6r`+`ESC[1;1H`+`X` → row 3, observed); `38;2` truecolor **wipes bold+underline** (`ESC[1;4;38;2;255;0;0mA` → attrs=0, observed). **Critical** per-item (candy-vt `src/Parser/CsiHandlerImpl.php:108-119,146-167,240-252,307-319`, candy-ansi `HandlerAdapter.php:83-91`) ◐ **PARTIAL on `6d135b634`** — three of the four sub-claims ✅ **FIXED (PR #1420, merge `e8b03ca30`)**, probed 2026-09-16 through the vcr renderer (`Vt\Terminal::new()`): DECTCEM is no longer inverted (`?25l` → `cursor->visible=false`, `?25h` → `true`); `cup()` no longer region-clamps with DECOM off (`ESC[4;6r` + `ESC[1;1H` + `X` → row `0`, was `3`); `38;2` no longer wipes rendition (`ESC[1;4;38;2;255;0;0m` + `A` → `attrs=5`, bold+underline both true, was `attrs=0`). ⚠️ The first clause is **STILL TRUE**: `candy-ansi/src/Parser/HandlerAdapter.php:114-116` is an empty `escDispatch()` body on master, so in the vcr engine `ESC 7/8/D/E/M/H/c` remain no-ops — probe: `ESC 7` at `4,4`, move to `0,0`, `ESC 8` → cursor `0,0`; `ESC M` at the top leaves row 0 = `A` unscrolled. (Deferred wrap and `?7l` *are* fixed on this path — see #2.)
- **DECSC/`CSI s` share one cursor slot and save no attributes** (probe: `ESC7`@9,9 → `CSI s`@1,1 → `ESC 8` lands 1,1, not 9,9). **Major** (candy-vt `Cursor/Cursor.php:14-22`) ◐ **PARTIAL on `6d135b634`** — "save no attributes" ✅ **FIXED (PR #1427, merge `0d5a53833`)**: probe `ESC[1;4m` + `ESC 7` + `ESC[0m` + `ESC 8` + `X` → cell `bold=true underline=true` (the general slot now snapshots SGR, G0-G3, GL and DECOM alongside the position). The shared slot is ⚠️ **STILL TRUE by design**: probe `ESC 7` at `11,11` → `CSI s` at `2,2` → `ESC 8` lands `1,1`, i.e. the `CSI s` snapshot overwrote the `ESC 7` one — `Cursor` has a single `savedRow`/`savedCol` pair, which is xterm's behaviour for the SCO alias in its default state (no DECSET 1048/1049 alt-save), and `saveCursor()` now documents it as such. A VT510-strict *separate* general/alt slot pair remains the only open half.
- **DECSTBM doesn't home the cursor**; **sub-region scrolls pollute scrollback** (region 2..4 + 5 LFs → scrollback=5, observed; SD also pushes); **`CSI 3J` dead branch**; **erases inside `?2026` sync are dropped** (`AB`+`2J` flush → row still `AB`, observed). **Major** (candy-vt `Handler/ScreenHandler.php:210-213,379-445`, `Handler/EraseHandler.php:81-83`) ✅ **ALL FOUR FIXED (PR #1427, merge `0d5a53833`)** — probes 2026-09-16:
  1. **DECSTBM homes** — `ESC[5;5H` + `ESC[2;4r` → cursor `0,0` with the region at `1..3` (and under DECOM it homes to the region top; an invalid region takes the early return and homes nothing).
  2. **Sub-region scrolls no longer pollute scrollback** — the claim's own sequence (`ESC[2;4r` then 5 × LF) now leaves `scrollback->count()` at **0**, and that is a real scroll rather than a no-op: on a 6-row screen with region `2..4`, `ESC[4;1H` + `GGGGGG` + LF produces `AAAAAA|CCCCCC|GGGGGG||EEEEEE|FFFFFF` — rows 1..3 rotated with a blank inserted at the region bottom while rows 0/4/5 stay fixed — and `CSI 1S` / `CSI 1T` on the region likewise push 0. Non-vacuous control: 10 lines on a 4-row **full-screen** term → scrollback **10**, so the gate is `regionIsFullScreen()`, not "never push".
  3. **`CSI 3J`** is live — scrollback 4 → 0 (`ScreenHandler::csiDispatch()` drains the ring; `EraseHandler::eraseInDisplay()` `case 3` documents that ED 3 has no grid effect).
  4. **Erases inside `?2026` survive** — `AB` + `CSI ?2026h` + `CSI 2J` + `CSI ?2026l` → grid blank (the claim's `row still AB` is gone); mid-window the grid still reads `AB`, proving the erase is *queued* rather than applied early, and `CSI 3X` (ECH) inside the window flushes to `"   DEFGH"`. Mechanism (found by source walk, since `grep syncUpdate` inside `EraseHandler` is empty by design): `ScreenHandler.php:446-457` (call site `:454`) passes `$this->pendingMutations` **by reference** into `EraseHandler::apply(..., ?array &$pending = null)` (`EraseHandler.php:40`, by-reference since its introduction in `0e47c8ee0`), which routes every fill through `putOrQueue()` — copying the queue into a call-site local was the historical drop.
- **SCS / DEC Special Graphics missing in every consumer** — `ESC(0jqx` prints literal `jqxk` (ansicode:222-249 baseline); `ESC # 8` DECALN **mis-executes as DECRC** (cursor teleport observed). **Major** (candy-vt, candy-freeze, sugar-spark) ✅ *candy-vt half since resolved* — `ScreenHandler::escDispatch()` now gates on the intermediate, `designate()`/`Charsets` translate G0-G3, and `ESC # 8` dispatches as esc(0x38,0x23) — and since PR #1431 `escDispatch()` routes that intermediate/final pair to `displayAlignmentTest()`, so the sequence now executes the alignment fill rather than being ignored or mis-executed as DECRC (the emitter→emulator round trip, and the inertness of the `ESC [ # 8` misquote, are pinned by `candy-vcr/tests/CoreEmitterRoundTripTest.php` and `candy-vt/tests/Handler/DecalnWireTest.php`); candy-freeze/sugar-spark left to their own track. ✅ *Re-verified 2026-09-16 on `6d135b634`* — probe: `ESC ( 0` + `jqx` → `┘─│` (DEC Special Graphics translated), `ESC # 8` → `EEEEEEEEEE` on every row. DECALN's two remaining resets (`singleShift`, `wrapPending`) had no DECALN pin until `candy-vt/tests/Handler/DecalnResetPinTest.php` (this PR), each proven non-vacuous by mutation.
- **SGR 58/59 + colon `38:2::` mis-consumed**: `ESC[58;5;9m` → blink+strikethrough with underline OFF (observed cell sgr) while candy-core *emits* 58 (`Color.php:523`); colon form → `foreground:null`. **Major** (candy-vt `Handler/SgrHandler.php:69,72,135-150`; root cause candy-ansi `Parser.php:280-308` colon-flattening) ◐ **PARTIAL on `6d135b634`** — the 58/59 theft ✅ **FIXED (PR #1420, merge `e8b03ca30`)** and the colon flattening ✅ **FIXED (PR #1415, merge `5fa294452`)**: probe `ESC[58;5;9m` + `X` → cell `underline=false, strikethrough=false, blink=false` (58 no longer bleeds into the attributes); `ESC[4:3m` → `underline=true, italic=false` (curly, not mis-parsed); `38:2:255:0:0`, mixed `38:2;255;0;0` and `38:5:9` all resolve. ⚠️ Still true: the *canonical* omitted-colour-space and colourspace-id spellings `38:2::255:0:0` and `38:2:16:255:0:0` leave `foreground = NULL` — a leading colourspace field in the colon form is still not consumed. ✅ **FIXED (PR #1438, head `6d0ffc4b4`; merge pending)** — `SgrHandler` and the renderer's `CsiHandlerImpl` now consume the existing `Parser::subparams()` flags: a five-slot group takes RGB at +2, a six-slot group skips the colour-space sub-parameter (accepted, ignored — xterm `charproc.c:2142-2146`, tmux `input.c:2362-2365`), malformed groups consume their slots and set nothing. Probe: `38:2::255:0:0` and `38:2:16:255:0:0` → fg `255,0,0`, byte-equal to the semicolon form; `48:`/`58:` equivalents pinned too (`candy-vt/tests/Handler/ColonExtendedColourTest.php`, `tests/Parser/CsiHandlerImplColonSgrTest.php`).
- **candy-input misc decoders**: SGR wheel `ESC[<64;10;5M` → phantom left-click (observed); `ESC[Z` Backtab swallowed; focus `\x1b[I` only when chunk-final; `ESC O M`/keypad absent; OSC/DCS replies leak as Alt-key spam (all observed). **Major** (candy-input `EscapeDecoder.php:417-431,357-362,547,309-326`) ✅ **FIXED (PR #1416, merge `40e5d5d0d`)** — probes 2026-09-16, every observed value: `ESC[<64;10;5M` → `[Mouse(scroll,btn=96,10,5)]` with no phantom left-click; `ESC[Z` → `[Key(Backtab,mods=1)]`; focus `ESC[I` *mid-chunk* (`\x1b[Ix`) → `[Focus(gained=true), Key(x)]`; `ESC O M` → `[Key(Enter)]`; OSC 10 reply → `[Reply(family=string)]` with no Alt-key spam.
- **candy-freeze/sugar-spark re-copy bugs**: colon SGR → corrupt 18-hex `#ffffffffffffffff50a0` (observed); `38;5;m` → white; frame width from `Ansi::strip` ≠ drawn text (phantom cols); sugar-spark rewrites input bytes (`ESC[4;3m` reported as `ESC[4:3m` "underline curly"), emits ghost `ESC\` after DCS, loses truncated CSIs (`parse("\x1b[31")` → 0 segments), and an aborted `ESC O` **steals the next printable** (`ESCOX` "SS3 X", observed). **Major** (candy-freeze `AnsiParser.php:201-223`, sugar-spark `AnsiHandler.php:156-286`)
  > **Update — six of the seven above are fixed** (wave-4 parsers track, sugar-spark + candy-freeze).
  > Colon sub-parameters now resolve through `Parser::subparams()` in candy-freeze
  > `SgrStateHandler::extendedColour()` (`:181`) and every spelling is pinned six-hex by
  > `AnsiParserColonSubparametersTest`; `38;5;m` falls back to palette 0 per ECMA-48 §5.4.1
  > (`testOmittedTwentyFiveColourIndexFallsBackToBlack`). In sugar-spark `AnsiHandler`
  > re-emits the input's own separators (`joinParams()` `:325`), consumes a DCS/OSC string
  > terminator instead of re-emitting a ghost `ESC \` (and the window is one byte wide, so a
  > later standalone `ESC \` is still reported), reports an unterminated tail at `finish()`
  > instead of dropping it (`parse("\x1b[31")` → one `truncated CSI` segment), and consumes
  > exactly one SS3 final byte `0x40–0x7E`, re-processing anything else as text
  > (`printChar()` `:237`). All pinned by `sugar-spark/tests/ByteFidelityTest.php`.
  > Still open here: frame width from `Ansi::strip` ≠ drawn text (phantom cols) — different
  > subsystem, untouched by that track. The `file:line` refs above are the audit's own
  > snapshot and predate that change.

### Unimplemented extensions (not bugs — safely ignored, listed for completeness)

DECCKM/DECCOLM/DECSCNM/DECARM/LNM/SRM modes · IRM insert mode · DECOM re-anchor on toggle · DECSCA/DECPRO protected cells · DECRQM/DECRPM + **any reply channel in candy-vt** (CPR/DA/DSR/XTWINOPS/kitty `?u` go unanswered — `feed(): void`) ✅ **superseded in part, probe 2026-09-16 on `6d135b634`** (PR #1417, merge `3ae78d4a3`): candy-vt now has a reply channel — `ESC[6n` → `ESC[1;1R`, `ESC[c` → `ESC[?62;1;6;22c`, `ESC[>c` → `ESC[>1;10;0c`, `ESC[5n` → `ESC[0n`, `ESC[14t` → `ESC[4;48;80t` on a 10×3 screen (`ESC[4;384;640t` on 80×24 — the reply is geometry, as specified) — queued in `ScreenHandler::$replies` / drained by `Terminal::replies()`. `DECRQM` is answered too: `ESC[?7$p` → `ESC[?7;1$y`, `ESC[?25$p` → `ESC[?25;1$y`, `ESC[?2026$p` → `ESC[?2026;2$y`, and an unmodelled mode reports status `0` (`ESC[?1$p` → `ESC[?1;0$y`, non-private `ESC[4$p` → `ESC[4;0$y`). Still unanswered there: only the kitty flag query (`ESC[?u`) · XTSETALT/ICON (OSC 1) + `CSI 21 t` emitters · OSC 4/10/11/12 **query-side consumption**, OSC 21/22/52-consume/1337 · Sixel/DECDMA (framing-only discard is safe) · DECSLPP/DECSHORP/DECREQTPARM/DECTTC/DECPRO/DECID/DECHTS/DECCAHT/DECSHTS (`u` is currently *mis-captured* as SCORC → bug) · CTC/vertical tabs · urxvt-1015/pixel-1016/modifyOtherKeys/kitty-flag negotiation in candy-input · **all terminal probing in candy-palette** (`checkEscapeQueries()` is comments-only; `TerminalProbe.php:241-283`) incl. sixel `infocmp` regex that never matches real output (`:227`) ✅ **P-7 FIXED (w4-palette):** dead `/\bsixel\b/` replaced with a name-position parser (`TerminalProbe::terminfoHasSixel()`) that accepts `Sixel#1`/`Sixel=true`/`SixelScreenMode`/`Ss1`/`Ss2` and rejects "sixel" buried in a `des=` value or comment; fixture-based tests added; `Mosaic::auto()` precedence untouched · ✅ `ESC c` (RIS), SCS (`ESC ( ) * + F`, G0-G3) and DECALN emitters **added in candy-core**
(`Ansi::ris()` / `scs()`+`scsG0()`-`scsG3()` / `decSpecialGraphics()` / `asciiCharset()` / `decaln()`, plus
`shiftOut()`/`shiftIn()`), round-trip-guarded by `candy-vcr/tests/CoreEmitterRoundTripTest.php`. Note the emitter
spells DECALN as `ESC # 8` (VT510/xterm), not the `CSI # 8` that candy-vt's
`ScreenHandler::displayAlignmentTest()` heading still carries — its own wire-level note documents the gap correctly,
and the stale label is left to the vt track. The CSI form never completes: a CSI final must be 0x40-0x7E, so candy-ansi
drops it to Ground undispatched while a real receiver swallows the next graphic.
Still missing in candy-core: a named `DECCOLM` constant — the bytes are already expressible via the generic
`Ansi::decSet(3)`/`decReset(3)` (`ESC [ ? 3 h`), what is absent is the 132-column mode itself, which no consumer
models (candy-vt's `Mode` has no DECCOLM field) · DECKPAM/DECKPNM emitters (`ESC =`/`ESC >`; `Ansi` has no generic
two-character-escape helper to spell them with, and candy-input still decodes both as Alt+=/Alt+>) · an OSC 4 *query*
emitter (candy-vt has no reply channel, so it could not be round-tripped).

### Deferred / out of scope

DECCRA/DECICE/DECFCRA, DECUDKR (not documented in ansicode.txt) · kitty/WezTerm/iTerm image *rendering* · a unified candy-vt single-engine refactor (the two-engine split is the root amplifier of items 1-2, 7) · running the 58-lib CI (docs-only track).

## Method

ansicode.txt walked requirement-by-requirement; each family probed through the library's public API (`Vt\Terminal\Terminal::feed`, `Vt\Terminal::feed`, `EscapeDecoder::decode`, `Palette::degrade`, `Ansi::*`, `Freeze\AnsiParser::parse`, `Spark\Inspector::parse`) with `php -r`; expected values taken from ansicode.txt line numbers (index in the companion findings file) and xterm control-sequence conventions where ansicode predates (DECSCUSR, 2026, 1049, SGR 38/58). Parser-layer results (candy-ansi) are strong: a conforming VT500 state machine (8-bit C1, CAN/SUB abort, DEL-inside-escape, C0-inside-ESC resume, string caps) — the defects are concentrated in **handlers, defaults, the emitter's hand-rolled strip, and the input decoder's reply handling**.

## Recommended fix order (dependency-wise)

1. candy-ansi: colon sub-param fidelity (unblocks candy-vt + candy-freeze + sugar-spark colour bugs) · param-overflow merge · strip/sanitize rewrite over the state machine (candy-core).
2. candy-vt TEA: DECAWM default+pending-wrap, RIS/DECSTR, IL/DL, SCS/DECALN, DECSC slot split, sync-erase by-reference, scrollback gating.
3. candy-vt vcr: route ESC/execute through the same handlers (or delete the duplicate engine).
4. candy-input: reply swallowing (private CSI, CPR `R`, X10 `M`, kitty un-gating, `Z`, focus prefix-match).
5. candy-palette: degrade regex/group fixes + real capability queries behind a `--probe` opt-in.
